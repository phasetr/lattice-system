# audit-gate allowlist (exempts a declaration name from BOTH V1 and V2)
# One Lean name per line.
#   - V1: public book theorems (capstones) that are legitimately zero-referenced.
#   - V2: a declaration a reviewer has confirmed is NOT a real duplicate despite a
#         matching displayed statement (e.g. distinct ambient `variable` context
#         that the textual gate cannot see).
# Add a name only after confirming the finding is a genuine false positive.

# V2 false positive: 60-line truncation artifact. The two theorems below share a
# long common hypothesis prefix (AnisotropicHeisenbergSpinSCaseIIBlockPFMin.lean
# :361 and :446); the gate truncates the compared statement at 60 lines before
# reaching the differing conclusions (finrank ≤ 1 vs `mulVec Φ = 0` with an extra
# Φ hypothesis), so they are NOT real duplicates.
anisotropicHeisenbergS_case_ii_target_finrank_le_one_of_reachability_pf_min_path
anisotropicHeisenbergS_case_ii_target_zero_magnetization_of_reachability_pf_min_path

# V1 capstone: the balanced-sector positive-definite ground coefficient, the assembly of
# Tasaki §10.2.4 Lemma 10.10 (`R ≠ 0 ⟹ R.PosDef`) on the balanced (Ŝ³=0) sector (#4852).
# Legitimately zero-referenced book-lemma milestone: the Theorem 10.2 chain consumes the shared
# reusable dichotomy core `posDefCompress_dichotomy` (design GLUE 1), not this existence packaging,
# so this faithful Lemma 10.10 statement stays a documented zero-referenced milestone.
# (Its PR40f precursor exists_posSemidefW_ground_in_balanced_sector is now consumed here.)
exists_posDefCompress_ground_in_balanced_sector

# NOTE (tier2 2026-07-03): two former entries were DELISTED after the Theorem 10.2 discharge because
# they are now genuinely CONSUMED (real term references), so the V1 gate no longer needs to exempt
# them: `exists_signDefiniteCompress_ground_in_balanced_sector` (Lemma 10.9 existence, consumed at
# LiebAttractiveSingletGround.lean:183) and `attractiveHubbardFullSectorGround_unique_singlet`
# (full-sector milestone, consumed at LiebAttractiveTheorem102.lean:82). Their decls stay in place.

# V1 book capstone: Tasaki §10.2.4 Theorem 10.2 (Lieb's theorem for the attractive Hubbard model),
# now PROVED axiom-free (PR-E, #4852): for even Ne with 0 < Ne ≤ 2|Λ|, connected symmetric hopping
# and U > 0, the Ne-electron ground state is unique and a spin singlet. Assembles the plain-space
# full-sector singlet-uniqueness milestone into the Euclidean `IsUniqueGroundStateOn` predicate.
# Legitimately zero-referenced book capstone (documented §10.2 Theorem 10.2); Theorem 10.3 (Tian)
# consumes this unique ground as a hypothesis.
theorem_10_2_lieb_attractive_unique_singlet

# V1 book capstone: Tasaki §10.2.4 Theorem 10.3 (Tian's pair-correlation positivity for the
# attractive Hubbard model), now PROVED axiom-free (PR #4949): under Theorem 10.2 hypotheses
# (even Ne, symmetric hopping, U > 0), the unique ground state has strictly positive on-site
# pair-transfer correlation ⟨ΦGS| ĉ†_{x↑}ĉ†_{x↓}ĉ_{y↓}ĉ_{y↑} |ΦGS⟩ > 0. Tian's reflection-positivity
# extension + balanced-sector positive-definite compression + pair-hopping transfer-matrix
# factorization + trace lemma for PosDef×PosSemidef products. Legitimately zero-referenced
# book capstone (documented §10.2 Theorem 10.3).
theorem_10_3_tian_pair_correlation_positive

# PERMANENT book capstone (Tasaki §10.2.2 Theorem 10.5, Shen–Qiu–Tian transverse-spin sign):
# on the general spin-z sector Ŝ³ = m (m = (Ne−(N+1))/2, even Ne, 0 < Ne < 2(N+1)) the unique
# repulsive-Hubbard ground state has transverse spin correlation strictly positive on the same
# sublattice and strictly negative across sublattices. Proved axiom-free (#4852) by Shiba transport
# via repulsiveSpinZSector_ground_unique + spin↔pair conjugation (eq. 10.2.13) + Theorem 10.3.
# Replaces the removed over-strong per-sector axiom. Legitimately zero-referenced book milestone.
theorem_10_5_shen_qiu_tian_transverse_sign

# PERMANENT book capstone (Tasaki §10.2.5 Theorem 10.11, Kubo–Kishi finite-temperature
# susceptibility bound): at half-filling (μ = U/2) the charge and on-site pairing Duhamel
# susceptibilities of the uniform repulsive Hubbard model on a bipartite connected lattice
# satisfy χ^c_q ≤ 1/U and χ^p_q ≤ 2/U for every β > 0 and wave number q (eq. 10.2.56).
# Tasaki states this WITHOUT proof, citing Kubo–Kishi, Phys. Rev. B 41, 4866 (1990); recorded
# as a documented axiom (same policy as mielke_theorem_11_13). Legitimately zero-referenced.
theorem_10_11_kubo_kishi_susceptibility_bound

# V1 book capstone: Tasaki §4.2.2 Theorem 4.8 (the Tanaka symmetry-breaking state is low-lying),
# now PROVED axiom-free (PR-E, #4958): there is one pair of positive constants (C₁, C₂) satisfying
# both IsAndersonTowerConstants (Theorem 4.6) and IsTanakaSSBConstants (Theorem 4.8). Discharges the
# former `tanakaSSB_lowLying_energy_bound` axiom by reducing the Tanaka state's Rayleigh quotient to
# the Theorem 4.6 double-commutator machinery via the central-binomial (Pascal) cancellation
# C(2(k-1),k-1)/C(2k,k) = k/(2(2k-1)) ≤ 1/2 (eqs. (4.2.69)–(4.2.71)). Legitimately zero-referenced
# book capstone; the PR-A…PR-D arc tips (numerator/denominator/telescope/cross-term/charge pieces)
# are now genuinely CONSUMED by this capstone chain, so their former allowlist entries were delisted.
tanakaSSB_lowLying_energy_bound

# §4.2.2 Theorem 4.9 (Tanaka full symmetry breaking) — the discharged book capstone (#4967, PR5).
# The former axiom `tanakaSSB_full_symmetry_breaking` is now a PROVED theorem
# (`AndersonTowerTheorem49.lean`) that CONSUMES the whole Theorem 4.9 arc: the (4.2.14) transverse
# moments (`tanakaOrderMean2_eq_zero`/`tanakaOrderMean3_eq_zero`), the (4.2.12)/(4.2.13) lower bounds
# (`sqrt_q0_le_tanakaOrderMean1`/`q0_le_tanakaOrderSecond1`), and the (4.2.15) fluctuation decay
# (`tanakaOrderSecond2_le`).  Those arc tips are therefore DELISTED here (same lifecycle as the
# delisted Theorem 4.8 PR-A…PR-D tips); only the capstone remains allowlisted.
tanakaSSB_full_symmetry_breaking

# V1 book capstone: §4.2.2 Theorem 4.11 (the two order parameters), eq. (4.2.23), p. 101 — the
# discharged book capstone (#5006, PR-3, `AndersonTowerTheorem411.lean`).  The former axiom
# `tanakaSSB_orderParameter_lowerBound` is now a PROVED theorem `√(3 q₀) ≤ m∗` that CONSUMES the
# Conjecture-4.12-free base-ratio limit `orderSqMoment_baseRatio_tendsto_threeQ` (`→ 3 q₀`, PR-1) and
# the documented `SU(2)`/`ô²` concentration axiom `orderSqMoment_ratio_le_mStarSq_family` (PR-2, the
# `p̂`/`U(1)` mirror companion).  Legitimately zero-referenced terminal book statement (Conjecture
# 4.12 `(m∗)² = 3 q₀` is only this "easy half" `≥`, never asserted; `IsConjecture412Equality` does
# not appear in `#print axioms`).
tanakaSSB_orderParameter_lowerBound

# TEMPORARY — §4.1 Theorem 4.2 (#4777) bond-square field infrastructure (PR-BS1,
# `RingReflectionBondSquareField.lean`). The Hermiticity of Tasaki's bond-square field Hamiltonian
# (4.1.48) via the reduction identity to the linear-core form (Tasaki §4.1, book p.86). Supporting
# theorem of the PR-BS1 reduction identity `ringBondSquareFieldHamiltonian_eq`; both are zero-referenced
# until PR-BS2 (scalar-constant Hermiticity repair) consumes them. RE-EVALUATE at PR-BS2 when the
# bond-square scalar constant and the reflected field Hamiltonians enter the reflection-positivity
# chain; DELIST if consumed or sunset if replaced by a different scalar-repair route.
ringBondSquareFieldHamiltonian_isHermitian

# TEMPORARY arc-tip (temp-then-delist lifecycle) — §4.1 Theorem 4.2 (#4777) bond-square partition-function
# infrastructure (PR-BS2, `RingReflectionBondSquareFieldPartition.lean`). Scaled reduction identity
# `Z^{BS}_β(h) = e^{−βC(h)} · Z^{repo}_β(kOf h)` (bond-square partition function = scalar factor ×
# linear-core partition function, Tasaki §4.1 (4.1.49), p.86). Zero-referenced ONLY until PR-BS10
# (uniform-field bound) consumes it via the scaled reduction Z^{BS}_β(h) = e^{−βC(h)} · Z^{repo}_β(kOf h)
# to collapse each `Z_β(staggered_c)` to `Z_β(0)`. RE-EVALUATE/DELIST when consumed by PR-BS10.
ringBondSquareFieldPartitionRe_eq_scaled

# TEMPORARY arc-tip (temp-then-delist lifecycle) — §4.1 Theorem 4.2 (#4777) bond-square constant-field
# collapse (PR-BS3, `RingReflectionBondSquareFieldPartition.lean`). Partition-function collapse
# `Z^{BS}_β(fun _=>c)=Z^{repo}_β(0)` (constant field → zero field on even ring). Zero-referenced until
# PR-BS10 (uniform-field bound) consumes it. RE-EVALUATE/DELIST when consumed by PR-BS10.
ringBondSquareFieldPartitionRe_const

# TEMPORARY arc-tip (temp-then-delist lifecycle) — §4.1 Thm 4.2 (#4777) bond-square DLS (PR-BS6,
# `RingReflectionBondSquareTwoFieldWeight.lean`). Diagonal collapse `W(a,a)` of the two-field weight.
# Zero-referenced until PR-BS8 consumes the diagonal slot. RE-EVALUATE/DELIST when consumed by PR-BS8.
ringBondSquareTwoFieldWeight_self

# TEMPORARY arc-tip (temp-then-delist lifecycle) — §4.1 Thm 4.2 (#4777) bond-square reflection Cauchy–Schwarz capstone (PR-BS7, `RingReflectionTwoFieldCauchySchwarz.lean`).
# The bond-square variant of Tasaki's reflection Cauchy–Schwarz inequality: `(Re Tr W^{BS}(a,b))² ≤ Re Tr W^{BS}(a,a)·Re Tr W^{BS}(b,b)`.
# Zero-referenced until PR-BS8 (physical identification + reflection step) consumes it.
# RE-EVALUATE/DELIST when consumed by PR-BS8.
ringBondSquareTwoFieldWeight_reflection_cauchySchwarz

# NOTE (PR-BS8a-ii, #4996): the PR-BS8a-i tips `ringBondSquareFieldHamiltonian_ungauged_dls` and
# `ringBondSquareLeftBondSum_eq_leftCouplingBulk` are now DELISTED — both are genuinely CONSUMED by
# the PR-BS8a-ii gauge crux (G) `rightGauge_conj_ringBondSquareFieldHamiltonian`
# (`RingReflectionBondSquareGaugeCrux.lean`): the ungauged split is conjugated via the right-half
# Marshall gauge and the left field bulk bridge reindexes the gauged intra-left piece.
# Their decls stay in place (same delist lifecycle as the Theorem 4.8 / Theorem 4.9 arc tips).

# NOTE (PR-BS8b, #4997): the PR-BS8a-ii gauge crux (G) `rightGauge_conj_ringBondSquareFieldHamiltonian`
# (`RingReflectionBondSquareGaugeCrux.lean`) is now DELISTED — it is genuinely CONSUMED by the PR-BS8b
# physical identification stage (`RingReflectionBondSquarePhysId.lean`): conjugation of the partition
# function via the unitary Marshall gauge and gauge-invariance of trace yields the identification
# `Z^{BS}(physBondSquareFieldOf a b) = Re Tr W^{BS}(a,b)`. Its decl stays in place (same delist
# lifecycle as the Theorem 4.8 / Theorem 4.9 arc tips).

# NOTE (PR-BS9): the PR-BS8b physical-field one reflection step `ringBondSquareFieldPartitionRe_reflection_step`
# (`RingReflectionBondSquarePhysId.lean`) is now DELISTED — it is genuinely CONSUMED by the PR-BS9
# chessboard Gaussian-domination capstone `ringBondSquareFieldPartition_gaussianDomination`
# (`RingReflectionBondSquareGaussianDomination.lean`) as the `hrefl` reflection bound of the classical
# averaging lemma applied to `−log Z^{BS}_β(g)`. Its decl stays in place (same delist lifecycle as
# the Theorem 4.8 / Theorem 4.9 arc tips).

# NOTE (PR-BS10): the PR-BS9 chessboard Gaussian-domination capstone `ringBondSquareFieldPartition_gaussianDomination`
# (`RingReflectionBondSquareGaussianDomination.lean`) is now DELISTED — it is genuinely CONSUMED by the PR-BS10
# uniform-field bound `ringBondSquareFieldPartitionRe_uniform_bound`
# (`RingReflectionBondSquareUniformBound.lean`) as the `hchess` chessboard bound of the algebraic collapse.
# Its decl stays in place (same delist lifecycle as the Theorem 4.8 / Theorem 4.9 arc tips).

# TEMPORARY arc-tip (temp-then-delist lifecycle) — §4.1 Thm 4.2 (#4777) bond-square uniform-field bound
# capstone (PR-BS10, `RingReflectionBondSquareUniformBound.lean`). The uniform-field bound
# `Z^{BS}_β(h) ≤ Z^{BS}_β(0)` (Tasaki §4.1 uniform-field bound (4.1.49)/(4.1.52), pp. 85–86). Terminal
# capstone of the bond-square Gaussian-domination arc: purely algebraic glue over the PR-BS9 chessboard
# bound `Z^{BS}_β(h)^{2n} ≤ Π_j Z^{BS}_β(fun _=>h j)` and the PR-BS3 constant-field collapse
# `Z^{BS}_β(fun _=>c) = Z^{repo}_β(0)` (each factor collapses exactly at the operator level). Zero-referenced
# until the downstream Fourier/IR → susceptibility → Theorem 4.2 arc consumes it. RE-EVALUATE/DELIST when
# consumed.
ringBondSquareFieldPartitionRe_uniform_bound

# NOTE (PR-χ1, #4777): the PR-χ1 terminal capstone `ringBondSquareFieldHamiltonian_hermitianMinEigenvalue_ge_field_zero`
# (`RingReflectionBondSquareGroundEnergy.lean`) — the ground-state energy uniform-field bound
# `E_GS(h) ≥ E_GS(0)` (Tasaki §4.1 (4.1.40)/(4.1.49), pp. 85–86) from finite-β partition passage
# to zero temperature — is now DELISTED (PR-χ2b, #4777). It is genuinely CONSUMED by the PR-χ2b
# susceptibility sum rule capstone `ringBondSquareField_susceptibility_sum_rule`
# (`RingReflectionBondSquareSusceptibilitySumRule.lean:286`): the ground-energy bound provides the
# second-order coefficient extraction from the trial-state Rayleigh quotient in the variational bound.
# Its decl stays in place (same delist lifecycle as the Theorem 4.8 / Theorem 4.9 arc tips).

# NOTE (PR-χ2b, #4777): the PR-χ2a tips `hermitian_posSemidef_exists_orthogonal_potential`
# (Moore–Penrose pseudoinverse; `HermitianPseudoinverse.lean` def) and `ringBondSquareLinFieldOp_groundState_expectation_zero`
# (first-order vanishing `⟨Φ|V|Φ⟩ = 0`; `RingReflectionBondSquareSusceptibility.lean` def) are now DELISTED
# — both are genuinely CONSUMED by the PR-χ2b susceptibility sum rule capstone
# `ringBondSquareField_susceptibility_sum_rule` (`RingReflectionBondSquareSusceptibilitySumRule.lean`):
# the pseudoinverse potential is the crux of the variational bound (line 183) and the first-order vanishing
# is the λ²-coefficient reduction (line 156). Their decls stay in place (same delist lifecycle as the
# Theorem 4.8 / Theorem 4.9 arc tips).

# TEMPORARY arc-tip (temp-then-delist lifecycle) — §4.1 Thm 4.2 (#4777) susceptibility sum rule
# capstone (PR-χ2b, `RingReflectionBondSquareSusceptibilitySumRule.lean`). The variational bound on the
# susceptibility of the linear-in-field Zeeman operator `χ_b = Re⟨y, VΦ⟩ ≤ ½Σ(f_x+f_{x+1})²` for a unique
# ground state of the field-free ring Heisenberg Hamiltonian (Tasaki §4.1 eq. (4.1.41), book p. 84):
# the pseudoinverse potential `(H₀−E₀)y = VΦ` with `y ⊥ Φ` from PR-χ2a is used with the ground-energy
# bound (χ1) to extract the second-order coefficient from a trial-state Rayleigh quotient expansion.
# Zero-referenced until PR-χ3 (Green-function susceptibility bound via Shastry propagator) consumes it.
# RE-EVALUATE/DELIST when consumed by PR-χ3.
ringBondSquareField_susceptibility_sum_rule

# Tasaki §4.1 Corollary 4.3 (absence of long-range order in 1D), `NoLongRangeOrder1D.lean`, PR #5003.
# Zero-referenced public book capstone: `lim_{L↑∞} ⟨Φ_GS|(Ô_L^(3)/L)²|Φ_GS⟩ = 0` (eq. (4.1.11)),
# discharged from an axiom into a theorem via the documented Shastry susceptibility axiom
# `shastry_staggered_susceptibility_bound` fed into `no_long_range_order_1d_of_susceptibility`.
# Only referenced (as a comment) by AndersonTower.lean; a terminal book corollary, legitimately ref-0.
no_long_range_order_1d

# §4.2 Prop 4.10 (#4974) BOOK CAPSTONE — the arc's terminal book theorem,
# `Quantum/SpinS/AndersonTowerSphereGroundState.lean`.  Tasaki Proposition 4.10 (eq. (4.2.22),
# p. 100): the normalized solid-angle average of the symmetry-breaking states converges (up to a
# unimodular phase) to the ground state, conditional on Conjecture 4.12 (explicit hypothesis, never
# asserted) and the `ô²`-concentration documented axiom `orderSqMoment_ratio_le_mStarSq`; bundled
# with Theorem 4.9's constants.  Legitimately ref-0 (terminal book statement).  The PR-2..6c arc of
# temporary tips is now consumed by this assembly and delisted.
tanakaSphereAverage_groundState

# TEMPORARY arc-tip (temp-then-delist lifecycle) — §4.2 Prop 4.10 (#4974) PR-4b,
# `Quantum/SpinS/AndersonTowerSphereMomentRatio.lean`. The sphere-average relative-close estimate
# `|⟨Φ, (∫ (Ô_L^n)^{2m} dσ) Φ⟩.re − (4π/(2m+1)) R_m| ≤ (cartPinchPoly m · N^{2m−2} / (q₀^m · V²)) · R_m`
# (`R_m = ⟨Φ, (ô²)^m Φ⟩.re`, `V = L^d`): the pinch absolute remainder divided by the `ô²`-moment
# lower bound, cancelling the volume exponent `V^{2m−2}/V^{2m} = 1/V²` into a relative `O(1/V²)` error
# (Tasaki §4.2.2, eqs. (4.2.58)/(4.2.59), p. 108).  Zero-referenced until the ratio-limit collapse
# (PR-5) consumes it. RE-EVALUATE/DELIST when consumed.
# (The PR-3.3b pinch tip `cartWord_sphereAverage_pinch` and the PR-4a `ô²`-moment lower bound
# `orderSqMoment_ge` are now consumed by this relative-close estimate and were delisted.)
sphereAverage_orderSqMoment_relative_close

# TEMPORARY arc-tip (temp-then-delist lifecycle) — §4.2 Prop 4.10 (#4974) L5-b-iii,
# `Quantum/SpinS/AndersonTowerOrderSqGeom.lean`.  The fixed-`n` geometric moment limit
# `lim_L R_n/(R_0·V^{2n}) = (m∗)^{2n}` for a total-spin-singlet ground-state family, a squeeze of the
# log-convex lower bound `s_0^n ≤ R_n/(R_0·V^{2n})` (`orderSqMoment_geom_lower`, driven to `(m∗)^{2n}`
# by the base ratio limit `orderSqMoment_baseRatio_tendsto`) against the concentration upper bound
# `orderSqMoment_ratio_le_mStarSq` telescoped through `T_{n+1} = s_n·T_n` (Tasaki §4.2.2,
# eqs. (4.2.37), (4.2.57)–(4.2.61), p. 105–108).  Conditional on Conjecture 4.12 (never asserted).
# Zero-referenced standalone milestone tip until consumed downstream.  RE-EVALUATE/DELIST when consumed.
orderSqMoment_geom_tendsto

# Tasaki §4.2.1 Theorem 4.13 (SSB under an infinitesimal staggered field), PROVED axiom-free
# (`#print axioms` = propext/Classical.choice/Quot.sound only).  The `lim_{h↓0} liminf_{L↑∞}` staggered
# moment of the field ground state is `≥ m∗` (eqs. (4.2.27)–(4.2.28), pp. 102–103) via the Rayleigh–Ritz
# (KHVL) variational method of §3.4 Thm 3.2, with the Tanaka state as trial state, the proved companion
# `tanakaSSB_realizingFamily_energyBound` (Thm 4.8) as energy input, and the family's own moment limit.
# Book-capstone tip (Thm 4.13 is the §4.2.1 SSB payoff); the energy companion below is now consumed here.
tanakaSSB_field_lowerBound

# TEMPORARY arc foundations (temp-then-delist lifecycle) — Tasaki §5.3 Theorem 5.2 (BEC low-lying
# tower states) discharge arc PR-1 (#5051), `Quantum/SpinS/BoseEinsteinCondensateAlgebra.lean`.  These
# two Hamiltonian-independent foundation lemmas are proved axiom-free (`#print axioms` =
# propext/Classical.choice/Quot.sound only) and are consumed by the later PRs of the same arc:
#   - `xyChemicalPotentialHamiltonianS_isHermitian` (Ĥ_μ Hermitian) — PR-3/PR-5 double-commutator gap;
#   - `totalSpinSOp3_commutator_towerPow` ([Ŝ³_tot, tower]=M·tower, §4-a) — PR-3 −μ Ŝ³_tot term.
# Zero-referenced foundation tips until consumed downstream.  RE-EVALUATE/DELIST when consumed.
# (The former `Ldhalf_bridge` entry was DELISTED: it moved to the generic real-analysis home
# `Math/Analysis/RealRpowNatSqrt.lean` as `LatticeSystem.Math.Ldhalf_bridge` and is now genuinely
# consumed by the two Anderson-tower sites `AndersonTowerTheorem46.lean` / `AndersonTowerTanakaCapstone.lean`.)
xyChemicalPotentialHamiltonianS_isHermitian
totalSpinSOp3_commutator_towerPow

# TEMPORARY arc-tip (temp-then-delist lifecycle) — Tasaki §5.3 Theorem 5.2 (BEC low-lying tower
# states) discharge arc PR-2 (#5051), `Quantum/SpinS/BoseEinsteinCondensateMoment.lean`.  The
# U(1)-planar base entry `2 q₀ ‖Φ‖² ≤ ⟨Φ, p̂ Φ⟩` (Tasaki §5.3 eq. (5.2.5) → eq. (4.2.37) base,
# p. 139/105) driven directly by the two XY-plane ODLRO hypotheses (α = 1, 2), replacing the SU(2)
# singlet/isotropy base of Theorem 4.6 (`phatMoment_one_ge_of_lro`).  Proved axiom-free
# (`#print axioms` = propext/Classical.choice/Quot.sound only).  Zero-referenced until PR-4/PR-5 feed
# it into the singlet-free recursion `phatMoment_succ_ratio` for the tower denominator lower bound.
# RE-EVALUATE/DELIST when consumed by PR-4/PR-5.
phatMoment_one_ge_of_planar_lro

# TEMPORARY arc-tip (temp-then-delist lifecycle) — Tasaki §5.3 Theorem 5.2 (BEC low-lying tower
# states) discharge arc PR-3 (#5056), `Quantum/SpinS/BoseEinsteinCondensateNumerator.lean`.  The exact
# variational-numerator algebra of the `−μ Ŝ³_tot` term: the double commutator of
# `Ĥ_μ = 2 Ĥ_XY − μ Ŝ³_tot` with the tower operator splits as
# `[Aᴴ,[Ĥ_μ,A]] = [Aᴴ,[2 Ĥ_XY,A]] − (μM)·[Aᴴ,A]` (`chemicalPotential_double_commutator_muTerm`,
# §4-b), with real-part numerator `⟨Φ,[Aᴴ,[2 Ĥ_XY,A]]Φ⟩ − μM(‖AΦ‖²−‖AᴴΦ‖²)`
# (`chemicalPotential_numerator_muTerm`, §4-c); the `[Ŝ³_tot,A]=M·A` input is PR-1's
# `totalSpinSOp3_commutator_towerPow`.  Proved axiom-free (`#print axioms` =
# propext/Classical.choice/Quot.sound only).  Zero-referenced until the PR-5 assembly feeds them into
# `variational_gap_le_double_commutator` for the cubic energy bound.  RE-EVALUATE/DELIST when consumed
# by PR-5.
chemicalPotential_double_commutator_muTerm
chemicalPotential_numerator_muTerm
