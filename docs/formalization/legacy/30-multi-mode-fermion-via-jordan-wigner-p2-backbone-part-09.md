---
layout: page
title: "Legacy catalogue: Multi-mode fermion via Jordan–Wigner (P2 backbone) (part 9 of 9)"
permalink: /formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-09/
---

# Legacy catalogue: Multi-mode fermion via Jordan–Wigner (P2 backbone) (part 9 of 9)

> **Interim authority.** This lossless catalogue chunk remains authoritative for formalization status and capstone identification until Issue #5228. The version 1 JSON catalogue is still a non-authoritative prototype.

[Interim catalogue](/lattice-system/formalization/legacy/) › [Fermions and Hubbard models](/lattice-system/formalization/legacy/#group-fermions-hubbard)

## Authoritative supplemental implementation record (Theorem 11.4 discharge arc, PR-1: axiom hypothesis correction)

This section is maintained by hand, lies outside the migrated catalogue block, and records new
supporting declarations added after the migration baseline (Issue #5363, PR #5365); it is not
subject to the frozen byte-for-byte parity of the migrated block. `Theorem 11.4` itself has no
prior row in this catalogue (the axiom predates the migrated block and was never listed); this
section opens its entry.

| Lean name | Statement | File |
|---|---|---|
| `hubbard_theorem_11_4` | **Tasaki Theorem 11.4 (impossibility of ferromagnetism at low densities), now PROVED — axiom discharged** (Tasaki §11.1.1, eqs. (11.1.8)–(11.1.10), p. 376; Theorem 11.4 discharge arc, Issue #5363, PR #5365 PR-1). Corrects an over-quantification in the axiom statement rather than proving it: adds a hopping row-sum bound `_hK : ∀ x, ∑ y, ‖t x y‖ ≤ K` and a filling floor `_hNen₀ : 2 * n₀ ≤ Ne`. `_hK` is required by the order of the quantifiers (the density threshold `ρ₁` is fixed before the hopping `t`, and the variational estimate's threshold is governed by the ratio `c/K`, so an a priori bandwidth bound `K` must be fixed in advance; every concrete `t` admits one, so only the quantifier order is restricted). `_hNen₀` puts the top occupied level inside the band condition's constrained range `n ≥ n₀` (below it — the flat-band regime where ferromagnetism does occur — the band condition (11.1.8) says nothing, so the variational argument gives no bound there); without it the axiom was **false** (over-quantified over the unconstrained flat-band regime). PR-1 itself added no new declarations: it only corrected the statement. **Discharged** by PR #5374 PR-7c of the same arc: the axiom became a theorem in place, its statement byte-for-byte unchanged, `#print axioms` = std3, by squeezing the ground energy between PR-6's Roth variational bound above and the fractional-occupation floor `sum_lowestLevels_mul_le_rayleighOnVec_hubbardKinetic` below, separated by the sublinear threshold `exists_pos_forall_mul_lt_rpow` at the explicit `ρ₁ = min ρ₀ (min (1/2) r)`. The PR-1 hypothesis correction recorded above still stands and is what makes the discharged statement true. | `Fermion/JordanWigner/Hubbard/HubbardImpossibilityLowDensity.lean` |

**Reconciliation note.** The PR-2–PR-7b rows below each say "`hubbard_theorem_11_4` itself
remains an axiom" — that phrase is accurate as of each row's own stage and is left unchanged as a
historical record of that stage; the row above is the current authoritative status and records
that the axiom was discharged into a theorem by PR #5374 PR-7c.

## Authoritative supplemental implementation record (Theorem 11.4 discharge arc, PR-2: spin-resolved kinetic layer)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-1 section above (Issue #5363, PR #5366); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `hubbardKineticSpin` / `hubbardKinetic_eq_sum_hubbardKineticSpin` / `hubbardKinetic_eq_hubbardKineticSpin_add` / `eigenbasisAsBasis_completeness_sum` / `eigenNumberOp_posSemidef` / `hubbardKineticSpin_eq_sum_eigenNumberOp` / `hubbardKinetic_eq_sum_eigenNumberOp` / `sum_eigenNumberOp_eq_sum_spinSiteNumber` / `hubbardKineticSpin_le_smul_sum_spinSiteNumber` / `fermionDownAnnihilation_mulVec_eq_zero_of_downNumber_zero` / `hubbardKineticSpin_one_mulVec_eq_zero_of_downNumber_zero` | **The spin-resolved kinetic operator layer** (Tasaki §11.1.1, eqs. (11.1.8)–(11.1.10), p. 376, underlying argument Tasaki, Prog. Theor. Phys. **99** (1998) 489, Theorem 3.3, Appendix F, pp. 545–547; Theorem 11.4 discharge arc, Issue #5363, PR #5366 PR-2; **PROVED**, `#print axioms` = std3; pure infrastructure, `hubbard_theorem_11_4` itself remains an axiom). New file `HubbardKineticSpin.lean`: `hubbardKineticSpin` names the spin-`σ` fiber `Ĥ^σ = Σ_{i,j} t_{ij} ĉ†_{iσ}ĉ_{jσ}` of the kinetic operator; `hubbardKinetic_eq_sum_hubbardKineticSpin` (`rfl`, the spin sum is `hubbardKinetic`'s outermost layer) and `hubbardKinetic_eq_hubbardKineticSpin_add` record `Ĥ_kin = Σ_σ Ĥ^σ = Ĥ^↑ + Ĥ^↓`. `GeneralFlatBandEigenbasis.lean`'s `eigenbasisAsBasis_completeness_sum` is the coordinate form of eigenbasis completeness `Σ_j e_j(y)·conj(e_j(x)) = δ_{yx}`, dual to the pre-existing orthonormality sum; `GeneralFlatBandSpanning.lean`'s `eigenNumberOp_posSemidef` shows the eigenmode number operator is a Gram matrix, hence positive-semidefinite. `HubbardImpossibilityLowUTrial.lean` splits the existing whole-kinetic eigenmode diagonalization into a per-spin lemma `hubbardKineticSpin_eq_sum_eigenNumberOp` (`Ĥ^σ = Σ_j ε_j n̂_{j,σ}`) with the old statement `hubbardKinetic_eq_sum_eigenNumberOp` kept as its spin-summed corollary; `HubbardImpossibilityLowUTrialCore.lean`'s `sum_eigenNumberOp_eq_sum_spinSiteNumber` (`Σ_j n̂_{j,σ} = Σ_x n̂_{x,σ}`) is the operator form of completeness. New file `HubbardKineticSpinBounds.lean` supplies the two estimates the low-density variational argument needs in place of an exact evaluation: `hubbardKineticSpin_le_smul_sum_spinSiteNumber` is the **Loewner bound** `Ĥ^σ ≤ e·N̂_σ` whenever every level satisfies `ε_j ≤ e` (the difference is a nonnegative combination of positive-semidefinite eigenmode number operators); `fermionDownAnnihilation_mulVec_eq_zero_of_downNumber_zero` and `hubbardKineticSpin_one_mulVec_eq_zero_of_downNumber_zero` are the **fully-polarized kill** `N̂_↓Φ = 0 ⟹ Ĥ↓Φ = 0` (the down-number operator is a sum of Gram matrices, so a vanishing total forces each term, hence each down annihilator, to vanish). **Debt.** None of these 11 declarations is yet consumed outside this file's own internal chain (`hubbardKineticSpin_eq_sum_eigenNumberOp` feeds `hubbardKinetic_eq_sum_eigenNumberOp`; `sum_eigenNumberOp_eq_sum_spinSiteNumber` feeds `hubbardKineticSpin_le_smul_sum_spinSiteNumber` via `eigenbasisAsBasis_completeness_sum`/`eigenNumberOp_posSemidef`) — every declaration is staged with a named consumer in the arc's later PRs, not decoration: `hubbardKineticSpin`/`hubbardKinetic_eq_hubbardKineticSpin_add`/`hubbardKineticSpin_eq_sum_eigenNumberOp` for the arc's PR-6 energy split; `fermionDownAnnihilation_mulVec_eq_zero_of_downNumber_zero`/`hubbardKineticSpin_one_mulVec_eq_zero_of_downNumber_zero` for PR-4 and PR-7. If the arc's later PRs end up not consuming `hubbardKinetic_eq_hubbardKineticSpin_add`, `hubbardKineticSpin_le_smul_sum_spinSiteNumber`, or `hubbardKineticSpin_one_mulVec_eq_zero_of_downNumber_zero`, those three are to be **deleted** as reference-0 decoration rather than kept "for later" — matching this catalogue's general policy for unconsumed staged declarations (see e.g. the Theorem 10.4/10.8 arc rows above). Axiom-free. | `Fermion/JordanWigner/Hubbard/HubbardKineticSpin.lean` + `Fermion/JordanWigner/Hubbard/HubbardKineticSpinBounds.lean` + `Fermion/JordanWigner/Hubbard/GeneralFlatBandEigenbasis.lean` + `Fermion/JordanWigner/Hubbard/GeneralFlatBandSpanning.lean` + `Fermion/JordanWigner/Hubbard/HubbardImpossibilityLowUTrial.lean` + `Fermion/JordanWigner/Hubbard/HubbardImpossibilityLowUTrialCore.lean` |

## Authoritative supplemental implementation record (Theorem 11.4 discharge arc, PR-3: the on-site interaction on the one-↓-electron sector)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-1 section above (Issue #5363, PR #5367); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `hubbardOnSiteInteraction_mulVec_mulVec_of_downNumber_one` / `fermionTotalDownNumber_mulVec_hubbardOnSiteInteraction_mulVec_of_downNumber_one` / `hubbardOnSiteInteraction_mulVec_sub_self_eq_zero_of_downNumber_one` / `dotProduct_star_self_sub_hubbardOnSiteInteraction_re_of_downNumber_one` | **The on-site interaction on the one-↓-electron sector** (Tasaki §11.1.1, eqs. (11.1.9)/(11.1.10), p. 376, and the underlying computation Tasaki, Prog. Theor. Phys. **99** (1998) 489, Theorem 3.3, Appendix F, eqs. (F.1)/(F.2), p. 545; Theorem 11.4 discharge arc, Issue #5363, PR #5367 PR-3; **PROVED**, `#print axioms` = std3; `hubbard_theorem_11_4` itself remains an axiom). New file `HubbardOnSiteInteractionSingleDown.lean`, stated for an arbitrary vector `v` of the sector `N̂_↓v = v` (not for the Roth trial state itself). `hubbardOnSiteInteraction_mulVec_mulVec_of_downNumber_one`: `ν̂(ν̂v) = ν̂v` (`ν̂ = hubbardOnSiteInteraction M 1 = Σ_x n̂_{x↑}n̂_{x↓}`), since a configuration carrying a single ↓ electron has at most one doubly occupied site, so `ν̂`'s diagonal eigenvalue there is `0` or `1`, hence idempotent. `fermionTotalDownNumber_mulVec_hubbardOnSiteInteraction_mulVec_of_downNumber_one`: `N̂_↓(ν̂v) = ν̂v`, i.e. the doubly occupied part stays inside the sector. `hubbardOnSiteInteraction_mulVec_sub_self_eq_zero_of_downNumber_one`: `Ĥ_int(U)(v − ν̂v) = 0` for **every** `U` — the `(1 − ν̂)` form of the projected vector is eq. (F.1) (`Ψ̃ = P̂₀Ψ = (1 − ν̂)Ψ`, p. 545), while the vanishing itself is the book's statement that the state (11.1.9) minimizes the Coulomb interaction (p. 376) — since `Ĥ_int(U) = U·ν̂` and idempotency kills the difference regardless of the coupling, which is the source of the `U`-independence of the variational bound. `dotProduct_star_self_sub_hubbardOnSiteInteraction_re_of_downNumber_one`: `‖v − ν̂v‖²_re = ‖v‖²_re − ⟨v,ν̂v⟩_re` (eq. (F.2)), via `ν̂`'s Hermiticity and idempotency collapsing the cross terms; stated on real parts (not the `ℂ` identity) because that is the shape `rayleighOnVec` consumes. **Debt.** Outside this file the lemmas are so far consumed only by the test file `Tests/HubbardImpossibilityLowDensity.lean` — Red 8 consumes `hubbardOnSiteInteraction_mulVec_sub_self_eq_zero_of_downNumber_one`, Red 12 consumes `dotProduct_star_self_sub_hubbardOnSiteInteraction_re_of_downNumber_one`, and Red 13 consumes `fermionTotalDownNumber_mulVec_hubbardOnSiteInteraction_mulVec_of_downNumber_one` by feeding `ν̂v` back through the sector hypothesis, while `hubbardOnSiteInteraction_mulVec_mulVec_of_downNumber_one` is consumed inside this file by the other proofs; each is staged with a named consumer in the arc's PR-6 energy-split/normalisation assembly (`hubbardOnSiteInteraction_mulVec_mulVec_of_downNumber_one` and `fermionTotalDownNumber_mulVec_hubbardOnSiteInteraction_mulVec_of_downNumber_one` feed the kinetic-energy split; `hubbardOnSiteInteraction_mulVec_sub_self_eq_zero_of_downNumber_one` feeds the `U`-independent interaction vanishing; `dotProduct_star_self_sub_hubbardOnSiteInteraction_re_of_downNumber_one` feeds the `(1−D)` Rayleigh normalisation) — if PR-6 ends up not consuming one, it is to be **deleted** as reference-0 decoration rather than kept "for later" (PR-2's row set this precedent). Axiom-free. | `Fermion/JordanWigner/Hubbard/HubbardOnSiteInteractionSingleDown.lean` |

## Authoritative supplemental implementation record (Theorem 11.4 discharge arc, PR-4: the spin-flip trial state and its Jordan–Wigner parity factorisation)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-1 section above (Issue #5363, PR #5368); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `fermionMultiHopping_commute_fermionMultiCreation_of_ne` / `fermionMultiHopping_commute_fermionMultiAnnihilation_of_ne` / `upHopping_commute_fermionDownCreation` / `upHopping_commute_fermionDownAnnihilation` / `fermionDownNumber_mulVec_spinfulCreationFromVector_of_downNumber_zero` / `dotProduct_fermionDownCreation_sandwich` / `dotProduct_star_self_spinfulCreationFromVector_of_downNumber_zero` / `hubbardLowDensityTrialState` / `fermionTotalDownNumber_mulVec_spinfulGeneralBasisState_empty` / `fermionTotalDownNumber_mulVec_hubbardLowDensityTrialState` / `fermionTotalNumber_mulVec_hubbardLowDensityTrialState` / `hubbardLowDensityTrialState_ne_zero` / `fermionDownNumber_mulVec_hubbardLowDensityTrialState` / `hubbardKineticSpin_mul_spinfulCreationFromVector` / `hubbardKineticSpin_commute_spinfulCreationFromVector_of_ne` / `hubbardKinetic_mulVec_hubbardLowDensityTrialState` | **The spin-flip trial state and its Jordan–Wigner parity factorisation** (Tasaki §11.1.1, eqs. (11.1.6)/(11.1.9), pp. 375–376, and the underlying computation Tasaki, Prog. Theor. Phys. **99** (1998) 489, Theorem 3.3, eq. (3.6), p. 506, with the anticommutation bookkeeping of Appendix F yielding eqs. (F.8)–(F.11), pp. 545–546; Theorem 11.4 discharge arc, Issue #5363, PR #5368 PR-4; **PROVED**, `#print axioms` = std3; `hubbard_theorem_11_4` itself remains an axiom). Three new files. `HoppingCommuteLadder.lean` (single-species, spin-free, beside the hole-projection analogue): a hopping bilinear `c_i† c_j` is an **even** element, so it commutes with `c_k†` and with `c_k` for `k` distinct from both `i` and `j` (`fermionMultiHopping_commute_fermionMultiCreation_of_ne`, `fermionMultiHopping_commute_fermionMultiAnnihilation_of_ne`) — two anticommutations whose signs cancel, which is why the arc needs **no** Jordan–Wigner string reasoning. `HubbardImpossibilityLowDensityTrialCore.lean`, stated for an arbitrary fully polarised `Φ` (`N̂_↓Φ = 0`) rather than a particular Slater determinant: `upHopping_commute_fermionDownCreation`/`upHopping_commute_fermionDownAnnihilation` are the spinful readings (the two species occupy disjoint Jordan–Wigner modes); `fermionDownNumber_mulVec_spinfulCreationFromVector_of_downNumber_zero` is the one-↓ contraction `n̂_{x↓}Ĉ†_↓(v)Φ = v_x ĉ†_{x↓}Φ` (the diagonal term by `n̂_x ĉ†_x = ĉ†_x`, every other term commuting past `n̂_{x↓}`, which then annihilates `Φ`); `dotProduct_fermionDownCreation_sandwich` is the δ factorisation `⟨ĉ†_{y↓}Φ, X ĉ†_{x↓}Φ'⟩ = δ_{xy}⟨Φ, XΦ'⟩` for any `X` commuting with the ↓ annihilations — the hypothesis is weaker than “even ↑-only” and the anticommutator remainders die on `ĉ_{y↓}Φ' = 0`; stated `ℂ`-valued (not on real parts, unlike PR-3's normalisation) because the later double sum contracts it against `conj(v_y)v_x` before taking real parts. `dotProduct_star_self_spinfulCreationFromVector_of_downNumber_zero` is that factorisation at `X = 1`, the norm `⟨Ĉ†_↓(v)Φ, Ĉ†_↓(v)Φ⟩ = (Σ_x |v_x|²)⟨Φ,Φ⟩`. `HubbardImpossibilityLowDensityTrial.lean`: `hubbardLowDensityTrialState` is Tasaki's `Ψ = Ĉ†_↓(v)Φ↑` (eq. (11.1.6)) as a `def` (the projected `Ψ̃` stays the explicit `v − ν̂v` of PR-3, so no second trial-state definition is introduced); `fermionTotalDownNumber_mulVec_spinfulGeneralBasisState_empty` supplies the polarisation hypothesis `N̂_↓Φ↑ = 0`; `fermionTotalDownNumber_mulVec_hubbardLowDensityTrialState` is `N̂_↓Ψ = Ψ`, which is exactly the sector hypothesis of the four PR-3 lemmas, and `fermionTotalNumber_mulVec_hubbardLowDensityTrialState` is `N̂Ψ = (|S↑|+1)Ψ`; `hubbardLowDensityTrialState_ne_zero` gives `Ψ ≠ 0` for `v ≠ 0` from the norm above; `fermionDownNumber_mulVec_hubbardLowDensityTrialState` is the trial-state form of the contraction; `hubbardKineticSpin_mul_spinfulCreationFromVector` is the same-spin relation `Ĥ^σ Ĉ†_σ(w) = Ĉ†_σ(w) Ĥ^σ + Ĉ†_σ(t·w)` (the contraction `δ_{jx}ĉ†_{iσ}` summed against `t_{ij}w_x` rebuilds the smeared creator of the transported vector) against `hubbardKineticSpin_commute_spinfulCreationFromVector_of_ne` for `σ ≠ τ`, which has no extra term; `hubbardKinetic_mulVec_hubbardLowDensityTrialState` combines them with PR-2's fully-polarised kill into the kinetic energy of the spin-flipped state (the book's unnumbered running text on p. 375; the numbered (11.1.7) is the total-energy inequality), `Ĥ_kin Ψ = (Σ_{j∈S↑} ε_j + ε)Ψ` when `t·v = εv` (the eigenvalue is kept `ℂ`-typed so later stages need no cast). **Debt.** Consumed today: `fermionMultiHopping_commute_fermionMultiCreation_of_ne` by the cross-spin commutation and by `upHopping_commute_fermionDownCreation`; `fermionMultiHopping_commute_fermionMultiAnnihilation_of_ne` by `upHopping_commute_fermionDownAnnihilation`; the contraction, the δ factorisation and the norm inside this arc's own files; and `fermionTotalDownNumber_mulVec_hubbardLowDensityTrialState`, `dotProduct_fermionDownCreation_sandwich`, `hubbardLowDensityTrialState_ne_zero`, `hubbardKineticSpin_mul_spinfulCreationFromVector`, `hubbardKineticSpin_commute_spinfulCreationFromVector_of_ne` and `hubbardKinetic_mulVec_hubbardLowDensityTrialState` by the test file `Tests/HubbardImpossibilityLowDensity.lean` (Red 14–19). Staged with named consumers in the arc's later PRs: `upHopping_commute_fermionDownCreation`/`upHopping_commute_fermionDownAnnihilation` and `fermionDownNumber_mulVec_hubbardLowDensityTrialState` for PR-6's double-occupancy weight and ↑-correction; `fermionTotalNumber_mulVec_hubbardLowDensityTrialState` for PR-7's `Ne`-electron sector hypothesis. If PR-6/PR-7 end up not consuming one of those four, it is to be **deleted** as reference-0 decoration rather than kept “for later” (PR-2/PR-3's rows set this precedent). Axiom-free. | `Fermion/JordanWigner/HoppingCommuteLadder.lean` + `Fermion/JordanWigner/Hubbard/HubbardImpossibilityLowDensityTrialCore.lean` + `Fermion/JordanWigner/Hubbard/HubbardImpossibilityLowDensityTrial.lean` |

## Authoritative supplemental implementation record (Theorem 11.4 discharge arc, PR-5: the delocalised lowest eigenmode)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-1 section above (Issue #5363, PR #5369); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `commute_toPEquiv_toMatrix_of_perm_invariant` / `norm_apply_eq_norm_apply_of_comp_perm_smul` / `eigenspace_mulVecLin_ne_bot_of_map_eq` / `exists_uniformModulus_eigenvector_of_transitive_perm_invariance` | **The delocalised eigenmode of a transitively permutation-invariant matrix** (Tasaki §11.1.1, Theorem 11.4, p. 376, discharging the delocalisation assumption `|ψ_x| = 1/√|Λ|`, which Tasaki, Prog. Theor. Phys. **99** (1998) 489, Theorem 3.3 does not derive but *assumes* as the upper bound `|ψ_x^{(p)}| ≤ α/√N_s` for a constant `α` (Appendix F, eq. (F·14), p. 547, `N_s = |Λ|`), with the remark that translation invariance in the sense of Appendix E gives `α = √m` for a unit cell of `m` sites — hence exactly `1/√|Λ|` on a site-transitive lattice, once normalisation saturates the bound; the Bloch form `ψ_x = e^{ik·x} v_{u(x)}` that translation invariance forces is Appendix E, eqs. (E·3)/(E·5), p. 543; Theorem 11.4 discharge arc, Issue #5363, PR #5369 PR-5; **PROVED**, `#print axioms` = std3; `hubbard_theorem_11_4` itself remains an axiom). One new **generic** file `Math/MatrixAnalysis/PermInvariantUniformEigenvector.lean` — the content mentions neither Hubbard, nor Tasaki, nor fermions, so it is organised by mathematical topic and sits beside its two dependencies rather than under the Hubbard tree. `commute_toPEquiv_toMatrix_of_perm_invariant`: a permutation symmetry `t (σ i) (σ j) = t i j` of the hopping matrix is exactly the commutation `P_σ · t = t · P_σ` with mathlib's permutation matrix `σ.toPEquiv.toMatrix` (both products are `t` reindexed by `σ`, reducing the claim to the hypothesis at `(i, σ⁻¹ j)`); **no new permutation-matrix `def` is introduced**, mathlib's `toPEquiv.toMatrix` is used inline. `norm_apply_eq_norm_apply_of_comp_perm_smul`: if `w ∘ σ = μ • w` with `w ≠ 0` and the powers of `σ` act transitively, then `x ↦ ‖w x‖` is constant — `‖μ‖ = 1` follows in three lines from `Equiv.sum_comp` (σ only permutes the summands of `∑ x ‖w x‖`, which is positive since `w ≠ 0`), with **no order-of-`σ` argument**, and induction propagates `‖w (σ x)‖ = ‖w x‖` along the orbit. This is the single place in the whole arc where transitivity of the translations is consumed. `eigenspace_mulVecLin_ne_bot_of_map_eq`: if the multiset of values of a level function `ε` equals the multiset of eigenvalues of a Hermitian `t`, then the eigenspace of `t` at `ε i` is non-trivial for every `i` (witness: mathlib's `IsHermitian.eigenvectorBasis` at the level matching `ε i`); stated at a general index rather than specialised to `i = 0`, since the general statement carries no extra hypothesis. `exists_uniformModulus_eigenvector_of_transitive_perm_invariance`: the capstone — every non-trivial eigenspace of a transitively symmetric `t` contains `v` with `t·v = λv` and `‖v x‖ ^ 2 = 1 / Fintype.card n` at every index, obtained by rescaling `w` by `√(card n)·‖w x₀‖`; the eigenvalue is kept abstract so that consumers need no level enumeration. The two non-trivial ingredients are **reused verbatim, not re-proved**: `mulVec_mem_eigenspace_of_commute` (`Math/MatrixAnalysis/CommutingEigenspaceInvariance.lean`) gives eigenspace invariance under `P_σ`, and `exists_eigenvector_in_invariant_submodule` (`Math/InvariantSubmoduleEigenvector.lean`) produces the eigenvector `w` of `P_σ` inside it. **Debt.** PR-5 has no production consumer yet, so all four are pinned by the test file `Tests/HubbardImpossibilityLowDensity.lean`: Red 20/21 consume the capstone at two sizes (pinning `1/(M+1)` rather than a coincidence), Red 21b exercises the capstone at a non-diagonal, swap-invariant `t` with a genuine eigenvalue `lam = 1` (Reds 20/21 run only at `t = 0`, `lam = 0`, where the eigen-equation is vacuous), so the returned eigenvector must satisfy `t.mulVec v = 1 • v` non-trivially on top of the uniform modulus, Red 22 is the standalone sharpness counterexample showing the transitivity hypothesis cannot be dropped, Red 23/23b pin `commute_toPEquiv_toMatrix_of_perm_invariant` and the failure of its hypothesis, Red 24 pins `eigenspace_mulVecLin_ne_bot_of_map_eq`, Red 25 pins `norm_apply_eq_norm_apply_of_comp_perm_smul` in isolation at a non-trivial phase `μ = -1`. The module is wired into the `LatticeSystem` root so that CI actually compiles it. Named consumer in the arc: PR-6 opens by composing `eigenspace_mulVecLin_ne_bot_of_map_eq` into `exists_uniformModulus_eigenvector_of_transitive_perm_invariance`, whose eigen-equation feeds `hubbardKinetic_mulVec_hubbardLowDensityTrialState` and whose modulus feeds the double-occupancy weight and `hubbardLowDensityTrialState_ne_zero`. If PR-6/PR-7 end up not consuming one of the four, it is to be **deleted** as reference-0 decoration rather than kept "for later" (PR-2/PR-3/PR-4's rows set this precedent). Axiom-free. | `Math/MatrixAnalysis/PermInvariantUniformEigenvector.lean` |

## Authoritative supplemental implementation record (Theorem 11.4 discharge arc, PR-5b: the single-particle spectrum enumeration bridge)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-1 section above (Issue #5363, PR #5370); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `eq_comp_sort_of_monotone_of_map_eq` / `exists_lowestLevels_finset_of_map_eq` / `sum_lowestLevels_succ` | **The monotone re-enumeration of the single-particle spectrum and its lowest levels** (Tasaki §11.1, p. 375 — the ordering assumption "we also assume that the energy eigenvalues are ordered as `ε_j ≤ ε_{j+1}`", and eqs. (11.1.5)/(11.1.6) with `E_ferro = Σ_{j=1}^{N} ε_j` described as "one of the *lowest* energy states" with `S_tot = S_max` — and §11.1.1, Theorem 11.4, eq. (11.1.8), p. 376, the band condition whose level index the enumeration supplies; Theorem 11.4 discharge arc, Issue #5363, PR #5370 PR-5b; **PROVED**, `#print axioms` = std3; `hubbard_theorem_11_4` itself remains an axiom). One new **generic** file `Math/MonotoneEnumeration.lean` — the content mentions neither Hubbard, nor Tasaki, nor fermions, nor even a matrix; it is order theory about families indexed by a finite ordinal, stated for an abstract linearly ordered value type, so it is organised by mathematical topic and sits at top level in `Math/` beside the other non-matrix utilities rather than under `Math/MatrixAnalysis/` or the Hubbard tree. The book's ordering is formally not an assumption but a **re-enumeration**: mathlib's spectral theorem returns the eigenvalues of the hopping matrix in an arbitrary order as a family `g`, and Tasaki's `ε` is a monotone family carrying the same values. `eq_comp_sort_of_monotone_of_map_eq`: those two descriptions determine each other, `ε = g ∘ Tuple.sort g`, the multiset hypothesis becoming a permutation of the underlying lists, which is then pinned by both lists being sorted; this is **close to but not** mathlib's `Tuple.unique_monotone` (that lemma compares two permutations of one and the same tuple, whereas here `ε` and `g` are unrelated families tied only by the multiset equality), a distinction recorded in the doc comment so it is not mistaken for a duplication. "The `k` lowest levels" is spelled `∑ i : Fin k, ε (Fin.castLE hk i)` throughout, with the book's `j = 1, …, N` read as `i = 0, …, N-1`; the `filter`-shaped alternative is deliberately **not** provided as a second spelling, since the `Fin k` form composes directly with `Fin.sum_univ_castSucc`. `sum_lowestLevels_le_sum_of_monotone` and `sum_lowestLevels_le_sum_of_map_eq`: minimality, `∑_{i<k} ε_i ≤ ∑_{p ∈ S} g_p` for **every** `k`-element set `S` of levels — the formal content of "one of the lowest energy states", and the one genuinely new ingredient, mathlib having no counterpart; enumerating `S` in increasing order with `Finset.orderEmbOfFin`, its `i`-th smallest element has index at least `i` (a private index-arithmetic step: a strictly monotone map of index types does not decrease the index, for which `exact?` finds nothing in mathlib), so monotonicity compares the two sums term by term, the second lemma being the first transported along the sorting permutation so that it applies to the **unsorted** spectrum the caller actually holds. `exists_lowestLevels_finset_of_map_eq`: attainment, some `k`-element set realises that value — the occupied set of (11.1.5), the image of the first `k` indices under the sorting permutation; stated **existentially so that no `def` for the occupied set is introduced**. `sum_lowestLevels_succ`: the split `∑_{i<k+1} ε_i = ∑_{i<k} ε_i + ε_k`, separating the highest occupied level (the one (11.1.6) removes) and providing the hook by which the band condition (11.1.8) enters at level `n = N`; stated with `k + 1` rather than `k - 1` so that truncated natural subtraction cannot appear at the use site. **Debt.** PR-5b has no production consumer yet — the arc's PR-6 keeps the spectrum abstract — so the test file `Tests/HubbardImpossibilityLowDensity.lean` is the sole coverage: four of the five public declarations (`eq_comp_sort_of_monotone_of_map_eq`, `sum_lowestLevels_le_sum_of_map_eq`, `exists_lowestLevels_finset_of_map_eq`, `sum_lowestLevels_succ`) are pinned directly, while `sum_lowestLevels_le_sum_of_monotone` is covered only indirectly, through `sum_lowestLevels_le_sum_of_map_eq`'s proof, which is the sole chain that reaches it. Red 26 pins the bridge on a fixture whose inverted hypothesis would still typecheck, Red 27 pins minimality together with the direction of its inequality, Red 28 pins the occupied set and, by asserting the *lowest* value rather than an arbitrary `k`-subset's, guards the single most likely silent bug, Red 29 pins the off-by-one of the split, Red 30 is the standalone sharpness counterexample showing the monotonicity hypothesis cannot be dropped, and Red 31 is the junction guard at the real consumer's types, feeding the `Finset` returned by this generic layer straight into `occupiedEigenEnergy`. The module is wired into the `LatticeSystem` root so that CI actually compiles it. Named consumer in the arc: PR-7 (the assembly and discharge) obtains its occupied set from `exists_lowestLevels_finset_of_map_eq`, its ferromagnetic energy floor from `sum_lowestLevels_le_sum_of_map_eq`, and its top occupied level from `sum_lowestLevels_succ`, while `eq_comp_sort_of_monotone_of_map_eq` and `sum_lowestLevels_le_sum_of_monotone` are consumed inside this file by the other proofs. If PR-7 ends up not consuming one of the three, it is to be **deleted** as reference-0 decoration rather than kept "for later" (PR-2/PR-3/PR-4/PR-5's rows set this precedent). Axiom-free. **Resolution of the staged debt (PR-7b, Issue #5363, PR #5373).** `sum_lowestLevels_le_sum_of_monotone` (C4a), `sum_lowestLevels_le_sum_of_map_eq` (C4) and the private `val_le_val_of_strictMono` (C2) are **deleted**: the ferromagnetic floor needs *fractional* occupation weights rather than a `{0, 1}`-valued indicator, so the weighted lemmas `sum_lowestLevels_le_sum_weighted` and `sum_lowestLevels_le_sum_weighted_of_map_eq` subsume all three, and Red 27 is retargeted onto the latter on the same fixture so the sharpness coverage does not decrease. This is the "delete rather than keep for later" outcome this row pre-authorised, not a regression; `eq_comp_sort_of_monotone_of_map_eq`, `exists_lowestLevels_finset_of_map_eq` and `sum_lowestLevels_succ` survive unchanged and keep their generic `α`. The name column of this row accordingly lists only those three survivors: **no declaration named `sum_lowestLevels_le_sum_of_monotone`, `sum_lowestLevels_le_sum_of_map_eq` or `val_le_val_of_strictMono` exists in the repository**, and the two public ones occur in the description above only as an account of what PR-5b delivered, their mathematical content being available from `sum_lowestLevels_le_sum_weighted` and `sum_lowestLevels_le_sum_weighted_of_map_eq` (PR-7b's row below) at an indicator weight. | `Math/MonotoneEnumeration.lean` |

## Authoritative supplemental implementation record (Theorem 11.4 discharge arc, PR-6: Roth's variational bound)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-1 section above (Issue #5363, PR #5371); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `norm_le_of_mulVec_eq_smul_of_rowSum_le` / `abs_eigenvalues_le_of_rowSum_le` / `fermionTotalNumber_mulVec_spinfulGeneralBasisState_empty` / `hubbardKineticSpin_zero_mulVec_spinfulGeneralBasisState_empty` / `fermionTotalUpNumber_mulVec_spinfulGeneralBasisState_empty` / `fermionTotalDownNumber_mulVec_fermionUpNumber_mulVec_eq_zero` / `hubbardKineticSpin_zero_commute_fermionDownAnnihilation` / `hubbardOnSiteInteraction_one_mulVec_hubbardLowDensityTrialState` / `dotProduct_star_hubbardOnSiteInteraction_trial_sandwich` / `dotProduct_star_fermionUpNumber_mulVec_sandwich` / `fermionMultiNumber_mul_hubbardKineticSpin_mul_self` / `rayleighOnVec_hubbardKineticSpin_one_le_of_downNumber_one` / `hubbardLowDensityRothState` / `fermionTotalNumber_mulVec_hubbardLowDensityRothState` / `dotProduct_star_self_hubbardLowDensityTrialState` / `dotProduct_star_hubbardOnSiteInteraction_hubbardLowDensityTrialState` / `rayleighOnVec_hubbardKineticSpin_zero_doubleOccupancy_le` / `rayleighOnVec_hubbardKineticSpin_one_doubleOccupancy_le` / `dotProduct_star_self_hubbardLowDensityRothState_re` / `dotProduct_star_self_hubbardLowDensityRothState_pos` / `rayleighOnVec_hubbardHamiltonian_hubbardLowDensityRothState_le` | **Roth's variational estimate for the spin-flip trial state** (Tasaki §11.1.1, Theorem 11.4, eqs. (11.1.9)/(11.1.10), p. 376; Tasaki, Prog. Theor. Phys. **99** (1998) 489, Theorem 3.3, Appendix F, eqs. (F.1)–(F.3) and (F.8)–(F.13), pp. 545–546, plus the boundedness assumption on `max_x t_{x,x}`, `max_x Σ_y |t_{x,y}|` and `ε_1` stated on p. 547; Theorem 11.4 discharge arc, Issue #5363, PR #5371 PR-6; **PROVED**, `#print axioms` = std3 for all twenty-one public declarations; `hubbard_theorem_11_4` itself remains an axiom and its statement is untouched). Three new files. `hubbardLowDensityRothState` is the Roth state of eq. (11.1.9), written as the explicit difference `Ψ − ν̂Ψ` rather than through a projection operator `P̂₀`, with `fermionTotalNumber_mulVec_hubbardLowDensityRothState` recording that it stays in the `|SUp| + 1`-electron sector. The capstone `rayleighOnVec_hubbardHamiltonian_hubbardLowDensityRothState_le` proves `⟨Ψ̃, ĤΨ̃⟩ ≤ (E↑ + ε + 8K|SUp|/(M+1))‖Ψ̃‖²` for **every** coupling `U` — the coupling is absent from the right because the Coulomb interaction annihilates `Ψ̃` identically (PR-3's layer) — with the explicit numeral `8` and the genuine side hypothesis `2|SUp| ≤ M + 1`. **What is replaced, not reproduced.** Appendix F's Fourier mode representation (F.4)–(F.7) is not formalised at all, the exact value of (F.9) is not computed, and the two-point remainder `R` of (F.10)/(F.11) is never constructed; an auditor should not read this row as an incomplete transcription of Appendix F. In their place: `hubbardOnSiteInteraction_one_mulVec_hubbardLowDensityTrialState` computes the doubly occupied component exactly as `Σ_x v_x ĉ†_{x↓}(n̂_{x↑}Φ↑)`, and `dotProduct_star_hubbardOnSiteInteraction_trial_sandwich` (fed by PR-4's δ factorisation, with the hypothesis supplied by `hubbardKineticSpin_zero_commute_fermionDownAnnihilation` and `fermionTotalDownNumber_mulVec_fermionUpNumber_mulVec_eq_zero`) diagonalises any quadratic form in it over sites; `dotProduct_star_fermionUpNumber_mulVec_sandwich` folds the Hermitian occupations into a sandwich. At the identity this gives `dotProduct_star_hubbardOnSiteInteraction_hubbardLowDensityTrialState`, the exact double-occupancy weight `⟨Ψ, ν̂Ψ⟩ = (|SUp|/(M+1))‖Φ↑‖²` replacing (F.8) — and, because only the uniform modulus of the ↓ orbital enters, it needs **no** delocalisation of the occupied ↑ modes, an improvement over the book's route. The majority-spin correction comes from the arc's crux, the number sandwich `fermionMultiNumber_mul_hubbardKineticSpin_mul_self`, `n̂_{xσ}Ĥ^σn̂_{xσ} = n̂_{xσ}Ĥ^σ − Σ_z t_{xz}ĉ†_{xσ}ĉ_{zσ} + t_{xx}n̂_{xσ}`, stated at a general spin label `σ` (the proof is spin-agnostic and the whole PR-2 layer is per-`σ`) and proved from four elementary canonical-anticommutation facts plus the distinct-mode commutations; summing it against the site weights, the double hopping sum **is** `⟨Φ↑, Ĥ^↑Φ↑⟩` by the definition of `hubbardKineticSpin`, which is why no mode expansion is needed, and `rayleighOnVec_hubbardKineticSpin_zero_doubleOccupancy_le` collects the result as `E↑⟨Ψ, ν̂Ψ⟩ + 2K(|SUp|/(M+1))‖Φ↑‖²`. The minority-spin correction is not evaluated but bounded: `rayleighOnVec_hubbardKineticSpin_one_le_of_downNumber_one` transports PR-2's Loewner bound `Ĥ^↓ ≤ e N̂_↓` to the Rayleigh quotient of any one-↓-electron vector, and `rayleighOnVec_hubbardKineticSpin_one_doubleOccupancy_le` applies it at `e := K`. `dotProduct_star_self_hubbardLowDensityTrialState` and `dotProduct_star_self_hubbardLowDensityRothState_re` supply the two norms (`‖Ψ‖² = ‖Φ↑‖²` and `‖Ψ̃‖² = (1 − |SUp|/(M+1))‖Φ↑‖²`, the latter being eq. (F.2) combined with the weight), and `dotProduct_star_self_hubbardLowDensityRothState_pos` its positivity, which is where `2|SUp| ≤ M + 1` is first needed. **The row-sum hypothesis is the source paper's own.** Appendix F, p. 547, assumes that `max_x t_{x,x}`, `max_x Σ_y |t_{x,y}|` and `ε_1` converge to finite quantities, which is exactly `∀ x, Σ_y ‖t x y‖ ≤ K`; the eigenvalue bounds `|ε_j| ≤ K` and `|ε| ≤ K` it feeds are obtained from mathlib's `eigenvalue_mem_ball` (Gershgorin) rather than a hand-rolled argmax argument, as `norm_le_of_mulVec_eq_smul_of_rowSum_le` and its Hermitian corollary `abs_eigenvalues_le_of_rowSum_le`. That corollary mentions no fermion, no Hubbard model and no textbook, so it is organised by mathematical topic in `Math/MatrixAnalysis/` beside `CourantFischer.lean` and is wired into the `LatticeSystem` root so CI compiles it. `fermionTotalNumber_mulVec_spinfulGeneralBasisState_empty` and `hubbardKineticSpin_zero_mulVec_spinfulGeneralBasisState_empty` are **extractions** of blocks previously inlined inside `HubbardImpossibilityLowDensityTrial.lean`'s proofs, lifted so this stage reuses them instead of duplicating them; `fermionTotalUpNumber_mulVec_spinfulGeneralBasisState_empty` is the ↑ analogue that the double-occupancy weight consumes. Appendix F's (F.14), the *assumed* bound `|ψ^{(p)}_x| ≤ α/√N_s`, is assumed nowhere: PR-5 derives the uniform modulus of the ↓ orbital from translation invariance, and the majority-spin channel needs no such bound. **Debt.** PR-6's production consumer is PR-7 (the assembly and discharge), which consumes `rayleighOnVec_hubbardHamiltonian_hubbardLowDensityRothState_le`, `fermionTotalNumber_mulVec_hubbardLowDensityRothState` and `dotProduct_star_self_hubbardLowDensityRothState_pos`; everything else is consumed inside this stage or pinned by the test module. Of those three, `fermionTotalNumber_mulVec_hubbardLowDensityRothState` is at present **reference-0** — nothing in the repository, test module included, mentions it, the electron-count statement being of use only once PR-7 assembles the variational argument — and is carried as staged debt for exactly one stage. If PR-7 ends up not consuming one of the three, it is to be **deleted** as reference-0 decoration rather than kept "for later" (PR-2/PR-3/PR-4/PR-5/PR-5b's rows set this precedent). Coverage is the sibling test module `Tests/HubbardImpossibilityLowDensityRoth.lean` (Red 32–41), the existing arc test module being past the 700-line review trigger: Red 32 is the full consumption of the capstone at a fixture exercising every binder, Red 33 pins the `U`-independence that is the theorem's headline, Red 34/34b pin the `|SUp|/(M+1)` ratio at two distinct fixtures so the `1/N_s` factor is a shape rather than a coincidence, Red 35 pins the number sandwich at a diagonal hopping matrix where the correction must cancel the diagonal term, Red 36 is the spin-tag guard at `σ = 0` versus `σ = 1`, Red 37 is the standalone sharpness counterexample for the row-sum hypothesis, Red 38 guards Red 32 against a "both sides are zero" degeneracy, and Red 39 documents why `2|SUp| ≤ M + 1` is load-bearing rather than convenient. Red 32/33/38 all run at `SUp := ∅`, where the Roth construction degenerates (`ν̂Ψ = 0`, `Ψ̃ = Ψ`, `ρ = 0`), so two further fixtures carry the nonempty regime: Red 40 consumes the capstone at `SUp := {0}` on the same `t := 0`, `K := 0` fixture, where `ρ = 1/2`, `hhalf` is saturated rather than vacuous and both corrections act on a nonzero doubly occupied component while the residual `8K|SUp|/(M+1)` is forced to vanish exactly, and Red 41 reads off the Roth norm `‖Ψ̃‖² = (1/2)‖Φ↑‖²` there, pinning the factor `1 − |SUp|/(M+1)` that is invisible at `SUp := ∅`. Axiom-free. | `Math/MatrixAnalysis/RowSumEigenvalueBound.lean`, `HubbardImpossibilityLowDensityRothCore.lean`, `HubbardImpossibilityLowDensityRoth.lean` (extractions in `HubbardImpossibilityLowDensityTrial.lean`) |

**Staged debt settled.** All three names carried as PR-7c's obligation — `rayleighOnVec_hubbardHamiltonian_hubbardLowDensityRothState_le`, `fermionTotalNumber_mulVec_hubbardLowDensityRothState` and `dotProduct_star_self_hubbardLowDensityRothState_pos` — are consumed by the discharged `hubbard_theorem_11_4` (PR #5374 PR-7c); none was deleted.

## Authoritative supplemental implementation record (Theorem 11.4 discharge arc, PR-7a: the general-filling weight machinery)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-1 section above (Issue #5363, PR #5372); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `mem_hubbardEigenspaceAt` / `fermionTotalSpinZ_mulVec_mem_hubbardEigenspaceAt` / `fermionTotalSpinPlus_mulVec_mem_hubbardEigenspaceAt` / `fermionTotalSpinMinus_mulVec_mem_hubbardEigenspaceAt` / `hubbardEigenspaceAt_inf_eigenspace_eq_bot` / `hubbardEigenspaceAt_eq_iSup_weight` / `fermionTotalSpinPlus_mulVec_ne_zero_of_maxSpin` / `exists_topWeight_of_maxSpin` | **The `Ŝ³`-weight machinery of the ground eigenspace at a general electron filling** (Tasaki §11.1.1, Proposition 11.2 and eq. (11.1.4), pp. 377–378 — the half-filling multiplet whose supporting machinery is generalised — and §11.1.1, Theorem 11.4, eqs. (11.1.8)–(11.1.10), p. 376, the low-filling statement that requires it; Theorem 11.4 discharge arc, Issue #5363, PR #5372 PR-7a; **PROVED**, `#print axioms` = std3 for all eight public declarations, and re-checked = std3 for `hubbard_proposition_11_2` and `hubbard_theorem_11_3`, which this stage touches transitively; `hubbard_theorem_11_4` itself remains an axiom and its file is byte-for-byte untouched). No new file. **Seven lemmas were generalised in place, not copied**: the six `…AtFilling` weight lemmas of `HubbardFerromagnetismStructureCore.lean` gain an electron-number parameter `Ne` and are renamed to `…hubbardEigenspaceAt…` accordingly, and `fermionTotalSpinPlus_mulVec_ne_zero_of_maxSpin` keeps its name while its hard-coded `S_max = (N+1)/2` becomes a real parameter `S`. A `…_general` copy beside each old lemma would have been a duplicate statement and was **not** written; the ~10 half-filling call sites in `HubbardFerromagnetismStructure.lean` and `HubbardImpossibilityLowU.lean` instead instantiate the general lemmas at `Ne := N + 1`, which is what `hubbardEigenspaceAtFilling` unfolds to by definition. That abbreviation is kept, being the spelling in which Proposition 11.2 and Theorem 11.3 are stated. Every proof body survives the substitution unchanged, because none of these arguments ever knew about half filling: they are the electron bookkeeping `a↑ + a↓ = Ne`, `Ŝ³ = (a↑ − a↓)/2 = a↑ − Ne/2` (so the occurring weights are `a − Ne/2` for `0 ≤ a ≤ Ne`, which is what `hubbardEigenspaceAt_inf_eigenspace_eq_bot` and `hubbardEigenspaceAt_eq_iSup_weight` now say), together with the `SU(2)` commutation relations, and — for the raising injectivity — the highest-weight Casimir identity `S(S+1) = sz(sz+1)`, which mentions neither the Hamiltonian nor the filling. **What is deliberately not generalised.** The `finrank` layer (`…_top_block_finrank_le_one`, `…_block_finrank_le_succ`, `…_block_finrank_le_one`, `…_finrank_le`, `…_finrank_ge`, `hubbard_proposition_11_2`) stays at half filling and keeps the `…AtFilling` spelling: the fully ↑-polarised `Ne`-electron space has dimension `C(N+1, Ne)`, which is `1` only at the two extreme fillings `Ne = 0` (the vacuum) and `Ne = N + 1` (half filling), so the one-dimensional top block the count needs exists only at the filling Proposition 11.2 is stated at, and an auditor should not read the surviving `AtFilling` names as an incomplete rename. **New.** `exists_topWeight_of_maxSpin`: a nonzero `Ne`-electron energy-`E₀` eigenspace all of whose vectors are maximal-spin at `S(S+1)`, `S = Ne/2`, contains a nonzero vector of top `Ŝ³`-weight `S`. This is the one thing Theorem 11.4 needs from this layer, and it is obtained by **raising, not by counting**: some weight block is nonzero because the blocks exhaust the eigenspace, and `Ŝ⁺` carries a nonzero block strictly below the top to a nonzero block one step higher (it preserves the eigenspace, it raises the weight by one, and by the maximal-spin hypothesis it cannot annihilate a vector of weight `< S`), so iterating reaches the top block. No `finrank`, no basis and no spanning statement about the polarised sector occurs, which is exactly what makes the argument available at every filling — Proposition 11.2's own top-block argument, by contrast, runs through dimensions and is therefore confined to half filling. Those two half-filling proofs (`hubbardEigenspaceAtFilling_finrank_ge` and `hubbardAllUpState_mem_of_maxSpin`) still carry their own inlined block chain; folding them into the new lemma is a refactoring left to a later stage and is recorded here so it is not read as an unnoticed duplication. **Debt.** `exists_topWeight_of_maxSpin` has no production consumer yet — its named consumer is PR-7b's ferromagnetic floor, which feeds the returned top-weight vector into the full-polarisation and fractional-occupation steps — so for one stage its only coverage is the test module `Tests/HubbardImpossibilityLowDensityWeights.lean` (a sibling of `Tests/HubbardImpossibilityLowDensity.lean`, which is already past the 700-line review trigger, exactly as PR-6 did with `Tests/HubbardImpossibilityLowDensityRoth.lean`), where Red 43 consumes it at `N := 1`, `Ne := 1`, `t := 0`, `U := 0`, `E₀ := 0` — again a filling that is not `N + 1` — and **discharges both hypotheses instead of positing them**: `hne` by exhibiting a single ↑ electron, and `hferro` because a one-electron sector is a spin-1/2 doublet (each of its two `Ŝ³`-weight blocks is annihilated by the ladder operator that would carry it off the `Ne = 1` weight grid, which pins the Casimir eigenvalue to `S(S+1) = 3/4`). What that test guards is the shape of the conclusion — the returned vector's weight is the top one `+S` — together with the joint satisfiability of the hypotheses; it does **not** guard where the internal raising induction starts, and no fixture can: `hferro` and `hne` constrain the eigenspace as a whole and give the caller no way to single out which weight block is the nonzero one. If PR-7b ends up not consuming it, it is to be **deleted** as reference-0 decoration rather than kept "for later" (PR-2/PR-3/PR-4/PR-5/PR-5b/PR-6's rows set this precedent). The generalisation itself is covered by Red 42 in the same module, stated at `N := 1`, `Ne := 1`, a filling that is *not* `N + 1`, so a proof that silently retained the old filling cannot typecheck against it; Red 44 is the standalone sharpness counterexample for the strict-weight hypothesis of `fermionTotalSpinPlus_mulVec_ne_zero_of_maxSpin`, exhibiting a maximal-spin vector at exactly `sz = S` that *is* annihilated by `Ŝ⁺`. Axiom-free. | `HubbardFerromagnetismStructureCore.lean` + `HubbardFerromagnetismStructure.lean` (call sites in `HubbardImpossibilityLowU.lean`) |

## Authoritative supplemental implementation record (Theorem 11.4 discharge arc, PR-7b: the fractional-occupation floor ingredients)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-1 section above (Issue #5363, PR #5373); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `sum_lowestLevels_le_sum_weighted` / `sum_lowestLevels_le_sum_weighted_of_map_eq` / `one_sub_eigenNumberOp_posSemidef` / `hubbardOnSiteInteraction_mulVec_eq_zero_of_downNumber_zero` / `fermionTotalDownNumber_mulVec_eq_zero_of_topWeight` | **The fractional-occupation bound and the algebraic ingredients of the ferromagnetic floor** (Tasaki §11.1, eqs. (11.1.5)/(11.1.6), p. 375 — `E_ferro = Σ_{j=1}^{N} ε_j` as the energy of "one of the *lowest* energy states" with `S_tot = S_max` — and §11.1.1, Theorem 11.4, eqs. (11.1.8)–(11.1.10), p. 376; the underlying argument is Tasaki, Prog. Theor. Phys. **99** (1998) 489, Theorem 3.3, Appendix F, eqs. (F.12)/(F.13), p. 546; Theorem 11.4 discharge arc, Issue #5363, PR #5373 PR-7b; **PROVED**, `#print axioms` = std3 for all five public declarations; `hubbard_theorem_11_4` itself remains an axiom and its file is byte-for-byte untouched). **Why fractional.** Appendix F reads the ferromagnetic energy as a sum over a definite *set* of occupied levels, which presupposes a Slater determinant. Theorem 11.4 hands over no such thing: its hypothesis yields a nonzero top-weight vector `u` of the eigenspace (PR-7a's `exists_topWeight_of_maxSpin`), an arbitrary element of a possibly high-dimensional space. Enumerating the Slater determinants spanning the fully polarised `Ne`-electron sector would require a spanning statement **and** a linear-independence statement for that sector at a general `Ne`, **and** "an eigenvalue of an operator diagonal in a basis is a diagonal entry"; none of the three exists in this development (`GeneralFlatBandSpanning.lean` is about the flat-band *ground* subspace, not this sector). The fractional route needs none of them. **The order-theoretic half.** `sum_lowestLevels_le_sum_weighted`: for monotone `ε : Fin m → ℝ` and any weights with `0 ≤ w j ≤ 1` and `∑ j, w j = k`, `∑ i : Fin k, ε (Fin.castLE hk i) ≤ ∑ j, ε j * w j`; `sum_lowestLevels_le_sum_weighted_of_map_eq` is the same against the unsorted spectrum `g`, transported along `Tuple.sort g` through `eq_comp_sort_of_monotone_of_map_eq`. The proof is an exchange argument, not an index enumeration: with `T` the first `k` indices, `χ` its indicator and `θ = ε ⟨k-1, _⟩` the top level of `T`, every term of `∑ j, (ε j − θ) * (w j − χ j)` is nonnegative (inside `T` both factors are nonpositive, outside `T` both are nonnegative) while `∑ j, w j = k = ∑ j, χ j` cancels the `θ`-terms, leaving exactly `∑ j, ε j * w j − ∑ i : Fin k, ε (Fin.castLE hk i)`. Both are stated over **ℝ**, not the generic `α` the deleted lemmas used, because the only consumer is at `ℝ` and a generic-`α` version buys nothing; `eq_comp_sort_of_monotone_of_map_eq`, `exists_lowestLevels_finset_of_map_eq` and `sum_lowestLevels_succ` keep their generic `α`. Since the indicator of an **arbitrary** `k`-element set of levels is an admissible weight — not the proof's own `χ`, which is the indicator of the *first* `k` and turns the bound into an identity — PR-5b's `sum_lowestLevels_le_sum_of_monotone`, `sum_lowestLevels_le_sum_of_map_eq` and the private `val_le_val_of_strictMono` become reference-0 and are **deleted** here — subsumed, not regressed, exactly as PR-5b's own row pre-authorised — and with them goes the module's dependence on `Finset.orderEmbOfFin` and the `Mathlib.Data.Finset.Sort` import. **The operator half**, one new leaf `HubbardImpossibilityLowDensityFloor.lean`. `one_sub_eigenNumberOp_posSemidef`: writing `A = Ĉ_σ(ē_j)` for the eigenmode annihilator, the dual canonical anticommutation relation `A·Aᴴ + Aᴴ·A = 1` turns `1 − n̂_{j,σ} = A·Aᴴ` into a Gram matrix, so with the existing `eigenNumberOp_posSemidef` the occupation weight `w_j = ⟨u, n̂_{j↑}u⟩/‖u‖²` is confined to `[0, 1]` — Pauli exclusion in exactly the form the knapsack bound consumes, and the one orientation (`A·Aᴴ` versus `Aᴴ·A`) that could silently flip. `fermionTotalDownNumber_mulVec_eq_zero_of_topWeight`: `N̂ = N̂_↑ + N̂_↓` and `Ŝ³ = (N̂_↑ − N̂_↓)/2` are operator identities in this repository, so `N̂u = Ne·u` together with `Ŝ³u = (Ne/2)·u` gives `N̂_↓u = 0` by subtraction. This is where Theorem 11.3's configuration counting (`topWeight_eq_smul_hubbardAllUpState`, ~40 lines, half filling only) disappears: two eigenvalue equations replace it, and the step becomes available at **every** `Ne`. `hubbardOnSiteInteraction_mulVec_eq_zero_of_downNumber_zero`: every summand of `Ĥ_int = U Σ_x n̂_{x↑}n̂_{x↓}` ends in a down annihilation, so a state with `N̂_↓ = 0` has zero interaction energy and the floor carries no `U`-dependent correction — the reason Theorem 11.4 holds for every `U ≥ 0`. It is the interaction sibling of PR-2's `hubbardKineticSpin_one_mulVec_eq_zero_of_downNumber_zero` and lives beside the other floor ingredients rather than in `HubbardKineticSpinBounds.lean`, whose subject is the kinetic fiber. **Build wiring.** `LatticeSystem.lean` imports no `Hubbard` module directly; the whole Hubbard tree hangs off the `Fermion/JordanWigner.lean` umbrella, into which the new leaf is wired. That umbrella is reached from **both** roots: from the `LatticeSystem` root indirectly, through `Fermion/JordanWigner/SmearedCAR.lean` → `Fermion/JWAbstractCrossSite.lean` → `Fermion/JWAbstract.lean`, whose first import is the umbrella; and from the `LatticeSystem.Tests` root through the arc's test modules. So the leaf compiles under either root, and both are still built here because only the test root pins its coverage. **Debt.** Neither half has a production consumer yet: the named consumer of all five declarations is PR-7c (the ferromagnetic floor proper and the axiom discharge), and for the same reason PR-7a's `exists_topWeight_of_maxSpin` also stays unconsumed one further stage — the PR-7b/PR-7c split moved the assembly, not the ingredients. Coverage is therefore the test modules: Red 27 (retargeted) and Red 30 in `Tests/HubbardImpossibilityLowDensity.lean` pin the weighted bound on the `{0, 1}`-valued fixture and the `hmono` counterexample; Reds 48/49/50 in `Tests/HubbardImpossibilityLowDensityRoth.lean` pin the genuinely **half-integral** weight `![1, 1/2, 1/2]` (a proof handling only `{0, 1}`-valued weights cannot instantiate them), the sharpness of `hw1` (`w := ![2, 0, 0]` gives `∑ ε·w = 0 < 1`, so the hypothesis cannot be dropped) and the sorting transport at an unsorted `g`; Reds 52/52b/53 pin F1's orientation at a concrete fixture, F2 on the Fock vacuum, and F3 on the all-up two-electron state, where a dropped factor `2` in `Ŝ³` fails to unify. If PR-7c ends up not consuming one of the five, it is to be **deleted** as reference-0 decoration rather than kept "for later" (PR-2/PR-3/PR-4/PR-5/PR-5b/PR-6/PR-7a's rows set this precedent). Axiom-free. | `Math/MonotoneEnumeration.lean` + `HubbardImpossibilityLowDensityFloor.lean` |

## Authoritative supplemental implementation record (Theorem 11.4 discharge arc, PR-7c: the `rpow` threshold, the ferromagnetic floor and the axiom discharge)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-1 section above (Issue #5363, PR #5374); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `exists_pos_forall_mul_lt_rpow` / `sum_lowestLevels_mul_le_rayleighOnVec_hubbardKinetic` / `hubbard_theorem_11_4` | **The `rpow` threshold, the ferromagnetic floor and the axiom discharge** (Tasaki §11.1.1, Theorem 11.4, eqs. (11.1.8)–(11.1.10), p. 376, and §11.1, eqs. (11.1.5)/(11.1.6), p. 375; the underlying argument is Tasaki, Prog. Theor. Phys. **99** (1998) 489, Theorem 3.3, Appendix F, eqs. (F.12)/(F.13), pp. 546–547, and the boundedness assumption on p. 547; Theorem 11.4 discharge arc, Issue #5363, PR #5374 PR-7c; **PROVED**, `#print axioms` = std3 on `hubbard_theorem_11_4` and on both new public declarations, and re-checked = std3 on `hubbard_theorem_11_3` and `hubbard_proposition_11_2`). **The statement is not touched.** The axiom is rewritten in place as a theorem — `axiom … :` becomes `theorem … := by` — so what is proved is PR-1's corrected statement and otherwise byte-for-byte the book's; no `…Discharge.lean` leaf was needed, the in-place rewrite being cycle-free here (the 10.x leaves were forced by cycles that do not exist in this tree). **T1**, `exists_pos_forall_mul_lt_rpow`: for `a, b > 0` and `p < 1` there is `r > 0` with `b·ρ < a·ρ^p` throughout `(0, r]`, witnessed by `r = R/2` at the crossing abscissa `R = (a/b)^{1/(1−p)}`. It mentions no fermion and no textbook, so it is organised by mathematical topic in `Math/Analysis/` beside `RealRpowNatSqrt.lean` and wired into the `LatticeSystem` root. Sublinearity is load-bearing: at `p = 1` the conclusion fails for every `b ≥ a`, which is the formal content of the book's `d > 2`. **F4**, `sum_lowestLevels_mul_le_rayleighOnVec_hubbardKinetic`: a fully polarised `Ne`-electron state has kinetic energy at least `(∑_{i<Ne} ε_i)‖u‖²`. The weights are the eigenmode occupations `w_j = ⟨u, n̂_{j↑}u⟩/‖u‖²`, confined to `[0,1]` by `eigenNumberOp_posSemidef` and PR-7b's F1 and summing to `Ne` because `∑_j n̂_{j↑} = N̂_↑` and F3 turns the number equation into `N̂_↑u = Ne·u`; the spin-↓ kinetic block annihilates `u`, so the quotient is `∑_j ε_j w_j` and the bound is PR-7b's fractional-knapsack lemma read against the unsorted spectrum. **This is the route change, not a transcription.** Appendix F reads the ferromagnetic energy off a definite set of occupied levels, which presupposes a Slater determinant; the hypothesis supplies only an arbitrary nonzero top-weight vector of a possibly high-dimensional eigenspace, and enumerating the determinants spanning the polarised sector at a general `Ne` would need a spanning statement, a linear-independence statement and a diagonal-entry/eigenvalue bridge, none of which exists in this development. The fractional route needs none of the three and holds at every filling. Appendix F's (F.14), the *assumed* uniform modulus `|ψ_x| ≤ α/√N_s`, is likewise assumed nowhere: PR-5 derives it from translation invariance. **The assembly.** With `p = 2/d`, `a = c/2^p`, `b = 8·max(K, 1)` and `r` from T1, the threshold is the explicit `ρ₁ = min ρ₀ (min (1/2) r)`. Taking `max K 1` keeps `b` positive at `K = 0`, where `_hK` alone forces only `K ≥ 0`, and costs nothing since `8Kρ ≤ 8·max(K,1)·ρ`; the `1/2` supplies both `2|S_↑| ≤ |Λ|`, which PR-6's Roth norm needs, and `Ne ≤ |Λ|`, which F4 needs. Above, PR-6's capstone on the Roth state built from the `Ne − 1` lowest eigenmodes and PR-5's delocalised lowest mode gives `E₀ ≤ ∑_{i<Ne−1} ε_i + ε_1 + 8K(Ne−1)/|Λ|`; below, F4 on the top-weight ground state furnished by PR-7a gives `∑_{i<Ne} ε_i ≤ E₀`, the interaction dropping out for every `U ≥ 0` by PR-7b's F2. Splitting the floor at the top occupied level leaves `ε_top − ε_1 ≤ 8·max(K,1)·ρ`, while the band condition at that level gives `c(ρ/2)^p ≤ ε_top − ε_1`, and `c(ρ/2)^p = a·ρ^p > b·ρ` is exactly what T1 forbids below `r`. **Coverage.** `Tests/HubbardImpossibilityLowDensityRoth.lean` Reds 46, 47 and 51 consume T1 at three fixture triples plus the pinned instance, with the standalone `p = 1` counterexample; Reds 54/54b consume F4 at `t = 0` and at `diag(0, 1)`, the latter with `hspec` earned through the characteristic-polynomial route rather than the `ε := hT.eigenvalues` shortcut, so it is the Red that fails if the weights or the multiset transport are wrong. Red 55 pins the capstone's quantifier order — one `ρ₁` for every `N` and every `U` — which the existing Red 1 cannot see because it fixes `N` and `U` before obtaining `ρ₁`. No fixture-based instance of the capstone is possible: the density hypothesis refers to the *obtained* `ρ₁`, so the model can never be a numeral, which is what Red 2 already pins. Axiom-free. | `Math/Analysis/RpowSublinearThreshold.lean`, `HubbardImpossibilityLowDensityFloor.lean`, `HubbardImpossibilityLowDensity.lean` |

---

[← Multi-mode fermion via Jordan–Wigner (P2 backbone)](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-08/) · [Catalogue](/lattice-system/formalization/legacy/) · End →
