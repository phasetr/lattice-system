---
layout: page
title: "Legacy catalogue: Multi-mode fermion via Jordan–Wigner (P2 backbone) (part 4 of 4)"
permalink: /formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/
---

# Legacy catalogue: Multi-mode fermion via Jordan–Wigner (P2 backbone) (part 4 of 4)

> **Interim authority.** This lossless catalogue chunk remains authoritative for formalization status and capstone identification until Issue #5228. The version 1 JSON catalogue is still a non-authoritative prototype.

[Interim catalogue](/lattice-system/formalization/legacy/) › [Fermions and Hubbard models](/lattice-system/formalization/legacy/#group-fermions-hubbard)

<!-- legacy-source:start:2605:2731 -->
| Lean name | Statement | File |
|---|---|---|
| <a id="tasaki-chapter-10-part-02"></a> `theorem_10_4_lieb_repulsive_half_filling` | **Theorem 10.4** (Tasaki §10.2.2, p. 350, **AXIOM**): at half-filling `N = \|Λ\|`, the ground subspace is nonzero, energy-minimal, consists entirely of total-spin `S₀ = \|\|A\|−\|B\|\|/2` states (Casimir `S₀(S₀+1)`), and has dimension exactly `\|A\|−\|B\|+1` (the unavoidable SU(2) multiplet degeneracy). Lieb's reflection positivity via the Shiba transformation → faithful documented axiom. | `Fermion/JordanWigner/Hubbard/LiebRepulsive.lean` |
| <a id="tasaki-chapter-9-part-03"></a> `shibaConfig` / `shibaConfigEquiv` / `shibaConfig_up` / `shibaConfig_down` / `shibaConfig_involutive` | **Shiba configuration involution** (Tasaki §9.3.3, eq. (9.3.48), p. 335): particle-hole flip on down-spin occupations `(n̂↑, n̂↓) ↦ (n̂↑, 1−n̂↓)` as the permutation part of the Shiba transformation for Theorem 10.4. `shibaConfig` is the down-species flip; `shibaConfigEquiv` packages it as an involution (`Equiv`); proofs establish it is self-inverse with fixed up-config and flipped down-config. **PR #4950 (c2)**: axiom-free infrastructure. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveShibaTransform.lean` |
| `shibaPermMatrix` / `shibaPermMatrix_apply` / `shibaPermMatrix_isHermitian` / `shibaPermMatrix_conj_diagonal` | **Shiba permutation matrix** (Tasaki §9.3.3, eqs. (9.3.48)/(9.3.50), pp. 335–336): the permutation matrix realizing the Shiba involution on Fock configurations; Hermitian (`Pᴴ=P`); conjugating a diagonal by `P` reindexes via `shibaConfig`. **PR #4950 (c3)**: unsigned permutation part of the full Shiba unitary; axiom-free infrastructure. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveShibaUnitary.lean` |
| `symmetricRepulsiveInteractionDiag` / `symmetricRepulsiveHubbardInteraction_eq_diagonal` / `shibaConfig_apply_up` / `shibaConfig_apply_down` / `symmetricRepulsiveInteractionDiag_shibaConfig` / `shibaPermMatrix_conj_symmetricInteraction` / `shibaSignedUnitary` / `shibaSignedUnitary_conjTranspose` / `shibaSignedUnitary_conj_symmetricInteraction` | **Shiba interaction conjugation** (Tasaki §9.3.3, eqs. (9.3.47)/(9.3.54), pp. 334–336): the symmetric repulsive interaction `Ĥ_{int}'` is diagonal; Shiba flip negates each term (n̂↓−½ changes sign); **capstone `shibaSignedUnitary_conj_symmetricInteraction`**: the signed-Shiba unitary satisfies `Ûᴴ·Ĥ_{int}'·Û = −Ĥ_{int}'` (eq. 9.3.54). **PR #4950 (c5)**: discharges axiom-free interaction sign-flip; consumed by the c6 full-conjugation capstone (PR #4952). | `Fermion/JordanWigner/Hubbard/LiebRepulsiveShibaInteraction.lean` |
| `gaugeSign` / `shibaGauge` / `shibaCrossingSpecies` / `shibaJwFlipParity` / `shibaSignFn` / `gaugeSign_mul_of_bipartite` / `shibaGauge_star_mul_self` / `shibaSignFn_star_mul_self` / `shibaSignedUnitary_conj_symmetricKinetic` | **Shiba kinetic-term conjugation** (Tasaki §9.3.3, eq. (9.3.52), p. 336): the Shiba transformation leaves the symmetric kinetic term invariant `Ûᴴ·Ĥ_kin·Û = Ĥ_kin`. Mechanism: on a bipartite bond `x∈A, y∈B`, the particle-hole flip converts `ĉ†_x ĉ_y` into `−ĉ†_y ĉ_x` (CAR sign −1) while the sublattice gauge contributes `ε_x ε_y = −1`; the two signs multiply to `+1`, restoring the symmetric term `t_xy = t_yx`. **capstone `shibaSignedUnitary_conj_symmetricKinetic`**: kinetic invariance; `shibaSignFn_star_mul_self` (|s|=1) feeds the c6 conjugation. **PR #4951 (c4)**: axiom-free kinetic-term preservation. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveShibaKinetic.lean` |
| `symmetricHubbardOnSite_expand` / `hubbardKinetic_add` / `hubbardKinetic_diagonal` / `neg_symmetricRepulsiveInteraction_eq` / `shibaSignedUnitary_conj_symmetricRepulsive_eq_attractive` | **Shiba full conjugation repulsive→attractive** (Tasaki §10.2.2, eqs. (10.2.10)/(10.2.11), p. 352): combining c4 (kinetic invariance) and c5 (interaction sign-flip), **capstone `shibaSignedUnitary_conj_symmetricRepulsive_eq_attractive`**: `Ûᴴ·(Ĥ_kin+Ĥ_int')·Û = attractiveHubbardHamiltonian (T + diag(U/2)) U − ¼Σ_x U_x` (eq. 10.2.10). The chemical shift of `−Ĥ_int'` (from `(n̂↑−½)(n̂↓−½)=n̂↑n̂↓−½(n̂↑+n̂↓)+¼`, eq. 10.2.11) is absorbed into the hopping diagonal, leaving the negative scalar constant. **PR #4952 (c6)**: axiom-free; consumes the c4/c5 milestones; feeds the c7 balanced-sector transport. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveShibaConjugation.lean` |
| `sum_spinful_split` / `fermionTotalNumber_eq_diagonal` / `fermionTotalSpinZ_eq_diagonal` / `shibaSignedUnitary_conj_diagonal` / `shibaSignedUnitary_conj_totalNumber` / `shibaSignedUnitary_conj_totalSpinZ` | **Shiba number/spin-z charge exchange** (Tasaki §§9.3.3/10.2.2, eqs. (9.3.48)/(9.3.50), pp. 334–352): the charges `N̂`, `Ŝ³` are diagonal in the Fock basis, and the Shiba flip interchanges them — `Û·N̂·Ûᴴ = 2Ŝ³ + (N+1)·1` and `Ûᴴ·Ŝ³·Û = ½(N̂ − (N+1)·1)`. **PR #4953 (c7 foundation)**: axiom-free; the sector-correspondence identities feeding the balanced-sector transport. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveShibaSector.lean` |
| `spinZSectorEuclidean` / `repulsiveSpinZSector_ground_unique` | **Theorem 10.4, general spin-`z`-sector portion** (Tasaki §10.2.2, pp. 350–352, eqs. (10.2.10)/(10.2.11), **PROVED axiom-free**): for any `N`, an even electron number `0 < Ne < 2(N+1)`, connected real symmetric bipartite hopping and on-site repulsion `U_x > 0`, the symmetric (`μ = U/2`) repulsive Hubbard Hamiltonian has a **unique** ground state on the spin-`z` sector `Ŝ³ = m` with `m = (Ne−(N+1))/2`. Proved by transporting Theorem 10.2 (number sector `N̂ = Ne`) through the Shiba unitary (c6 conjugation + c7 charge exchange `Û N̂ Ûᴴ = 2Ŝ³+(N+1)·1`, so `Ne ↦ m`). The total-spin value is **not** claimed — `Û` maps `Ŝ²` to the η-pseudospin Casimir; identifying it needs the (finite-dimensional) degenerate perturbation theory of Lemma 10.1 (`tasaki_lemma_10_1_degenerate_perturbation`, proved axiom-free). Half-integer `m` (odd `Ne`) is out of scope (Theorem 10.2 requires `Even Ne`). **Number-operator eigenvalue** (Issue #5320, PR #5321 PR-1): because Theorem 10.2's attractive ground state is a spin singlet, its transport lands in the fixed `(N+1)`-electron (half-filling) sector on every spin-`z` sector — `N̂ φ = (N+1)·φ` — independently of `Ne`. **PR #4955 (general-sector PR-1)**. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveBalancedGround.lean` |
| `shibaCrossingSpecies_update_flip_product` / `shibaGauge_update_down_flip_product` / `shibaSignedUnitary_conj_siteSpinPlus` / `shibaSignedUnitary_conj_siteSpinMinus` / `shibaSignedUnitary_conj_spinPlusMinus` | **Shiba spin-operator conjugation** (Tasaki §10.2.2, eq. (10.2.13), p. 353, **PROVED axiom-free**): the Shiba flip sends the transverse spin operators to the on-site pair (η-pairing) operators — `Ûᴴ Ŝ⁺_x Û = ε_x·ĉ†_{x↑}ĉ†_{x↓}`, `Ûᴴ Ŝ⁻_x Û = ε_x·ĉ_{x↓}ĉ_{x↑}` (`ε_x` = sublattice gauge), hence **capstone `shibaSignedUnitary_conj_spinPlusMinus`**: `Ûᴴ(Ŝ⁺_xŜ⁻_y)Û = ε_xε_y·hubbardPairCorrelationOp N x y`. Basis-vec computation mirroring the Shiba down-hop conjugation. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveShibaSpinOp.lean` |
| `fermionSpinTransverse` / `vectorExpectation` / `SameSublattice` / `theorem_10_5_shen_qiu_tian_transverse_sign` | **Theorem 10.5** (Shen–Qiu–Tian; Tasaki §10.2.2, p. 351/353, eqs. (10.2.7)/(10.2.8)/(10.2.13), **PROVED axiom-free** on the general spin-`z` sector `Ŝ³=m` (PR #4956)): for any `N`, even electron number `0<Ne<2(N+1)`, the transverse spin correlation `⟨φ\| Ŝ¹_xŜ¹_y+Ŝ²_xŜ²_y \|φ⟩` (`= ½(Ŝ⁺_xŜ⁻_y+Ŝ⁻_xŜ⁺_y)`) in the unique repulsive-Hubbard ground state on the sector `Ŝ³=m` (`m=(Ne−(N+1))/2`) is real, strictly positive when `x,y` are in the same sublattice and strictly negative otherwise. Proof: transport `φ=Ûφ_attr` through the Shiba unitary via the general-sector uniqueness `repulsiveSpinZSector_ground_unique`, apply the spin↔pair conjugation (eq. 10.2.13) and Tian's positivity (Theorem 10.3). Only the correlation sign is claimed (total-spin value not asserted); the balanced `Ŝ³=0` sector is the `Ne=N+1` special case. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveCorrelation.lean` |
| `fermionStaggeredCasimirOp` / `theorem_10_6_lieb_ferrimagnetism` | **Theorem 10.6** (Shen–Qiu–Tian ferrimagnetism; Tasaki §10.2.3, p. 354, eqs. (10.2.16)/(10.2.17), **AXIOM**): every normalized repulsive-Hubbard ground state satisfies `⟨v\| (Ô_L)² \|v⟩ ≥ ((\|A\|−\|B\|)/2)²`, where `(Ô_L)² = Σ_{x,y} ε_xε_y Ŝ_x·Ŝ_y` (staggered sign `ε_x=±1` per sublattice) — ferrimagnetic long-range order. Reuses `IsLiebRepulsiveModel`. Reflection positivity → faithful documented axiom. | `Fermion/JordanWigner/Hubbard/LiebFerrimagnetism.lean` |
| `bipartiteSignMatrix` / `proposition_10_7_charpoly_neg_eq` / `proposition_10_7_zero_mode_lower_bound` | **Proposition 10.7** (Tasaki §10.2.3, p. 356, **PROVED axiom-free**): for a bipartite real symmetric hopping `T`, (i) the single-electron spectrum is symmetric about zero — `(-T).charpoly = T.charpoly` via the gauge `D T D = -T` (`D = diag(±1)`); (ii) there are at least `\|A\|−\|B\|` zero modes — `Module.finrank (ker T.mulVecLin) ≥ \|A\|−\|B\|` via the `A→B` block map and rank–nullity. The one Chapter-10 item that is finite-dim linear algebra. | `Fermion/JordanWigner/Hubbard/BipartiteSpectrum.lean` |
| `totalPairAnnihilationOperator` / `totalPairCreationOperator` / `totalPairCorrelationOperator` / `symmetricAttractiveHubbardHamiltonian` / `liebShenQiuPairLowerBound` / `theorem_10_8_lieb_shen_qiu_superconductivity` | **Theorem 10.8** (Lieb–Shen–Qiu superconductivity; Tasaki §10.2.3, p. 359, eq. (10.2.22), **AXIOM**): for the **symmetric** attractive Hubbard model `Ĥhop − Σ_x U_x(n̂_↑−½)(n̂_↓−½)` (eq. (10.2.21)) on a bipartite lattice with even `N`, `2\|B\| ≤ N ≤ 2\|A\|`, the unique ground state satisfies `⟨φ\| b̂† b̂ \|φ⟩ ≥ (\|A\|−N/2)(N/2−\|B\|)` with `b̂ = Σ_x ĉ_{x↓}ĉ_{x↑}` — off-diagonal long-range order (fermion-pair condensation / superconductivity). Reflection positivity + Theorem 10.2 uniqueness → faithful documented axiom. | `Fermion/JordanWigner/Hubbard/LiebShenQiu.lean` |

#### Kubo–Kishi finite-temperature susceptibility bound (Tasaki §10.2.5, Theorem 10.11, AXIOM)

The finite-temperature version of Lieb's theorem (Kubo–Kishi): at half-filling the repulsive Hubbard model's charge and on-site pairing susceptibilities are bounded uniformly in temperature and wave number, so there is no CDW or superconducting long-range order. Tasaki states it **without proof**, citing Kubo–Kishi, *Phys. Rev. B* **41**, 4866 (1990) → recorded as a **documented axiom** (same policy as `mielke_theorem_11_13` / `theorem_10_4`).

| Lean name | Statement | File |
|---|---|---|
| `imagTimeEvolve` / `duhamelStaticSusceptibility` | (generic) the imaginary-time (Wick-rotated) Heisenberg evolution `A(τ) = e^{τH}A e^{-τH}` and the **Duhamel (Kubo) static isothermal susceptibility** `χ_{AB}(β) = ∫₀^β (⟨A(τ)B⟩_β − ⟨A⟩_β⟨B⟩_β) dτ` (Tasaki §10.2.5, the fluctuation–dissipation form of the second-derivative susceptibilities eqs. (10.2.53)/(10.2.54); prefactor `1`, `ℂ`-valued `intervalIntegral`) | `Quantum/GibbsState/Duhamel.lean` |
| `grandCanonicalRepulsiveHubbard` / `fourierCharacter` / `chargeFourierMode` / `pairFieldFourierMode` / `chargeSusceptibility` / `pairSusceptibility` | the grand-canonical Hamiltonian `Ĥ − μN̂` (core of eq. (10.2.52)); the 1D wave-number character `w_x = exp(2πi k x/(N+1))` (eq. (10.2.55), `|w_x|=1`, genuine periodic lattice character); the charge Fourier mode `ñ_q = |Λ|^{-1/2}Σ_x w_x n̂_x` and the pairing Fourier mode `p̂_q = |Λ|^{-1/2}Σ_x w_x(ĉ†_{x↑}ĉ†_{x↓}+ĉ_{x↓}ĉ_{x↑})`; and the charge / on-site pairing susceptibilities `χ^c_q`, `χ^p_q` as Duhamel two-point functions of these modes at `±q` (eqs. (10.2.53)/(10.2.54)) | `Fermion/JordanWigner/Hubbard/LiebKuboKishi.lean` |
| `theorem_10_11_kubo_kishi_susceptibility_bound` | **Theorem 10.11** (Kubo–Kishi; Tasaki §10.2.5, pp. 368–369, eqs. (10.2.52)–(10.2.56), **AXIOM**, PR #4957): for the uniform repulsive Hubbard model (`U > 0`, eq. (10.2.5)) on a bipartite real symmetric connected hopping `T` (Theorem 10.4 conditions except electron number) at half-filling `μ = U/2`, for every `β > 0` and every wave number `k` the susceptibilities are real and `χ^c_k(β, U/2) ≤ 1/U`, `χ^p_k(β, U/2) ≤ 2/U` (eq. (10.2.56)) — no CDW or superconducting order at finite temperature. Tasaki states it without proof, citing Kubo–Kishi, *Phys. Rev. B* **41**, 4866 (1990) → faithful documented axiom. | `Fermion/JordanWigner/Hubbard/LiebKuboKishi.lean` |

#### Hubbard effective Hamiltonian on the hard-core sector (Tasaki §11.2)

| Lean name | Statement | File |
|---|---|---|
| <a id="tasaki-chapter-11-part-03"></a> `hubbardEffectiveHamiltonian N t U` | the hard-core compression `Ĥ_eff = P̂_hc · H · P̂_hc` of the full Hubbard Hamiltonian (Tasaki §11.2; 1st ed., pp. 381-388) | `Fermion/JordanWigner/Hubbard/EffectiveHamiltonian.lean` |
| `hubbardEffectiveHamiltonian_isHermitian` | `Ĥ_eff` is Hermitian for Hermitian `t` and real `U` | `Fermion/JordanWigner/Hubbard/EffectiveHamiltonian.lean` |
| `hubbardEffectiveHamiltonian_mulVec_eq_projected_kinetic_of_mem` | `U → ∞` reduction: on a hard-core vector, `Ĥ_eff ψ = P̂_hc (H_hop ψ)` (interaction drops out) | `Fermion/JordanWigner/Hubbard/EffectiveHamiltonian.lean` |
| `hubbardEffectiveHamiltonian_mulVec_mem` | `Ĥ_eff ψ` always lies in `hubbardHardcoreSubspace` | `Fermion/JordanWigner/Hubbard/EffectiveHamiltonian.lean` |
| `hubbardHardcoreProjection_mulVec_effectiveHamiltonian` | `P̂_hc` fixes every vector in the range of `Ĥ_eff` | `Fermion/JordanWigner/Hubbard/EffectiveHamiltonian.lean` |

#### Tasaki ordered-creation basis (Tasaki §11.2, eq. (11.2.3))

| Lean name | Statement | File |
|---|---|---|
| `occupationOf js` | the configuration occupied exactly on the indices in a list `js` | `Fermion/JordanWigner/Hubbard/TasakiBasis.lean` |
| `prod_creation_mulVec_vacuum` | a strictly index-sorted product of creation operators applied to the vacuum yields `basisVec (occupationOf js)`, every Jordan–Wigner string sign being `1` | `Fermion/JordanWigner/Hubbard/TasakiBasis.lean` |
| `tasakiIndexList N τ` / `tasakiIndexList_sorted` / `mem_tasakiIndexList_iff` | the increasing list of occupied JW indices of the all-filled spin-`τ` configuration; strictly sorted; membership iff the spin label selects that orbital | `Fermion/JordanWigner/Hubbard/TasakiBasis.lean` |
| `hubbardTasakiBasisState N x σ` | the (11.2.3) ordered-creation basis state `\|Φ_{x,σ}⟩ = ĉ_{x,↑} (∏_{y} ĉ†_{y,σ̄_y}) \|vac⟩` | `Fermion/JordanWigner/Hubbard/TasakiBasis.lean` |
| `hubbardTasakiBasisState_eq_smul_basisVec` | `\|Φ_{x,σ}⟩ = ε • basisVec (hubbardOneHoleConfig N x σ)`, where `ε` is the single annihilation string sign | `Fermion/JordanWigner/Hubbard/TasakiBasis.lean` |
| `hubbardTasakiBasisSign_mul_self` | the string sign squares to `1` | `Fermion/JordanWigner/Hubbard/TasakiBasis.lean` |
| `hubbardTasakiBasisState_inner` / `hubbardTasakiBasisState_self_inner` | orthonormality inherited from `basisVec`: the pairing is the sign product times the configuration-equality indicator; self-overlap is `1` | `Fermion/JordanWigner/Hubbard/TasakiBasis.lean` |

#### Uniform-sign hole-filling action (Tasaki §11.2, eq. (11.2.4))

| Lean name | Statement | File |
|---|---|---|
| `jwSign_eq_neg_one_pow` | `jwSign N j c = (-1)^{∑_{k<j} (c k).val}` (string sign as parity of occupied modes) | `Fermion/JordanWigner/Hubbard/TasakiHopActionCore.lean` |
| `sum_spinful_reindex` | reindex a sum over the `2N+2` JW modes into a double site/spin sum | `Fermion/JordanWigner/Hubbard/TasakiHopActionCore.lean` |
| `hubbardTasakiBasisSign_eq` | the Tasaki basis sign is `ε = (-1)^x` (independent of `σ`) | `Fermion/JordanWigner/Hubbard/TasakiHopActionCore.lean` |
| `hop_jwSign_source` / `hop_jwSign_target` | the two hop string signs as `(-1)` to the occupied-site count below the source / target orbital | `Fermion/JordanWigner/Hubbard/TasakiHopActionCore.lean` |
| `hubbardTasakiHop_mulVec` | eq. (11.2.4): `ĉ†_{(x,s)} ĉ_{(z,s)} \|Φ_{x,σ}⟩ = -\|Φ_{z, σ_{z→x}}⟩` — the four fermion signs combine to the uniform `-1` (parity `2(x+z)-1` is odd) | `Fermion/JordanWigner/Hubbard/TasakiHopAction.lean` |

#### Effective-Hamiltonian matrix element (Tasaki §11.2, eq. (11.2.5))

| Lean name | Statement | File |
|---|---|---|
| `hubbardEffective_tasaki_matrixElement` | eq. (11.2.5), off-diagonal (`x ≠ y`): `⟨Φ_{y,τ} \| Ĥ_eff \| Φ_{x,σ}⟩ = -t_{x,y}` if `τ = σ_{y→x}` (configurations coincide), else `0`. Only the hole-filling channel `(x, y, σ_y)` survives the hard-core projection | `Fermion/JordanWigner/Hubbard/EffectiveHamiltonianMatrix.lean` |
| `hubbardEffective_tasaki_matrixElement_diag` | eq. (11.2.5), diagonal (`x = y`) under no self-hopping (`∀ i, t_{ii} = 0`): `⟨Φ_{x,τ} \| Ĥ_eff \| Φ_{x,σ}⟩ = 0` — `Ĥ_eff` moves the hole off `x`, so its image is orthogonal to `\|Φ_{x,τ}⟩` | `Fermion/JordanWigner/Hubbard/EffectiveHamiltonianMatrix.lean` |

#### Cauchy–Schwarz energy bound (Tasaki §11.2, eq. (11.2.9))

| Lean name | Statement | File |
|---|---|---|
| `HoleSpin N x` / `holeSpinMoveEquiv` | canonical hole-spin configurations (hole-site spin fixed `↑`); the hole-move bijection `HoleSpin N x ≃ HoleSpin N y` | `Fermion/JordanWigner/Hubbard/WeakNagaoka.lean` |
| `hubbardEffEnergy` / `hubbardEffEnergy_expand` | the real-bilinear effective-Hamiltonian energy; its quadratic-form expansion in the Tasaki basis | `Fermion/JordanWigner/Hubbard/WeakNagaoka.lean` |
| `oneHoleConfig_move_eq_iff` | the hole-move bridge: the moved configuration matches `C_{x,σ}` iff `τ` is the hole-move image | `Fermion/JordanWigner/Hubbard/WeakNagaoka.lean` |
| `hubbardEffEnergy_tasaki_quadratic` | eq. (11.2.9) line 2: `⟨Φ\|Ĥ_eff\|Φ⟩ = -Σ_{x≠y} t_{y,x} Σ_σ c_{x,σ} c_{y, σ_{x→y}}` (no self-hopping) | `Fermion/JordanWigner/Hubbard/WeakNagaoka.lean` |
| `tasakiQuadForm_ferro_le` | the Cauchy–Schwarz bound on the real quadratic form: `Q(Φ_↑) ≤ Q(Φ)` for `t ≥ 0` | `Fermion/JordanWigner/Hubbard/WeakNagaoka.lean` |
| `hubbardWeakNagaoka_energy_bound` | eq. (11.2.9): `⟨Φ_↑\|Ĥ_eff\|Φ_↑⟩ ≤ ⟨Φ\|Ĥ_eff\|Φ⟩` (`t ≥ 0`, `t_{ii}=0`) — the ferromagnetic state is also a ground state | `Fermion/JordanWigner/Hubbard/WeakNagaoka.lean` |
| `tasakiState_orthonormal` | orthonormality of the Tasaki basis (indexed by hole site and canonical hole-spin) | `Fermion/JordanWigner/Hubbard/WeakNagaoka.lean` |
| `tasakiExpansion_normSq` / `ferroCoeff_normSq_eq` / `tasakiFerro_normSq_eq` | eqs. (11.2.7)–(11.2.8): `‖Σ ϕ_p Φ_p‖² = Σ ϕ_p²`, and the ferromagnetic state has the same norm `‖Φ_↑‖ = ‖Φ‖` | `Fermion/JordanWigner/Hubbard/WeakNagaoka.lean` |

#### SU(2) symmetry of the effective Hamiltonian (Tasaki §11.2)

| Lean name | Statement | File |
|---|---|---|
| `fermionTotalSpinPlus_commute_hubbardHardcoreProjection` | `Ŝ^+_tot` commutes with the hard-core projection `P̂_hc` (spin operators preserve the no-double-occupancy subspace) | `Fermion/JordanWigner/Hubbard/EffectiveHamiltonianSpinSymmetry.lean` |
| `fermionTotalSpinPlus_commute_hubbardEffectiveHamiltonian` / `fermionTotalSpinMinus_commute_hubbardEffectiveHamiltonian` | `[Ĥ_eff, Ŝ^±_tot] = 0` — the effective Hamiltonian inherits SU(2) symmetry (the backbone of the `(2S_max+1)`-degeneracy in Theorem 11.5) | `Fermion/JordanWigner/Hubbard/EffectiveHamiltonianSpinSymmetry.lean` |
| `fermionTotalSpinZ_commute_hubbardEffectiveHamiltonian` / `fermionTotalSpinSquared_commute_hubbardEffectiveHamiltonian` | `[Ĥ_eff, Ŝ^z_tot] = 0` and `[Ĥ_eff, (Ŝ_tot)²] = 0` — `Ĥ_eff` conserves total spin, so its eigenspaces split into fixed-`S_tot` sectors (Theorem 11.5 / 11.7) | `Fermion/JordanWigner/Hubbard/EffectiveHamiltonianSpinSymmetry.lean` |

#### Weak Nagaoka spin multiplet (Tasaki §11.2.1, Theorem 11.5 core)

| Lean name | Statement | File |
|---|---|---|
| `fermionTotalSpinPlus_commutator_fermionTotalSpinMinus` | SU(2) ladder commutator `[Ŝ^+_tot, Ŝ^-_tot] = 2 Ŝ^z_tot` (Tasaki §9.3.3) | `Fermion/JordanWigner/Hubbard/WeakNagaokaTheoremCore.lean` |
| `fermionTotalSpinSquared_commute_fermionTotalSpinMinus` | `[(Ŝ_tot)², Ŝ^-_tot] = 0` — the Casimir is constant along the spin-lowering tower | `Fermion/JordanWigner/Hubbard/WeakNagaokaTheoremCore.lean` |
| `fermionTotalSpinPlus_mul_fermionTotalSpinMinus` | `Ŝ^+_tot Ŝ^-_tot = (Ŝ_tot)² − Ŝ^z_tot(Ŝ^z_tot − 1)` — raising-after-lowering via the Casimir | `Fermion/JordanWigner/Hubbard/WeakNagaokaTheorem.lean` |
| `fermionTotalSpinZ_mulVec_spinMinusPow` / `fermionTotalSpinSquared_mulVec_spinMinusPow` | `Ŝ^z` / Casimir towers: `(Ŝ^-_tot)^k v` has `Ŝ^z = N/2 − k` and constant Casimir, for any highest-weight `v` | `Fermion/JordanWigner/Hubbard/WeakNagaokaTheoremCore.lean` (`fermionTotalSpinZ_mulVec_spinMinusPow`) + `Fermion/JordanWigner/Hubbard/WeakNagaokaTheorem.lean` (`fermionTotalSpinSquared_mulVec_spinMinusPow`) |
| `spinMinusPow_ne_zero` / `spinMinusPow_linearIndependent` | the `N+1` lowered states `(Ŝ^-_tot)^k v` (`k ≤ N`) are nonzero (ladder eigenvalue `(k+1)(N−k) ≠ 0`) and linearly independent (distinct `Ŝ^z`) | `Fermion/JordanWigner/Hubbard/WeakNagaokaTheorem.lean` |
| `weakNagaoka_spinMultiplet` | **Theorem 11.5 core**: a nonzero highest-weight `Ĥ_eff`-eigenvector `v` (`Ŝ^+_tot v = 0`, `Ŝ^z_tot v = (N/2) v`) generates `N+1 = 2 S_max + 1` linearly independent `Ĥ_eff`-eigenvectors `(Ŝ^-_tot)^k v` at the same energy, all with `S_tot = S_max = N/2`. | `Fermion/JordanWigner/Hubbard/WeakNagaokaTheorem.lean` |
| `tasakiEffMatrix` / `tasakiEffMatrixUp` | the Tasaki matrix `M = Tᴴ Ĥ_eff T` of `Ĥ_eff` in the one-hole basis (Hermitian, entries `⟨Φ_q\|Ĥ_eff\|Φ_p⟩`) and its all-up principal block `M_↑` (Hermitian); `Ĥ_eff` preserves the all-up sector | `Fermion/JordanWigner/Hubbard/WeakNagaokaGroundState.lean` |
| `hubbardEffectiveHamiltonian_mulVec_tasakiState` / `_tasakiExpansion` | operator lift: `Ĥ_eff Φ_p = Σ_q ⟨Φ_q\|Ĥ_eff\|Φ_p⟩ Φ_q` and `Ĥ_eff (Σ c_q Φ_q) = Σ_q (M c)_q Φ_q` (via particle-number conservation, sector completeness `tasaki_completeness`) | `Fermion/JordanWigner/Hubbard/WeakNagaokaGroundStateCore.lean` + `Fermion/JordanWigner/Hubbard/WeakNagaokaGroundState.lean` |
| `weakNagaoka_theorem_11_5` | **Tasaki Theorem 11.5 (weak Nagaoka, effective one-hole sector)**: there exist `N+1 = 2 S_max+1` linearly independent `Ĥ_eff`-eigenvectors at the maximal-spin sector minimum energy, all with `S_tot = S_max = N/2`; the ground state is the all-up state from a minimum eigenvector of the all-up block `M_↑` (no Perron–Frobenius). | `Fermion/JordanWigner/Hubbard/WeakNagaokaGroundState.lean` |
| `weakNagaoka_theorem_11_5_global` | **Tasaki Theorem 11.5 (global form)**: for `t ≥ 0` symmetric, the all-up minimum equals the global one-hole minimum (`hermitianMinEigenvalue M_↑ = hermitianMinEigenvalue M`, via the Schwarz bound (11.2.9) ferromagnetization on the real Tasaki matrix), so the `N+1` degenerate eigenvectors are genuine **ground states** with `S_tot = S_max` | `Fermion/JordanWigner/Hubbard/WeakNagaokaGlobalMin.lean` |
| `mulVec_eq_smul_of_rayleighOnVec_eq_min` | Hermitian variational equality: a nonzero vector attaining the minimum-eigenvalue Rayleigh bound is a minimum eigenvector (companion to the variational lower bound) | `Quantum/SpinS/HermitianVariationalEquality.lean` |

| Lean name | Statement | File |
|---|---|---|
| `hubbardKineticOnGraph N G J` | spinful Hubbard kinetic operator from a `SimpleGraph G` and edge weight `J` | `Fermion/JordanWigner.lean` |
| `hubbardKineticOnGraph_commute_fermionTotalNumber` / `hubbardKineticOnGraph_isHermitian` | charge conservation always; Hermiticity for real `J` | `Fermion/JordanWigner/Hubbard/Graph.lean` |
| `hubbardHamiltonianOnGraph N G J U` | full Hubbard Hamiltonian from a graph + on-site coupling | `Fermion/JordanWigner.lean` |
| `hubbardHamiltonianOnGraph_commute_fermionTotalNumber` / `hubbardHamiltonianOnGraph_isHermitian` | charge conservation; Hermiticity for real `J` and real `U` | `Fermion/JordanWigner/Hubbard/Graph.lean` |
| `hubbardChainHamiltonian N J U` | the canonical 1D nearest-neighbour Hubbard chain `−J Σ_{σ,⟨i,j⟩} c_{iσ}† c_{jσ} + U Σ_i n_{i↑} n_{i↓}` (built from `pathGraph (N+1)`) | `Fermion/JordanWigner.lean` |
| `hubbardChainHamiltonian_isHermitian` / `hubbardChainHamiltonian_commute_fermionTotalNumber` | Hermiticity (real `J, U`) and charge conservation | `Fermion/JordanWigner/Hubbard/Graph.lean` |
| `hubbardHamiltonianOnGraph_mulVec_vacuum` / `hubbardChainHamiltonian_mulVec_vacuum` | both graph-built Hubbard Hamiltonians annihilate the JW vacuum | `Fermion/JordanWigner/Hubbard/Graph.lean` |
| `hubbardChainGibbsState N β J U` | Gibbs state of the 1D Hubbard chain | `Fermion/JordanWigner.lean` |
| `hubbardChainGibbsState_isHermitian` / `hubbardChainGibbsState_commute_hamiltonian` | Hermiticity (real `J, U`) and commute with the Hamiltonian | `Fermion/JordanWigner/Hubbard/Graph.lean` |
| `hubbardCycleGibbsState_commute_hamiltonian` | the periodic Hubbard Gibbs state commutes with the periodic Hubbard Hamiltonian (companion of the open-chain version, free corollary of `gibbsState_commute_hamiltonian`) | `Fermion/JordanWigner/Hubbard/Graph.lean` |
| `hubbardChainGibbsExpectation_zero` / `_im_of_isHermitian` / `_commutator_hamiltonian` / `_hamiltonian_im` / `_hamiltonian_pow_im` / `hubbardChain_partitionFn_im` / `_ofReal_re_eq` / `hubbardChainGibbsState_pow_trace` | open-chain Hubbard expectation companions (β = 0 closed form, Hermitian-observable real, conservation, energy / energy-power expectations real, partition function real, real-cast, Rényi-n trace) | `Fermion/JordanWigner/Hubbard/Graph.lean` |
| `hubbardCycleGibbsExpectation_zero` / `_im_of_isHermitian` / `_commutator_hamiltonian` / `_hamiltonian_im` / `_hamiltonian_pow_im` / `hubbardCycle_partitionFn_im` / `_ofReal_re_eq` / `hubbardCycleGibbsState_pow_trace` | periodic Hubbard chain expectation companions (same family as the open chain) | `Fermion/JordanWigner/Hubbard/Graph.lean` |

#### Nagaoka's theorem on a magnetization sector (Tasaki §11.2.2, Theorem 11.7 / Lemma 11.9)

| Lean name | Statement | File |
|---|---|---|
| `tasakiEffReMatrixOnSector_ground_finrank_le_one` | **Theorem 11.7 core** (Tasaki, 1st ed. (2020), §11.2.2, Theorem 11.7, p. 385; PR #5149): on a non-empty magnetization sector whose Perron–Frobenius companion `nagaokaPFMatrixOnSector` is irreducible (the per-sector instance of the Definition 11.6 connectivity condition `nagaokaConnectivity`), the sector effective matrix `tasakiEffReMatrixOnSector` has a strictly positive eigenvector at the eigenvalue `−μ` (`μ` = the Perron eigenvalue of the negated matrix) and that eigenspace has `finrank ≤ 1`. Since `−μ = min spec M\|_sector`, this is the non-degeneracy of the sector ground state that Nagaoka's theorem asserts | `Fermion/JordanWigner/Hubbard/NagaokaMagnetizationSector.lean` |
| `reachSwapOff_of_exchangeReachable` | **Lemma 11.9: spin-swap generation along exchange bonds** (Tasaki §11.2.2, proof of Lemma 11.9 and footnote 13, p. 387; PR #5149): if `x ≠ y` are joined in the exchange-bond graph of `nagaokaBondGraph`, then the transposition `{x, y}` is reachable off a finite site set — from every hole position avoiding the support of the connecting exchange-bond walk, `(p, σ)` reaches `(p, swap_{x,y} σ)`. Under `ConnectedByExchangeBonds` the hypothesis holds for every pair, which is exactly the step "we can generate any permutation of spin configurations by successive exchanges on the exchange bonds" that Tasaki's footnote 13 defers to the property (iii) of p. 41 | `Fermion/JordanWigner/Hubbard/NagaokaStateQuiverReach.lean` |

#### General flat-band ground states: the annihilation peel behind eq. (11.3.46) (Tasaki §11.3.4)

| Lean name | Statement | File |
|---|---|---|
| `spinfulAnnihilation_rangeT_mulVec_eq_zero_of_mem_groundSubmodule` | **Range-`T` annihilation of a ground-submodule vector** (Tasaki, 1st ed. (2020), §11.3.4, premise of eq. (11.3.46), p. 412; PR #5149): for PSD hopping `T` and `U > 0`, every `Φ` in `generalFlatBandGroundSubmodule T U` is killed by every smeared annihilator built from the rows of a factorisation `T = Cᴴ · C`, i.e. `Ĉ_σ(∑_k a_k C_k) Φ = 0` for all coefficient vectors `a`. This is the ground-submodule packaging of the `Ĥ_hop Φ_GS = 0` step from which Tasaki concludes that a ground state is written only in terms of the `â†` operators | `Fermion/JordanWigner/Hubbard/GeneralFlatBandGroundAnnihilation.lean` |
| `site_annihilation_mulVec_generalFlatBandSlaterState` | **Site annihilator kills a Slater state vanishing at that site** (Tasaki §11.3.4, peel step toward eq. (11.3.46), p. 412; PR #5149): if `μ_q(z) = 0` for every mode `q` in the list, then `ĉ_{z,σ}` anticommutes through every `â†` factor of `generalFlatBandSlaterState μ qs` (site-dual CAR with vanishing pairing) and annihilates the vacuum. On the index set `I` this is the biorthogonality statement for `z ∈ I` not among the listed modes | `Fermion/JordanWigner/Hubbard/GeneralFlatBandOperators.lean` |

<!-- legacy-source:end:2605:2731 -->

## Authoritative supplemental implementation record (Hubbard chain / cycle Gibbs expectation companions)

This section is maintained by hand, lies outside the migrated catalogue block above, and records the
current membership of the open-chain and periodic Hubbard Gibbs expectation companion families in
`Fermion/JordanWigner/Hubbard/Graph.lean`. The migrated catalogue block above is a frozen historical
record — its rows are pinned byte-for-byte by `scripts/check_docs_hierarchy.py` and are never edited
for later deletions or relocations, so the two slash-separated stem-shorthand family rows and the
phrase "same family as the open chain" describe membership as it stood at migration time.

The open chain retains `hubbardChainGibbsExpectation_zero`,
`hubbardChainGibbsExpectation_hamiltonian_im`, `hubbardChainGibbsExpectation_hamiltonian_pow_im`,
`hubbardChain_partitionFn_im` and `hubbardChainGibbsState_pow_trace`. The periodic chain retains
`hubbardCycleGibbsExpectation_zero`, `hubbardCycle_partitionFn_im` and
`hubbardCycleGibbsState_pow_trace`; the two families are therefore no longer identical in membership.

Retired as unreferenced one-line specializations of the generic `gibbsExpectation*` and
`gibbsState_commute_hamiltonian` primitives (which are themselves unchanged in
`Quantum/GibbsState.lean`): `hubbardChainGibbsExpectation_im_of_isHermitian`,
`hubbardChainGibbsExpectation_commutator_hamiltonian`,
`hubbardChainGibbsExpectation_ofReal_re_eq`, `hubbardGibbsStateOnGraph_commute_hamiltonian`,
`hubbardCycleGibbsExpectation_im_of_isHermitian`,
`hubbardCycleGibbsExpectation_commutator_hamiltonian`,
`hubbardCycleGibbsExpectation_hamiltonian_im`,
`hubbardCycleGibbsExpectation_hamiltonian_pow_im` and
`hubbardCycleGibbsExpectation_ofReal_re_eq`.

## Authoritative supplemental implementation record (§10.2.2 eq. (10.2.9) uniform/symmetric ground-submodule reduction)

This section is maintained by hand, lies outside the migrated catalogue block above, and records
a new supporting theorem (the eq. (10.2.9) reduction step) added after the migration baseline; it
is not subject to the frozen byte-for-byte parity of the block above.

| Lean name | Statement | File |
|---|---|---|
| `symmetricRepulsiveHubbardHamiltonian_groundSubmodule_eq_uniform` | **Eq. (10.2.9): symmetric-interaction ground submodule reduces to the uniform-interaction one** (Tasaki, 1st ed. (2020), §10.2.2, p. 351, eq. (10.2.9); **PROVED**, `#print axioms` = std3, Issue #5320, PR #5321 PR-1): per site, `U(n̂↑−½)(n̂↓−½) = U n̂↑n̂↓ − (U/2)(n̂↑+n̂↓) + U/4`, so summing over all `N+1` sites gives `Ĥint^{sym}(U) = Ĥint^{unif}(U) − (U/2)N̂ + (U/4)|Λ|`, and hence, restricted to the `N̂ = Ne` electron-number sector, `Ĥ^{rep,sym}(U) = Ĥ^{rep,unif}(U) + c·1` with the scalar shift `c = −(U/2)Ne + (U/4)(N+1)`. Consequently, for every target energy `E`, the `E`-ground submodule of the symmetric-interaction Hamiltonian on the `Ne`-electron sector equals the `(E−c)`-ground submodule of the uniform-interaction Hamiltonian on the same sector — the two variants have the same ground submodule up to the constant energy shift `c`, supplying the form conversion (10.2.6) → (10.2.5) **at a constant `U`**. Scope: `hubbardGroundSubmoduleAtElectronNumber` is the `E`-eigenspace intersected with the `Ne`-electron sector and carries **no** minimality of `E`, so what this transports between the two variants is the eigenspace equality alone; transporting ground-state minimality (that the shifted `E − c` is the least sector eigenvalue) is a separate step. Axiom-free; finite-dimensional linear algebra (per-site algebraic identity + `Finset.sum` bookkeeping), no operator-algebra dependency. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveBalancedGround.lean` |
| `minEnergyOn` / `minEnergyOn_isGroundEigenvalueOn` / `abs_minEnergyOn_sub_le_sum_entryNorms` / `Continuous.minEnergyOn_comp` | **Minimum energy on a subspace** (generic infrastructure for the Theorem 10.4 discharge arc, Issue #5320, PR #5322 PR-2; **PROVED**, `#print axioms` = std3): `minEnergyOn W H = inf {re⟪v,Hv⟫ \| v∈W, ‖v‖=1}` restricts the whole-space `hermitianMinEigenvalue` variational functional (`Quantum/SpinS/SubmatrixMinEigenvalue.lean`; Lipschitz and continuity properties in `HermitianMinEigenvalueLipschitz.lean` / `HermitianMinEigenvalueContinuous.lean`) to unit vectors of a fixed nonzero subspace `W`. `minEnergyOn_isGroundEigenvalueOn` identifies it with the ground eigenvalue produced by `exists_isGroundEigenvalueOn` (`DegeneratePerturbationGroundEnergy.lean`), the `≥` half being that file's variational sharpness bound `IsGroundEigenvalueOn.mul_norm_sq_le`; the entry-norm Lipschitz bound and its parameter-continuity corollary mirror `abs_hermitianMinEigenvalue_sub_le_sum_entryNorms` / `Continuous.hermitianMinEigenvalue_comp`. Not itself a direct formalization of a numbered Tasaki statement — a reusable building block feeding the continuity arguments of Theorem 10.4's proof. Axiom-free. | `Math/MatrixAnalysis/MinEnergyOnSubspace.lean` |
| `numberSpinZSectorEuclidean` / `numberSpinZCasimirSectorEuclidean` / `numberSpinZSectorEuclidean_mem_of_commute` / `numberSpinZCasimirSectorEuclidean_mem_of_commute` / `exists_unique_casimir_sector_strict_min` | **Casimir sector machinery** (generic infrastructure for the Theorem 10.4 discharge arc, Issue #5320, PR #5324 PR-3; **PROVED**, `#print axioms` = std3): `numberSpinZSectorEuclidean N L m₀` is the joint sector `K = {N̂ = L} ⊓ {Ŝ³ = m₀}` (refining `spinZSectorEuclidean` by also fixing the total number `N̂`), and `numberSpinZCasimirSectorEuclidean N L m₀ c` further refines it by the total-spin Casimir eigenvalue, `K_c = K ⊓ ker(Ŝ² − c)`. **Proposition 1** (`numberSpinZSectorEuclidean_mem_of_commute`, `numberSpinZCasimirSectorEuclidean_mem_of_commute`): any Hamiltonian commuting with `N̂`, `Ŝ³` (and, for `K_c`, also `Ŝ²`) preserves `K` and `K_c`. **Proposition 2** (`exists_unique_casimir_sector_strict_min`): if `H` is Hermitian, commutes with `N̂`, `Ŝ³`, `Ŝ²`, and has a *unique* normalized ground state on `K`, then there is an occupied Casimir sector `K_c` (`K_c ≠ ⊥`) attaining the ground energy, unique among occupied sectors — every other occupied sector `K_{c'}` (`c' ≠ c`, `K_{c'} ≠ ⊥`) is strictly higher. Unoccupied sectors (`K_{c'} = ⊥`) are excluded on both sides, since `minEnergyOn ⊥ H` is a junk value. Not itself a direct formalization of a numbered Tasaki statement — sector-invariance/unique-minimality infrastructure that the later homotopy/perturbation PRs of the arc (PR-4 onward) use to locate the one Casimir eigenvalue the transported ground state occupies. **PR-13b extends** the conclusion of `exists_unique_casimir_sector_strict_min` with the membership witness `φ ∈ K_c` (needed by the selector-uniqueness argument across several couplings, `LiebRepulsiveCasimirPinning.lean`); of the three consumers, `liebRepulsive_exists_unique_casimir_sector` (`LiebRepulsiveSectorBridgeFinal.lean:194`, call site `:223`) discards the new component via `-`, while `LiebRepulsiveCasimirPinning.lean:385` and `LiebRepulsiveMultipletCompanion.lean:186` both use the membership witness. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveCasimirSector.lean` |
| `mulVec_mem_eigenspace_of_commute` / `toEuclideanLin_mem_eigenspace_of_commute` | **A commuting matrix preserves every eigenspace** (generic matrix analysis, Issue #5320, PR #5324 PR-3; **PROVED**, `#print axioms` = std3): from `B x = e x` and `A B = B A` one gets `B (A x) = A (B x) = e (A x)`, so `A` maps each `B`-eigenspace into itself. Stated for both carriers of the finite-dimensional matrix action — the function carrier `n → ℂ` acting by `Matrix.mulVecLin`, and the inner-product carrier `EuclideanSpace ℂ n` acting by `Matrix.toEuclideanLin`, the latter a transport of the former along `WithLp.ofLp` (which intertwines the two actions definitionally). Single home for the eigenspace-invariance step used by the full-`Ne`-sector weight decomposition (`Fermion/JordanWigner/Hubbard/LiebAttractiveFullSectorUnique.lean`) and by the Casimir sector invariance above. Axiom-free. | `Math/MatrixAnalysis/CommutingEigenspaceInvariance.lean` |
| `homotopyHamiltonian` / `homotopyHopping` / `liebEndpointHopping` / `homotopyOnSite` / `homotopyHamiltonian_zero` / `continuous_homotopyHamiltonian` / `continuous_minEnergyOn_homotopyHamiltonian` / `casimirSelector_eq_const_of_locally_unique_strict_min` | **Homotopy continuity and local-constancy for the deformation argument** (generic infrastructure for the Theorem 10.4 discharge arc, Issue #5320, PR #5325 PR-4; **PROVED**, `#print axioms` = std3): sets up Tasaki's coupling deformation `H_s` (p. 353) — the convex-combination hopping homotopy `T_s = (1-s)T + sT_1` (`homotopyHopping`) toward an explicit uniform `±λ` endpoint (`liebEndpointHopping`) and the matching on-site homotopy `U_s = (1-s)U + s` (`homotopyOnSite`), assembled into `H_s` (`homotopyHamiltonian`) via the uniform repulsive Hubbard Hamiltonian — together with the following algebraic/analytic lemmas: the base point `H_0 = Ĥ^{rep,unif}(T, U)` (`homotopyHamiltonian_zero`), sign preservation on the original support and the value `sλ` on added `A`-`B` edges, vanishing of same-sublattice entries, symmetry/bipartition/connectivity preservation along the homotopy (connectivity needs the strict endpoint hypothesis `0 < λ`, not merely `λ ≠ 0`, since a negative `λ` can cancel the original entry at an interior `s`), positivity of `U_s`, and continuity of `s ↦ H_s` (transported to continuity of the sector-restricted minimum energy via PR-2's `Continuous.minEnergyOn_comp`). The topological core, `casimirSelector_eq_const_of_locally_unique_strict_min`, upgrades PR-3's local (pointwise-in-`s`) unique-occupied-Casimir-sector conclusion to a single sector occupied for *every* `s ∈ [0, 1]` — the original model at `s = 0` included — via `IsLocallyConstant.apply_eq_of_preconnectedSpace` on the preconnected set `[0, 1]`; it assumes continuity of the sector minimum energy only at *occupied* sectors (matching what `continuous_minEnergyOn_homotopyHamiltonian` supplies) and assumes no continuity of the ground energy `E`, which is recovered from the fixed-sector continuity through the strict-minimality hypothesis. **Scope.** `homotopyOnSite` is a scalar, so `H_s` deforms the **uniform** Hamiltonian (eq. (10.2.5), one `U > 0`), not the site-dependent symmetric form (eq. (10.2.6)). What PR-1's `symmetricRepulsiveHubbardHamiltonian_groundSubmodule_eq_uniform` (eq. (10.2.9)) supplies is the form conversion (10.2.6) → (10.2.5) **at a constant `U`**: its coupling argument is a scalar `U : ℝ` and it compares `symmetricRepulsiveHubbardHamiltonian N T (fun _ => U)` with `repulsiveHubbardHamiltonian N T U`, so a non-constant `U_x` never occurs in it, and what it transports is the eigenspace equality alone (no minimality in either direction). **Open obligation (Issue #5320).** The site-dependent reduction `U_x → U` — Tasaki's own first deformation step, "For each `x ∈ Λ`, we change `U_x` to a common constant `U > 0`" (p. 353) — is therefore discharged nowhere yet: expanding the symmetric form with a genuine `U : Λ → ℝ` gives `Σ_x U_x n̂_{x,↑} n̂_{x,↓} − (1/2) Σ_x U_x (n̂_{x,↑} + n̂_{x,↓}) + (1/4) Σ_x U_x`, whose middle term is a site-dependent one-body potential rather than a multiple of the total number `N̂`, so PR-1's constant-energy-shift argument is inapplicable in principle, and this file cannot supply the step either (`homotopyOnSite : ℝ → ℝ → ℝ` is a scalar by definition). The second disjunct of `IsLiebRepulsiveHamiltonian` (the site-dependent symmetric form) thus remains unreduced, and the arc's final assembly of Theorem 10.4 (PR-12/PR-13) must either discharge it or restrict the theorem's hypothesis accordingly. This file starts from a uniform model, and what is deformed here is `U → 1`. **Provenance.** Tasaki's endpoint keeps the original bond set — "We do not modify `t_{x,y}` which is zero to begin with" (p. 353) — whereas `liebEndpointHopping` carries `sign(T x y) · λ` on the original support and `λ` on every *previously vanishing* `A`-`B` pair, completing the support to the full bipartite graph on `A ⊔ B`. That is a deliberate strengthening of the arc's design (the same "independent-route replacement" pattern as the Lemma 10.1 discharge, #5313), not a formalization of the book's stated deformation; it is harmless for what the arc consumes (the endpoint stays symmetric, bipartite and connected), its one visible consequence being that the `λ → 0` effective Heisenberg model (eq. (10.2.12)) sits on the complete bipartite graph rather than on the original bond set `{ {x, y} | t_{x,y} ≠ 0 }` — both bipartite for the same `A ⊔ B` and connected, hence the same Lieb–Mattis total spin `S₀ = ||A| − |B||/2`. Not itself a direct formalization of a numbered Tasaki statement — the homotopy-independent topological machinery that the concrete endpoint identification (PR-5 onward) reuses verbatim. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveHomotopyContinuity.lean` |
| `kernelProjectionMatrix_eq_of_fixes_kernel` / `kernelProjectionMatrix_diagonal` / `configSectorCompress_apply` / `liebHalfFillingPred` / `liebPerturbationH0` / `liebPerturbationV` / `homotopyHamiltonian_one_eq_perturbedHamiltonian` / `liebPerturbationH0_posSemidef` / `liebPerturbationH0_isReducedInverse` / `kernelProjectionMatrix_liebPerturbationH0_eq_hardcoreProjection` / `liebPerturbationH0Compressed` / `liebPerturbationVCompressed` / `liebPerturbationH0Compressed_posSemidef` / `liebPerturbationVCompressed_isHermitian` / `kernelProjectionMatrix_liebPerturbationH0Compressed_eq_diagonal` / `kernelProjection_mul_liebPerturbationVCompressed_mul_kernelProjection` | **Perturbation setup at the homotopy endpoint (Theorem 10.4 arc, Issue #5320, PR #5326 PR-5; PROVED, `#print axioms` = std3): not itself a direct formalization of a numbered Tasaki statement — generic degenerate-perturbation-theory infrastructure that sets up the `λ → 0` deformation of §10.2.2 (p. 353) as an instance of Tasaki Lemma 10.1's `Ĥ(λ) = Ĥ₀ + λV̂` contract (`tasaki_lemma_10_1_degenerate_perturbation`, discharged independently in issue #5313).** Generic layer (`Math/MatrixAnalysis/DegeneratePerturbation.lean`, `Math/MatrixAnalysis/MinEnergyOnSubspace.lean`): `kernelProjectionMatrix_eq_of_fixes_kernel` — a Hermitian matrix that maps into `ker H` and fixes every kernel vector *is* the orthogonal projection onto `ker H`; `kernelProjectionMatrix_diagonal` — the kernel projection of a diagonal matrix is the diagonal indicator of its zero entries; `minEnergyOn_add_const_smul_one` — shifting a Hamiltonian by `c • 1` shifts `minEnergyOn` by the same constant `c` (normalises the energy origin before comparing with the second-order effective Hamiltonian). Model-specific layer (`Fermion/JordanWigner/Hubbard/HubbardImpossibilityLowUVariationalCore.lean`, `LiebRepulsivePerturbationSetup.lean`, new file): `configSectorCompress_apply` — the `(s, s')` entry of the sector-compressed matrix `Tᴴ A T` is the Fock-space matrix element `A_{s,s'}`; `liebPerturbationH0`/`liebPerturbationV` — the on-site interaction `Ĥ₀ = Σ_x n̂_{x↑}n̂_{x↓}` (`U = 1`) and the unit-coupling endpoint hopping `V̂`, related to `H_s` at `s = 1` by `homotopyHamiltonian_one_eq_perturbedHamiltonian`; `liebPerturbationH0_posSemidef`, and the diagonal/kernel identification `kernelProjectionMatrix_liebPerturbationH0_eq_hardcoreProjection` (`ker Ĥ₀` = the hard-core subspace) together with the explicit reduced inverse `liebPerturbationH0_isReducedInverse`. `liebHalfFillingPred` is the half-filled, fixed-`Ŝ³` configuration-basis sector `K`; `liebPerturbationH0Compressed`/`liebPerturbationVCompressed` are the compressions of `Ĥ₀`, `V̂` to `K` (PSD and Hermiticity survive compression). **The capstone `kernelProjection_mul_liebPerturbationVCompressed_mul_kernelProjection` proves `P̂₀ V̂ P̂₀ = 0` only inside the compressed sector `K`, under the hypothesis that the hopping matrix respects the bipartition — this statement is FALSE on the whole Fock space** (a hard-core configuration with an empty site can absorb a hopping electron and stay hard-core, so `P̂₀ V̂ P̂₀ ≠ 0` there), so the half-filling restriction is essential, not cosmetic. What the proof uses of the hopping matrix is only the vanishing of the endpoint diagonal (`liebEndpointHopping_diag_eq_zero`), which removes the on-site number-operator part of `V̂`; bipartiteness is a sufficient condition for that, not a necessary one — a non-bipartite hopping matrix with vanishing diagonal gives the same first-order vanishing on the half-filled sector. **Left for later PRs of the arc (status after PR-6, Issue #5320, PR #5327)**, all on the compressed sector `K`: (a) the compressed `IsReducedInverse` — **discharged** by PR-6's `liebPerturbationH0Compressed_isReducedInverse` (`LiebRepulsiveSuperexchangeReducedInverse.lean`), the bridge from this `Ĥeff = −P̂₀V̂Ĥ₀⁻¹V̂P̂₀` setup to the PR-3 `K`-definition; (b) the compressed `perturbedHamiltonian` bridge `Ĥ_{s=1}(λ)|_K = Ĥ₀|_K + λ V̂|_K` — **discharged** by PR-6's `homotopyHamiltonian_one_compressed_eq_perturbedHamiltonian`, together with the sector-preservation of `V̂` it presupposed, proved separately by PR-6's `liebPerturbationV_preserves_liebHalfFillingPred`; and (c) nonemptiness of the compressed sector, `Nonempty (configSector N (liebHalfFillingPred N nUp))` — **discharged** by PR-6's `configSector_liebHalfFillingPred_nonempty`, an instance hypothesis of `tasaki_lemma_10_1_degenerate_perturbation`. **Debt.** What the compressed layer consumes from the whole-Fock-space layer is only the diagonality of `Ĥ₀` (`liebPerturbationH0_mulVec_basisVec`, read off in the sector basis through `configSectorCompress_apply`), `liebPerturbationH0_posSemidef`, and the entries of `V̂` between singly-occupied configurations; PR-6 additionally consumes the whole-space bridge `homotopyHamiltonian_one_eq_perturbedHamiltonian` and the *definition* `liebPerturbationH0Inv`, whose compression **is** `liebPerturbationH0InvCompressed`. What it does not consume is the *statement* `liebPerturbationH0_isReducedInverse`, because PR-6 proves the compressed contract off the diagonal form (`liebPerturbationH0InvCompressed_eq_diagonal`) instead of transporting the whole-Fock-space one; **that theorem alone is the reference-0 remnant of the pair**, and its deletion debt is still active and unresolved (the definition is referenced and cannot be deleted). Still at reference 0 after PR-6, and so still debt rather than settled API: on the whole Fock space the hard-core projection identity `kernelProjectionMatrix_liebPerturbationH0_eq_hardcoreProjection` (itself the sole consumer of the kernel criterion `mem_matrixKernel_liebPerturbationH0_iff`) and `liebPerturbationH0_isReducedInverse`; on the compressed sector `liebPerturbationH0Compressed_posSemidef` (sole consumer of `liebPerturbationH0_posSemidef`), `liebPerturbationVCompressed_isHermitian`, and the capstone `kernelProjection_mul_liebPerturbationVCompressed_mul_kernelProjection` — PR-6's `secondOrderEffectiveHamiltonian_liebPerturbation_eq` reaches `−P̂₀ V̂\|_K V̂\|_K P̂₀` from the diagonal reduced inverse alone and uses none of these three; and in the generic layer `minEnergyOn_add_const_smul_one` (**deleted in PR-13a** as a decorative reference-0 declaration, per its own doc comment's stipulation, rather than staged a further round; see the PR-13a row below). The rest were staged, unconsumed until the arc assembly (PR-11 to PR-13). Whatever that assembly does not consume is to be deleted, not kept. | `Fermion/JordanWigner/Hubbard/LiebRepulsivePerturbationSetup.lean` (+ `Math/MatrixAnalysis/DegeneratePerturbation.lean`, `Math/MatrixAnalysis/MinEnergyOnSubspace.lean`, `Fermion/JordanWigner/Hubbard/HubbardImpossibilityLowUVariationalCore.lean`) |
| `liebPerturbationV_preserves_liebHalfFillingPred` / `liebPerturbationH0InvCompressed` / `liebPerturbationH0InvCompressed_eq_diagonal` / `liebPerturbationH0Compressed_isReducedInverse` / `liebPerturbationV_intermediate_weight_eq_one` / `liebPerturbationH0InvCompressed_mul_V_mul_kernelProjection` / `secondOrderEffectiveHamiltonian_liebPerturbation_eq` / `configSector_liebHalfFillingPred_nonempty` / `homotopyHamiltonian_one_compressed_eq_perturbedHamiltonian` | **Superexchange reduced-inverse layer (Theorem 10.4 arc, Issue #5320, PR #5327 PR-6; PROVED, `#print axioms` = std3): not itself a direct formalization of a numbered Tasaki statement — generic infrastructure discharging PR-5 debt items (a)-(c) and preparing the superexchange operator identity that PR-7/PR-8 compute.** Discharges PR-5 debt (a): `liebPerturbationH0Compressed_isReducedInverse` is the compressed `IsReducedInverse` contract for `Ĥ₀|_K`, built directly off the diagonal form (`liebPerturbationH0InvCompressed`, `liebPerturbationH0InvCompressed_eq_diagonal`) rather than by compressing the whole-Fock-space `liebPerturbationH0Inv`. Discharges PR-5 debt (b): `homotopyHamiltonian_one_compressed_eq_perturbedHamiltonian` is the compressed bridge `Ĥ_{s=1}(λ)|_K = Ĥ₀|_K + λV̂|_K`, by linearity of `configSectorCompress` alone; the sector-preservation of `V̂` it presupposed is proved separately, `liebPerturbationV_preserves_liebHalfFillingPred` (every hopping term of `V̂` moves one electron of a fixed spin between two sites, so total electron number and total spin-up number are unchanged). Discharges PR-5 debt (c): `configSector_liebHalfFillingPred_nonempty` (an explicit half-filled witness configuration), the instance hypothesis `tasaki_lemma_10_1_degenerate_perturbation` needs. **Crux**: `liebPerturbationV_intermediate_weight_eq_one` — Tasaki's "the site `x` is doubly occupied in `ĉ†_{x,σ}ĉ_{y,σ}\|Φ⟩`" (eq. (10.1.7), p. 344), read at this arc's `U = 1` normalisation as "the intermediate interaction weight is exactly `1`" (not merely `1/U`); essential use of the one-electron-per-site half-filling hypothesis, since a hard-core configuration with an empty site can absorb the hopping electron and stay hard-core (weight `0`). Combined with the diagonal reduced inverse, this gives `liebPerturbationH0InvCompressed_mul_V_mul_kernelProjection` (`Ĥ₀⁻¹\|_K` acts as the identity on `V̂\|_K · P̂₀`) and the **PR-6 capstone** `secondOrderEffectiveHamiltonian_liebPerturbation_eq`: the second-order effective Hamiltonian collapses to `−(P̂₀ · V̂\|_K · V̂\|_K · P̂₀)`, the object PR-8 computes explicitly as the superexchange sum. **Generic layer reused**: `liebPerturbationV_preserves_liebHalfFillingPred` supplies the entrywise sector-preservation hypothesis needed by the new generic compression homomorphism `configSectorCompress_mul_of_preserves` (`HubbardImpossibilityLowUVariationalCore.lean`, PR-6); the pre-existing number-sector instance `configSectorNumberCompress_mul_of_right_preserves` (`LiebAttractiveFullSectorSU2Algebra.lean`, Theorem 10.2 full-sector-lift arc) was refactored in this PR to derive from that same generic lemma rather than reprove it. **Debt.** Of PR-5's whole-Fock-space reduced-inverse pair this file consumes the *definition* `liebPerturbationH0Inv` — `liebPerturbationH0InvCompressed` is literally `configSectorCompress` applied to it — but not the *statement* `liebPerturbationH0_isReducedInverse`, which the compressed contract re-derives from the diagonal form rather than transporting; that theorem alone stays at reference 0, with its deletion debt unresolved (see PR-5's debt note above). `liebPerturbationV_preserves_liebHalfFillingPred` — **discharged**: PR-8b (`LiebRepulsiveSuperexchange.lean`) consumes it via `configSectorCompress_mul_of_preserves` to identify the compressed product `V̂\|_K · V̂\|_K` with the compression of `V̂ · V̂`. Still staged and unconsumed: `liebPerturbationH0Compressed_isReducedInverse`, `configSector_liebHalfFillingPred_nonempty` (for the Lemma 10.1 application at the arc assembly, PR-11 to PR-13); PR-13a has since consumed `homotopyHamiltonian_one_compressed_eq_perturbedHamiltonian` (`LiebRepulsiveEndpointIdentification.lean`), closing that debt. Each is carried as debt, not settled API; whatever that assembly does not consume is to be deleted, not kept. The layer's capstone `secondOrderEffectiveHamiltonian_liebPerturbation_eq` is the exception (a capstone is the endpoint of its layer): PR-8 computes it explicitly as the superexchange sum. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveSuperexchangeReducedInverse.lean` |
| `isReducedInverse_diagonal` | **The reduced inverse of a real diagonal matrix** (generic infrastructure for the Theorem 10.4 discharge arc, Issue #5320, PR #5327 PR-6; **PROVED**, axiom-free): for a diagonal matrix `diag(d)` with real (self-adjoint) diagonal entries, the diagonal matrix that inverts each nonzero `d i` and is `0` at each zero entry is a reduced inverse (`IsReducedInverse`) of `diag(d)`. Consumed by the arc's compressed reduced-inverse contract `liebPerturbationH0Compressed_isReducedInverse` (`Fermion/JordanWigner/Hubbard/LiebRepulsiveSuperexchangeReducedInverse.lean`), and it also replaces the prior bespoke whole-Fock-space proof of `liebPerturbationH0_isReducedInverse` (`Fermion/JordanWigner/Hubbard/LiebRepulsivePerturbationSetup.lean`) by an instance of this lemma. Not itself a direct formalization of a numbered Tasaki statement. | `Math/MatrixAnalysis/DegeneratePerturbation.lean` |
| `fermionHopReturn` / `fermionHopReturn_same_spin_eq` / `fermionHopReturn_opposite_spin_eq` / `sum_fermionMultiNumber_spinfulIndex_mul_eq` / `fermionSiteSpinPlus_mul_Minus_add_fermionSiteSpinMinus_mul_Plus_eq` / `fermionHopReturn_eq` | **The superexchange operator identity (Theorem 10.4 arc, Issue #5320, PR #5328 PR-7; PROVED, `#print axioms` = std3): a direct formalization of Tasaki's eq. (10.1.9) together with the two auxiliary identities stated just below it (`Σ_σ n̂_{x,σ}n̂_{y,σ} = 2(Ŝ^{(3)}_xŜ^{(3)}_y + ¼n̂_xn̂_y)` and `Ŝ⁺_yŜ⁻_x + Ŝ⁻_yŜ⁺_x = 2(Ŝ^{(1)}_xŜ^{(1)}_y + Ŝ^{(2)}_xŜ^{(2)}_y)`), §10.1, p. 344, and — unlike PR-1 through PR-6 — model-independent, with no dependence on the arc's half-filling sector, hard-core projection, or bipartite endpoint graph.** **Not** eq. (10.1.10) (`Ĥspin = Σ_{x,y}(|t_{x,y}|²/U_x)·2(Ŝ_x·Ŝ_y − ¼)P̂₀`, p. 345): that is a distinct statement needing the hopping weights, the kernel projection `P̂₀` and `n̂_xP̂₀ = P̂₀`, and it is PR-8's target. The capstone `fermionHopReturn_eq` proves, for any two distinct sites `x ≠ y` on the spinful Jordan–Wigner backbone, `Σ_{σ,τ} ĉ†_{y,τ}ĉ_{x,τ}ĉ†_{x,σ}ĉ_{y,σ} = n̂_y − 2Ŝ_x·Ŝ_y − ½n̂_xn̂_y` (`fermionHopReturn`, the round-trip hopping sum). Proved via CAR (canonical anticommutation relations) rather than Jordan–Wigner sign combinatorics: `fermionHopReturn_same_spin_eq` collapses the `σ = τ` diagonal to `n̂_{y,σ} − n̂_{x,σ}n̂_{y,σ}`; `fermionHopReturn_opposite_spin_eq` turns the `σ ≠ τ` off-diagonal into `−Ŝ^{±}_yŜ^{∓}_x`; `sum_fermionMultiNumber_spinfulIndex_mul_eq` and `fermionSiteSpinPlus_mul_Minus_add_fermionSiteSpinMinus_mul_Plus_eq` assemble the two halves into the stated spin-dot/number form. `x ≠ y` is essential (at `x = y` the identity is false). The ladder decomposition reuses the different-site commutation `fermionSiteSpinMinus_mul_Plus_comm` (`Ŝ⁻_xŜ⁺_y = Ŝ⁺_yŜ⁻_x` for `x ≠ y`), which this PR moved to the shared `Fermion/JordanWigner/Hubbard/FermionSiteSpin.lean` so that the §11.5 t-J bond-sum and this identity import one proof instead of two copies. Prepares the sandwiching with the kernel projection `P̂₀` that PR-8 (`LiebRepulsiveSuperexchange.lean`) performs on the half-filled hard-core sector. **Debt.** `fermionHopReturn_eq` — **discharged**: PR-8b (`LiebRepulsiveSuperexchange.lean`) consumes it directly (`fermionHopReturn_apply_eq_of_singly_occupied`'s half-filling diagonal collapse rewrites through it), sandwiching the identity between `P̂₀` on the half-filled hard-core sector to reach eq. (10.1.10). The four feeder lemmas (`fermionHopReturn_same_spin_eq`, `fermionHopReturn_opposite_spin_eq`, `sum_fermionMultiNumber_spinfulIndex_mul_eq`, `fermionSiteSpinPlus_mul_Minus_add_fermionSiteSpinMinus_mul_Plus_eq`) are each consumed inside this file, by `fermionHopReturn_eq` alone, and have no consumer outside it — a single-consumer chain for the arc assembly to judge, not decoration. Axiom-free. | `Fermion/JordanWigner/Hubbard/SuperexchangeOperatorIdentity.lean` |
| `sum_val_update_hop_site` / `hop_pair_apply_eq_zero_of_ne` / `liebPerturbationV_sq_apply_eq_of_singly_occupied` | **The hop-pair collapse for `V̂·V̂` (Theorem 10.4 arc, Issue #5320, PR #5329 PR-8a; PROVED, `#print axioms` = std3): a direct formalization of Tasaki's collapse argument at eq. (10.1.8), p. 344 — "one electron must hop from `x` to `y` to resolve the double occupancy so that the excited state returns to the subspace `H₀`" — not generic infrastructure.** On the singly-occupied (half-filled hard-core) sector, expanding `V̂·V̂ = Σ_{σ,i,j,τ,k,l} t_{ij}t_{kl}(ĉ†_{i,σ}ĉ_{j,σ})(ĉ†_{k,τ}ĉ_{l,τ})` and evaluating a matrix entry between two singly-occupied configurations collapses the double sum to the diagonal `i = l ∧ j = k` (`hop_pair_apply_eq_zero_of_ne`, via the per-site occupancy transport lemma `sum_val_update_hop_site`, replacing nested `Function.update` case splits): the right factor (`k ≠ l`) forces an intermediate configuration with site `k` doubly occupied and site `l` empty, and the left factor (`i ≠ j`) can then only reach a singly-occupied result by hopping out of the doubly-occupied site and into the emptied one. The **capstone** `liebPerturbationV_sq_apply_eq_of_singly_occupied` proves `(V̂·V̂) e c = Σ_{x,y} (t_{yx}·t_{xy})·fermionHopReturn N x y e c` on the whole Fock space: the surviving `(i,j,k,l) = (l,k,k,l)` term is literally the `(τ',σ') = (σ,τ)` summand of PR-7's `fermionHopReturn N k l`, no Jordan–Wigner sign computation needed (both sides carry the same operator, `jwSign` cancels by construction). The coefficient is left as the **asymmetric** product `t_{yx}·t_{xy}` — reducing it to the endpoint-graph indicator `t_{xy}²` needs the additional hopping-symmetry hypothesis `hT`, which is **not** required here and is deferred to PR-8b. Same-site (`i = j` or `k = l`) terms vanish upstream via the coefficient `liebEndpointHopping_diag_eq_zero` (not a filter on the sum). **Debt.** The capstone `liebPerturbationV_sq_apply_eq_of_singly_occupied` — **discharged**: PR-8b (`LiebRepulsiveSuperexchange.lean`) consumes it directly in the `hentry` step of its own capstone, reducing its right-hand side further via the coefficient reduction and the half-filling diagonal collapse and lifting the result to the compressed sector `K`. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveSuperexchangeCollapse.lean` |
| `liebEndpointGraph` / `liebEndpointGraph_adj` / `liebEndpointHopping_sq_eq_indicator` / `liebEndpointHopping_mul_symm_eq_indicator` / `fermionHopReturn_apply_eq_of_singly_occupied` / `liebPerturbationVCompressed_sq_eq_configSectorCompress` / `kernelProjection_mul_liebPerturbationVCompressed_sq_mul_kernelProjection` / `secondOrderEffectiveHamiltonian_liebPerturbation_eq_tJExchange` | **The superexchange capstone — Tasaki eq. (10.1.10), p. 345 (Theorem 10.4 arc, Issue #5320, PR #5330 PR-8b; PROVED, `#print axioms` = std3): the second and final file of the superexchange-identity sub-arc (PR-6 to PR-8), completing the identity PR-7's model-independent `fermionHopReturn_eq` and PR-8a's whole-Fock-space collapse `liebPerturbationV_sq_apply_eq_of_singly_occupied` set up.** `liebEndpointGraph A` is the complete bipartite graph on the bipartition `(A, Aᶜ)` (the arc's intentional endpoint-graph substitution for the book's literal bond set `{ {x,y} \| t_{x,y}≠0 }`, documented since PR-4), defined not as a fresh `SimpleGraph` but as the `Finset` specialisation of the sublattice-indicator graph `bipartiteGraphFromA` (`Quantum/MarshallLiebMattis/BipartiteGraph.lean`) at the indicator `fun x => decide (x ∈ A)`, with `liebEndpointGraph_adj` recovering the `Finset` adjacency `x ∈ A ↔ y ∉ A` this file's proofs use. The coefficient reduction `liebEndpointHopping_mul_symm_eq_indicator` turns PR-8a's surviving **asymmetric** product `t_{yx}·t_{xy}` into the endpoint-graph indicator `t_{xy}²`, under the additional symmetry hypothesis `hT : ∀ x y, T x y = T y x` — genuinely necessary (not decoration): without it an asymmetric `T` with `T x y > 0 > T y x` gives `t_{yx}t_{xy} = −1` against indicator `+1`. `fermionHopReturn_apply_eq_of_singly_occupied` collapses PR-7's `fermionHopReturn_eq` number-operator remainder `n̂_y − ½n̂_xn̂_y` to `½n̂_xn̂_y` on the half-filled hard-core sector (both `n̂_x`, `n̂_y` act with eigenvalue `1`), turning it into `2((1/4)n̂_xn̂_y − Ŝ_x·Ŝ_y)`. `liebPerturbationVCompressed_sq_eq_configSectorCompress` lifts `V̂\|_K·V̂\|_K` to the compression of the whole-Fock-space `V̂·V̂` via `configSectorCompress_mul_of_preserves` applied to PR-6's `liebPerturbationV_preserves_liebHalfFillingPred`. **Capstone** `kernelProjection_mul_liebPerturbationVCompressed_sq_mul_kernelProjection`: `P̂₀·V̂\|_K·V̂\|_K·P̂₀ = 2•(P̂₀·compress(tJExchange(liebEndpointGraph A))·P̂₀)`. **Corollary for PR-9/PR-10** `secondOrderEffectiveHamiltonian_liebPerturbation_eq_tJExchange`, composing with PR-6's `secondOrderEffectiveHamiltonian_liebPerturbation_eq`: `Ĥeff = −2•(P̂₀·compress(tJExchange(liebEndpointGraph A))·P̂₀)`, i.e. Tasaki's `Ĥspin = Σ_{x,y}(\|t_{x,y}\|²/U_x)·2(Ŝ_x·Ŝ_y−¼)P̂₀` at this arc's `U=1` normalisation and on the complete-bipartite endpoint graph. This closes the superexchange sub-arc (PR-6 reduced-inverse layer, PR-7 model-independent identity, PR-8a whole-Fock-space collapse, PR-8b endpoint-graph capstone); nothing here yet depends on the sub-arc's own debt (all cleared above), and the capstone is consumed inside this file by the corollary (as the four feeder lemmas, `liebEndpointGraph` and its adjacency lemma `liebEndpointGraph_adj` are consumed by the capstone chain), leaving `secondOrderEffectiveHamiltonian_liebPerturbation_eq_tJExchange` as the file's only declaration at reference 0; PR-9a (Issue #5320, PR #5331) consumed it and resolved this debt (see the PR-9a row below). | `Fermion/JordanWigner/Hubbard/LiebRepulsiveSuperexchange.lean` |
| `liebHardCoreHalfFillingPred` / `liebHardCoreDownOccupation` / `liebHardCoreDownOccupation_magSumS_eq` / `liebHardCoreToMagConfigS` / `liebHardCoreOfMagConfigSFock` / `liebHardCoreOfMagConfigSFock_apply_up` / `liebHardCoreOfMagConfigSFock_apply_down` / `liebHardCoreOfMagConfigSFock_mem_pred` / `liebHardCoreOfMagConfigS` / `liebHardCoreToMagConfigS_apply_eq_zero_iff_up_occupied` / `singlyOccupied_eq_iff_downOccupation` / `liebHardCoreOfMagConfigS_liebHardCoreToMagConfigS` / `liebHardCoreToMagConfigS_liebHardCoreOfMagConfigS` / `liebHardCoreHalfFillingSectorEquivS` / `liebHardCoreSiteState` / `liebHardCoreSiteState_ne_zero` / `tJConfigOf_liebHardCoreSiteState_eq` / `fermionSiteNumber_mul_apply_eq_ite_of_singlyOccupied` / `fermionSpinDot_apply_eq_spinSDot_of_singlyOccupied` / `liebHardCoreToAmbientSector` / `kernelProjection_mul_liebPerturbationVCompressed_sq_mul_kernelProjection_reindex_eq` / `secondOrderEffectiveHamiltonian_liebPerturbation_reindex_eq_heisenbergOnMagSector` | **The Fermion–Spin bridge, first half — sector `Equiv` + Hamiltonian reindexing (Theorem 10.4 arc, Issue #5320, PR #5331 PR-9a; PROVED, `#print axioms` = std3): the first of the two-PR 9a/9b split of "PR-9: Fermion-Spin bridge", bridging the half-filled hard-core Fock sector to a spin-`1/2` magnetization sector so that PR-8b's superexchange capstone can feed Theorem 2.3 in PR-10.** `liebHardCoreHalfFillingPred` strengthens `liebHalfFillingPred` (double occupancy excluded, `LiebRepulsivePerturbationSetup.lean`) with single occupancy at every site, since `configSector N (liebHalfFillingPred N nUp)` alone is not in bijection with `magConfigS`. **The sector `Equiv`** `liebHardCoreHalfFillingSectorEquivS` (`M = N + 1 − nUp`, the **down**-count, matching `magSumS`/`spinSOp3`'s `0`=up/`1`=down convention): forward `liebHardCoreToMagConfigS` reads off the down-orbital occupation, backward `liebHardCoreOfMagConfigS` sets `c(spinfulIndex x 0) = 1 − σ x`, needing `nUp ≤ N + 1` for the truncated-subtraction round-trip; `liebHardCoreToMagConfigS_apply_eq_zero_iff_up_occupied` pins the up/down reading against a silent flip; `singlyOccupied_eq_iff_downOccupation` (the injectivity core of the forward map: a singly occupied configuration is already determined by its down-orbital occupation, the up orbital being the complement) is public rather than `private` because PR-9b's diagonal step consumes it across the module boundary. The **t-J reuse route** (`liebHardCoreSiteState`, `tJConfigOf_liebHardCoreSiteState_eq`) lets the crux **entrywise identity** `fermionSpinDot_apply_eq_spinSDot_of_singlyOccupied` reuse the already-proved sign-free t-J bond action (`tJExchangeBond_mulVec_tJConfigOf_full`, `TJHalfFillingBondAction.lean`) instead of re-deriving Jordan–Wigner sign combinatorics. **Capstones**: the **raw form** `kernelProjection_mul_liebPerturbationVCompressed_sq_mul_kernelProjection_reindex_eq` lands on the constant `\|A\|(N+1−\|A\|)` **minus** the antiferromagnetic Heisenberg matrix `heisenbergHamiltonianSMatrixOnMagSector ((2:ℂ) • bipartiteCoupling (fun x => decide (x ∈ A))) 1 (N+1−nUp)` (**constant − Heisenberg**), while its PR-8b `Ĥeff`-corollary reindex `secondOrderEffectiveHamiltonian_liebPerturbation_reindex_eq_heisenbergOnMagSector` lands on the *same* Heisenberg matrix **minus** the *same* constant (**Heisenberg − constant**, the opposite order) — exactly the shape PR-10 needs for Theorem 2.3. **The constant shift is `\|A\|(N+1−\|A\|)` (no `/2`)**, for both capstones: the `tJExchange`-level constant `κ = \|A\|(N+1−\|A\|)/2` is itself multiplied by the same factor `2` that the `Ĥeff` identity `P̂₀V̂V̂P̂₀ = 2•(P̂₀·tJExchange·P̂₀)` uses to scale the bond coupling to `(2:ℂ) • bipartiteCoupling`, giving the surviving shift `2κ = (A.card : ℂ) * (N+1−A.card : ℂ)` (no division). **Debt discharged**: this PR consumes PR-8b's sole reference-0 declaration `secondOrderEffectiveHamiltonian_liebPerturbation_eq_tJExchange`, closing that debt. **Debt**: PR-11a has since consumed the `Ĥeff` corollary `secondOrderEffectiveHamiltonian_liebPerturbation_reindex_eq_heisenbergOnMagSector`, closing that debt; the raw capstone `kernelProjection_mul_liebPerturbationVCompressed_sq_mul_kernelProjection_reindex_eq` is consumed inside the file by that corollary. The convention-guard lemma `liebHardCoreToMagConfigS_apply_eq_zero_iff_up_occupied` remains at reference 0 deliberately, as a single visible pinning statement, not decoration. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveFermionSpinBridge.lean` |
| `fermionTotalSpinSquared_eq_sum_fermionSpinDot` / `fermionSpinDot_self_eq` / `fermionTotalSpinSquared_apply_eq_totalSpinSSquared_of_singlyOccupied` / `fermionTotalSpinSquared_reindex_eq_totalSpinSSquaredOnMagSector` | **The Fermion–Spin bridge, second half — the ladder-vs-Cartesian Casimir bridge (Theorem 10.4 arc, Issue #5320, PR #5332 PR-9b; PROVED, `#print axioms` = std3): the second of the two-PR 9a/9b split of "PR-9: Fermion-Spin bridge", completing the Fermion–Spin bridge.** PR-9a's module docstring flagged as separate work the comparison of `fermionTotalSpinSquared` with `totalSpinSSquared`. **What the two Casimirs are, and what is *not* claimed of them**: `fermionTotalSpinSquared` (ladder form `Ŝ⁻_totŜ⁺_tot + Ŝ³_tot(Ŝ³_tot+1)`) is **not** a PR-9a declaration — it is the §11.1.1 saturated-ferromagnetism operator of `SaturatedFerromagnetism.lean`, acting on the whole Hubbard Fock space `Fin (2N+2) → Fin 2` — while `totalSpinSSquared` (the spin-`1/2` Cartesian Casimir `(Ŝ¹)²+(Ŝ²)²+(Ŝ³)²`, `Quantum/SpinS/TotalSquaredCore.lean`) acts on spin configurations `Fin (N+1) → Fin 2`. They are therefore **not operators on a common space**, and no identity between them *as operators* is even well-typed; the content of this PR is the **entrywise** correspondence on the hard-core half-filled sector, each fermionic matrix entry between two hard-core configurations equalling the Cartesian entry between their images under PR-9a's sector `Equiv` (`liebHardCoreHalfFillingSectorEquivS`). The restriction to that sector is essential rather than a convenience: off it the fermionic Casimir also sees empty and doubly occupied sites, whose same-site dot vanishes instead of contributing the spin-`1/2` value `3/4`. **Route**: both Casimirs are re-expressed as the *same* double sum of two-site dots `Σ_{x,y} Ŝ_x·Ŝ_y` — `fermionTotalSpinSquared_eq_sum_fermionSpinDot` for the fermionic ladder form (via the SU(2) ladder commutator `Ŝ⁺_totŜ⁻_tot − Ŝ⁻_totŜ⁺_tot = 2Ŝ³_tot`), matching the pre-existing spin-side expansion `totalSpinSSquared_eq_sum_spinSDot`. The off-diagonal terms of the double sum are PR-9a's crux `fermionSpinDot_apply_eq_spinSDot_of_singlyOccupied` verbatim; the new same-site diagonal term is computed in closed form by `fermionSpinDot_self_eq`, `Ŝ_x·Ŝ_x = (3/4)(n̂_{x↑}+n̂_{x↓}−2n̂_{x↑}n̂_{x↓})` (from the CAR `ĉĉ† = 1−n̂` and idempotence `n̂²=n̂`), which collapses to the scalar `3/4` on a hard-core configuration, matching the spin-`1/2` value `S(S+1)=3/4` (`spinSDot_self`). **Crux** `fermionTotalSpinSquared_apply_eq_totalSpinSSquared_of_singlyOccupied`: the entrywise identity on hard-core half-filled bra/ket Fock configurations, stated directly on Fock-space matrix entries (no `submatrix` plumbing), the same shape as PR-9a's crux. **Capstone** `fermionTotalSpinSquared_reindex_eq_totalSpinSSquaredOnMagSector`: packages the crux into the `submatrix`-along-the-sector-`Equiv` form matching PR-9a's capstone shape, restricting `fermionTotalSpinSquared N` to the hard-core sub-sector and reindexing onto `totalSpinSSquared (Fin (N+1)) 1` on the magnetization sector — the shape needed to transport the Casimir eigenvalue from Theorem 2.3 back to the fermionic ground states in the later PR-11/PR-12 assembly steps. **This closes the Fermion–Spin bridge arc (PR-9a sector `Equiv` + Hamiltonian reindexing, PR-9b Casimir bridge)**; also non-private'd `singlyOccupied_eq_iff_downOccupation` (`LiebRepulsiveFermionSpinBridge.lean`, PR-9a) to reuse it across the module boundary instead of duplicating it, and put the per-site spin-z sum `fermionTotalSpinZ_eq_sum_fermionSiteSpinZ` next to its `Plus`/`Minus` siblings in `FermionSiteSpin.lean` rather than here, that being the file that owns the per-site/total correspondence. **Debt discharged**: the capstone `fermionTotalSpinSquared_reindex_eq_totalSpinSSquaredOnMagSector` was this file's **only** reference-0 declaration at the time of this PR — the crux is consumed by the capstone, `fermionSpinDot_self_eq` by the crux's diagonal step (through the private `fermionSpinDot_self_mulVec_basisVec_of_singlyOccupied` / `fermionSpinDot_self_apply_of_singlyOccupied` chain), and the expansion `fermionTotalSpinSquared_eq_sum_fermionSpinDot` by the crux, so no feeder lemma was left unconsumed; PR-10b (`LiebRepulsiveTheorem23Instance.lean`) has since consumed the capstone itself, closing that debt. PR-9a's own debt is unchanged by this PR: its `Ĥeff` corollary remained staged until PR-11a consumed it (see the PR-11a entry below). Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveFermionSpinCasimirBridge.lean` |
| `coordinateSpan` / `coordinateRestrict` / `coordinateExtend` / `mem_coordinateSpan_iff` / `isUniqueGroundStateOn_coordinateSpan_iff_submatrix` / `matrixKernel_diagonal_eq_coordinateSpan` | **Coordinate-block ground-state transport (generic infrastructure for the Theorem 10.4 discharge arc, Issue #5320, PR #5333 PR-10a; PROVED, `#print axioms` = std3): not itself a direct formalization of a numbered Tasaki statement — generic model-independent transport layer preparing the arc's assembly steps (PR-11 onward).** `coordinateSpan P` is the subspace of `EuclideanSpace ℂ n` spanned by the standard basis vectors at indices satisfying a decidable predicate `P`, with `mem_coordinateSpan_iff` characterizing it as exactly the vectors vanishing outside `P`; `coordinateRestrict`/`coordinateExtend` are the restriction to, and zero-extension from, the subtype `{i // P i}`, mutually inverse on the coordinate span, homogeneous, and norm-preserving (`norm_coordinateExtend`). **Capstone 1** `isUniqueGroundStateOn_coordinateSpan_iff_submatrix`: for a matrix `H` supported on the coordinate block of `P` (`H = P̂·H·P̂`, `P̂` the diagonal indicator of `P`) and a candidate `φ ∈ coordinateSpan P`, `H` having `φ` as its unique ground state on `coordinateSpan P` is equivalent to the submatrix restriction `H.submatrix Subtype.val Subtype.val` having the restricted candidate as its unique ground state on the whole restricted space `⊤` — the `φ ∈ coordinateSpan P` hypothesis is essential (a candidate with nonzero coordinates outside the block restricts to a normalized submatrix ground state while failing both membership and normalization on the big space, so the `←` direction is false without it). **Generalized in PR-13a**: the block-support hypothesis `H = P̂·H·P̂` was weakened in place to the invariance hypothesis `hInv : ∀ i j, P j → ¬ P i → H i j = 0` (rows outside `P` vanish at columns inside `P`), which also covers a Hamiltonian merely block-*diagonal* across `P`/`¬P`; block support implies it via the new `blockSupport_apply_eq_zero`, so every existing call site (`LiebRepulsiveSectorAssembly.lean`) supplies `hInv` in one line without change of shape. **Debt (new)**: the generalization's actual payoff — covering a Hamiltonian that is block-diagonal across `P`/`¬P` but *not* block-supported (`hInv` holds while `H = P̂·H·P̂` fails) — has no consumer yet; every current call site still happens to be block-supported, so this coverage is staged for a later PR. **Capstone 2** `matrixKernel_diagonal_eq_coordinateSpan`: the kernel of a diagonal matrix is the coordinate span of its vanishing locus. This is the same block-support/kernel-identification pattern used throughout the arc's degenerate-perturbation layer (`Math/MatrixAnalysis/DegeneratePerturbation.lean`, e.g. `kernelProjectionMatrix_diagonal`) generalized from an ambient-space projection to an arbitrary coordinate block, so a model only has to supply the block-support hypothesis or the diagonal vanishing-locus characterization instead of reproving ground-state uniqueness or a kernel computation per model. PR-11a has since consumed both capstones — `isUniqueGroundStateOn_coordinateSpan_iff_submatrix` directly in `LiebRepulsiveSectorAssembly.lean`, and `matrixKernel_diagonal_eq_coordinateSpan` via `LiebRepulsivePerturbationSetup.lean`'s `matrixKernel_liebPerturbationH0Compressed_eq_coordinateSpan` — closing that debt. **PR-13b adds** `apply_eq_zero_of_mapsTo_coordinateSpan` — the converse direction, reading `hInv` off a matrix that merely maps `coordinateSpan P` into itself (the route by which a model that only knows commutation with the block-cutting charges supplies `hInv`), used by `LiebRepulsiveCasimirPinning.lean`'s block-invariance adapter — and promotes `coordinateRestrict_toEuclideanLin` from `private` to public for its own compressed-to-full transport. Axiom-free. | `Math/MatrixAnalysis/BlockTransport.lean` |
| `liebOrientedSublattice` / `liebOrientedSublattice_horient` / `liebOrientedSublattice_sublatticeImbalance_eq` / `liebOrientedSublattice_bipartiteCoupling_eq` / `liebRepulsive_theorem23_instance` / `liebRepulsiveSpinCasimir_eq_tasaki23PredictedCasimirValue` / `liebRepulsive_groundState_casimir_eq_predicted` | **Theorem 2.3 instance at the superexchange coupling, and its Casimir transport onto the fermionic sector (Theorem 10.4 discharge arc, Issue #5320, PR #5334 PR-10b; PROVED, `#print axioms` = std3).** `liebOrientedSublattice A` is the canonically oriented sublattice (`A` itself if `\|Aᶜ\| ≤ \|A\|` already, else `Aᶜ`), needed because Theorem 2.3 (`tasaki_2_5_theorem_2_3_of_bipartiteCompletePositive`, `Quantum/SpinS/Theorem23StructuralGeneralFinal.lean`) is stated under the canonical orientation `\|¬A\| ≤ \|A\|`; the swap leaves both the sublattice imbalance (`liebOrientedSublattice_sublatticeImbalance_eq`) and the bipartite coupling (`liebOrientedSublattice_bipartiteCoupling_eq`, entrywise, since `bipartiteCoupling` only detects whether two sites lie on *different* sublattices) unchanged. **Capstone 1** `liebRepulsive_theorem23_instance`: for a nondegenerate bipartition (`1 ≤ \|A\|`, `1 ≤ \|Aᶜ\|`; the fully polarised endpoints `A = ∅`/`Finset.univ` are excluded, needing instead the direct fully polarised ground state `S₀ = (N+1)/2`), the full Marshall–Lieb–Mattis statement `tasaki_2_5_theorem_2_3` holds at spin `S = 1/2` (`N_spin = 1`) for the superexchange coupling `J = (2 : ℂ) • bipartiteCoupling A'` of the oriented sublattice `A'`, at every diagonal shift the caller supplies. **Capstone 2** `liebRepulsiveSpinCasimir_eq_tasaki23PredictedCasimirValue`: the predicted total-spin Casimir value of Theorem 2.3 at spin `1/2` on the oriented sublattice is `liebRepulsiveSpinCasimir A = S₀(S₀+1)` at `S₀ = \|\|A\|-\|Aᶜ\|\|/2`, Tasaki Theorem 10.4's target total spin. **Capstone 3** `liebRepulsive_groundState_casimir_eq_predicted`: transports the whole Marshall-positive ground state of Capstone 1, not just its Casimir eigenvalue, onto the fermionic hard-core half-filled sector — for an admissible magnetization sector, an energy `μ` and a nonzero vector `c` there such that, along PR-9a's sector bijection, (i) `c` is entrywise strictly Marshall-positive against the oriented sublattice's Marshall sign, (ii) `c` is a `μ`-eigenvector of the superexchange Heisenberg matrix `heisenbergHamiltonianSMatrixOnMagSector ((2 : ℂ) • bipartiteCoupling A) 1 (N+1−nUp)` compressed onto that sector — the very matrix PR-9a's `Ĥeff` corollary identifies with the second-order effective Hamiltonian up to the constant `\|A\|(N+1−\|A\|)`, whence the coupling is stated at the *unoriented* `A` (consuming the orientation adapter) — (iii) `μ` is minimal among that compressed matrix's eigenvalues, so `c` is one of its ground states, and (iv) the fermionic total-spin Casimir `fermionTotalSpinSquared N` acts on `c` as the scalar `liebRepulsiveSpinCasimir A`, read on the fermionic side along PR-9b's sector bijection `fermionTotalSpinSquared_reindex_eq_totalSpinSSquaredOnMagSector` (`LiebRepulsiveFermionSpinCasimirBridge.lean`), discharging that PR-9b debt. Conjuncts (ii)/(iii) transport through `heisenbergHamiltonianSMatrixOnMagSector_mulVec_magSectorRestriction` (down to the magnetization sector) and `heisenbergHamiltonianS_mulVec_magSectorEmbedding` (back up, for the minimality's competitor eigenvectors), so PR-11/PR-12 need not re-derive the ground-state property from Theorem 2.3. **Debt.** One declaration is at reference 0: the file's own final capstone `liebRepulsive_groundState_casimir_eq_predicted`, staged for the arc's remaining assembly (PR-11/PR-12). PR-9a's `Ĥeff` corollary `secondOrderEffectiveHamiltonian_liebPerturbation_reindex_eq_heisenbergOnMagSector` and PR-10a's `coordinateSpan` capstones have since been consumed by PR-11a (see the PR-11a row below); this file's own capstone remains unconsumed. **PR-13b has since consumed** `liebRepulsive_groundState_casimir_eq_predicted` (via `casimirValue_eq_liebRepulsiveSpinCasimir_of_effectiveGroundState`, `LiebRepulsiveCasimirPinning.lean`), closing that debt. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveTheorem23Instance.lean` |
| `isUniqueGroundStateOn_reindex_iff` / `isUniqueGroundStateOn_sub_smul_one_iff` / `isUniqueGroundStateOn_of_finrank_eigenspace_le_one` / `secondOrderEffectiveHamiltonian_eq_kernelProjectionMatrix_conj` / `eigenspace_diagonal_eq_coordinateSpan` / `IsUniqueGroundStateOn.mono` / `matrixKernel_liebPerturbationH0Compressed_eq_coordinateSpan` / `liebHalfFillingHardcorePred` / `liebHardCoreAmbientSubtypeEquiv` / `liebHardCoreAmbientSubtypeEquiv_val` / `isUniqueGroundStateOn_liebPerturbationH0Compressed_kernel_iff_heisenberg` | **Generic ground-state transport layer and the PR-11a sector-assembly capstone (Theorem 10.4 discharge arc, Issue #5320, PR #5335 PR-11a; PROVED, `#print axioms` = std3): assembles PR-9a's Fermion-Spin bridge and PR-10a's coordinate-block transport into a single pure iff between the fermionic effective Hamiltonian and the superexchange Heisenberg matrix.** Generic layer (`Math/MatrixAnalysis/SubmatrixGroundState.lean`) supplies further model-independent facts about `IsUniqueGroundStateOn` beyond PR-10a's `BlockTransport.lean`: `isUniqueGroundStateOn_reindex_iff` transports it along an index `Equiv`; `isUniqueGroundStateOn_sub_smul_one_iff` transports it along a real constant shift of the Hamiltonian; `isUniqueGroundStateOn_of_finrank_eigenspace_le_one` promotes an eigenspace `finrank ≤ 1` bound plus eigenvalue-minimality to a genuine unique-ground-state witness (staged for the not-yet-written existence step, PR-11b onward); plus the eigenspace analogue of `matrixKernel_diagonal_eq_coordinateSpan` (`eigenspace_diagonal_eq_coordinateSpan`), the block-diagonality of the second-order effective Hamiltonian under the kernel projection (`secondOrderEffectiveHamiltonian_eq_kernelProjectionMatrix_conj`), and the downward-restriction lemma `IsUniqueGroundStateOn.mono` along a submodule inclusion. Model-specific layer: `matrixKernel_liebPerturbationH0Compressed_eq_coordinateSpan` (`LiebRepulsivePerturbationSetup.lean`) identifies `ker(Ĥ₀|_K)` with the coordinate span of the hard-core sector predicate `liebHalfFillingHardcorePred`, the sector-level analogue PR-9a's block transport needs; the new file `LiebRepulsiveSectorAssembly.lean` supplies the hard-core/ambient sector `Equiv` `liebHardCoreAmbientSubtypeEquiv` (bijecting the hard-core half-filled configuration sector with the hard-core-predicate subtype of the ambient half-filled sector, both round trips `rfl`) and its Fock-level compatibility lemma `liebHardCoreAmbientSubtypeEquiv_val` with PR-9a's `liebHardCoreToAmbientSector`. **Capstone** `isUniqueGroundStateOn_liebPerturbationH0Compressed_kernel_iff_heisenberg`: a pure iff, with no Theorem 2.3 input — for a candidate `Φ ∈ ker(Ĥ₀|_K)`, `Ĥeff` has a unique ground state on `ker(Ĥ₀|_K)` at energy `E` and candidate `Φ` iff the (constant-shifted, shift `\|A\|(N+1−\|A\|)`) antiferromagnetic Heisenberg matrix on the magnetization-`(N+1−nUp)` sector has a unique ground state on `⊤` at the same energy and the correspondingly reindexed candidate. Chains `BlockTransport.lean`'s `isUniqueGroundStateOn_coordinateSpan_iff_submatrix` with `SubmatrixGroundState.lean`'s `isUniqueGroundStateOn_reindex_iff` along the composite `Equiv` `magConfigS ≃ {s // liebHalfFillingHardcorePred N nUp s}` (`(liebHardCoreHalfFillingSectorEquivS N nUp hnUp).symm.trans (liebHardCoreAmbientSubtypeEquiv N nUp)`); the composite cancels PR-9a's own `liebHardCoreHalfFillingSectorEquivS`/`.symm` round trip via `liebHardCoreAmbientSubtypeEquiv_val`, so the target Heisenberg matrix carries no residual `submatrix`. The candidate's kernel-membership hypothesis is essential (without it a nonzero out-of-block component destroys the left-hand side while leaving the right-hand side, which only sees `coordinateRestrict`, intact). **This advances the arc's assembly infrastructure (PR-11b/PR-11c remain)**: the Theorem 2.3 instance (PR-10b) and the ground-state existence witness remain to be supplied by PR-11b; PR-11b is limited to producing the existence witness and applying Lemma 10.1, and axiom removal itself is PR-14's responsibility — the site-dependent-`U` homotopy (PR-12b) and full assembly (PR-13, expected to split into PR-13a/PR-13b) also remain before Theorem 10.4 is discharged (Issue #5320); the three-way sector bridge between `configSector`, `numberSpinZSectorEuclidean`, and `spinZSectorEuclidean` is separate out-of-scope work left for PR-11c. **Debt (new)**: six declarations introduced by this PR are at reference 0 — the capstone `isUniqueGroundStateOn_liebPerturbationH0Compressed_kernel_iff_heisenberg` itself (staged for PR-11b's consumption), `isUniqueGroundStateOn_sub_smul_one_iff` and `eigenspace_diagonal_eq_coordinateSpan` (staged for PR-11b's existence-witness step), `IsUniqueGroundStateOn.mono` (staged for PR-11b/PR-11c's submodule transport), `isUniqueGroundStateOn_of_finrank_eigenspace_le_one` (staged for the not-yet-written existence step, PR-11b onward, as noted above), and `HubbardImpossibilityLowUVariationalCore.lean`'s `configSectorCompress_eq_submatrix` (this PR's Hubbard-side sector kernel bridge), likewise staged for PR-11b/PR-11c consumption. Also non-private'd `hubbardConfigInteractionWeight_one_eq_zero_of_singlyOccupied` (`LiebRepulsiveFermionSpinBridge.lean`, PR-9a) to reuse it across the module boundary instead of duplicating it. Axiom-free. PR-11b has since consumed the capstone `isUniqueGroundStateOn_liebPerturbationH0Compressed_kernel_iff_heisenberg`, `isUniqueGroundStateOn_sub_smul_one_iff`, and `isUniqueGroundStateOn_of_finrank_eigenspace_le_one`, closing that debt; `eigenspace_diagonal_eq_coordinateSpan`, `IsUniqueGroundStateOn.mono`, and `configSectorCompress_eq_submatrix` remain unconsumed (staged for PR-11c/PR-12 onward). PR-11c has since consumed `eigenspace_diagonal_eq_coordinateSpan` and `IsUniqueGroundStateOn.mono`, closing those debts (see the PR-11c row below); `configSectorCompress_eq_submatrix` was **deleted in PR-12a** (a fourth staging round with no consumer is not an option — decorative declaration per `lean-coding-conventions`'s "capstone 以外の参照 0 は装飾宣言" discipline; see the PR-12a row below). **PR-13b factors out** `toEuclideanLin_submatrix_equiv_apply` from `isUniqueGroundStateOn_reindex_iff`'s proof as a standalone public lemma (the reindexing eigen-equation intertwiner), reused directly in the Casimir pinning capstone's hard-core reindexing step (`LiebRepulsiveCasimirPinning.lean`). | `Math/MatrixAnalysis/SubmatrixGroundState.lean` + `Fermion/JordanWigner/Hubbard/LiebRepulsivePerturbationSetup.lean` + `Fermion/JordanWigner/Hubbard/LiebRepulsiveSectorAssembly.lean` + `Fermion/JordanWigner/Hubbard/HubbardImpossibilityLowUVariationalCore.lean` |
| `liebOrientedSublattice_theorem23_side_conditions` / `liebRepulsive_isUniqueGroundStateOn_heisenbergOnMagSector` / `tasaki_lemma_10_1_liebRepulsive_apply` | **The uniqueness supply and the Lemma 10.1 application (Theorem 10.4 discharge arc, Issue #5320, PR #5336 PR-11b; PROVED, `#print axioms` = std3): supplies the two pieces PR-11b owns, closing PR-11a's staged existence-witness builders; PR-10b's own capstone `liebRepulsive_groundState_casimir_eq_predicted` remains at reference 0 (staged for PR-12/PR-13's assembly) — this PR only shares its four inline side conditions, not the capstone itself.** `liebOrientedSublattice_theorem23_side_conditions` factors the four nondegeneracy side conditions (`1 ≤ |A'|`, `1 ≤ |A'ᶜ|`, and the bare `true`/`false` witnesses) that PR-10b's `liebRepulsive_groundState_casimir_eq_predicted` derived inline from `liebOrientedSublattice_card_pos`, now shared with this file's own capstone (`liebSublattice_filter_true`/`liebSublattice_filter_false`, PR-10b; non-`private` because both are used at several call sites within `LiebRepulsiveTheorem23Instance.lean` itself, not because of any consumer outside the module). **Capstone 1** `liebRepulsive_isUniqueGroundStateOn_heisenbergOnMagSector`: combines PR-10b's `liebRepulsive_theorem23_instance` (a Marshall-positive sector eigenvector plus energy minimality, not by itself a genuine `IsUniqueGroundStateOn`) with the Perron–Frobenius `finrank ≤ 1` route (`heisenbergHamiltonianSMatrixOnMagSector_finrank_le_one_of_marshall_positive`, `Quantum/SpinS/Theorem24SectorPFFromTheorem23.lean`) and PR-11a's generic builders `isUniqueGroundStateOn_of_finrank_eigenspace_le_one` / `isUniqueGroundStateOn_sub_smul_one_iff` (`Math/MatrixAnalysis/SubmatrixGroundState.lean`) to promote it to a genuine `IsUniqueGroundStateOn ⊤ (heisenbergHamiltonianSMatrixOnMagSector … − shift • 1) E Φ` — the exact right-hand side of PR-11a's assembly capstone `isUniqueGroundStateOn_liebPerturbationH0Compressed_kernel_iff_heisenberg`. **Capstone 2** `tasaki_lemma_10_1_liebRepulsive_apply`: transports Capstone 1's witness back along PR-11a's `iff` onto `ker (Ĥ₀|_K)` to supply `tasaki_lemma_10_1_degenerate_perturbation`'s (`Math/MatrixAnalysis/DegeneratePerturbationConvergence.lean`, discharged independently in issue #5313) `hEffGS` hypothesis — every other hypothesis of that lemma was already discharged by PR-5/PR-6 assets (`liebPerturbationH0Compressed_posSemidef`, `liebPerturbationVCompressed_isHermitian`, `liebPerturbationH0Compressed_isReducedInverse`, `kernelProjection_mul_liebPerturbationVCompressed_mul_kernelProjection`) — giving the perturbed Hamiltonian `Ĥ₀c + λV̂c` a unique ground state for every sufficiently small `λ > 0`, converging to an effective ground state `Φeff` as `λ → 0⁺`. The full export accordingly packages, alongside the `λ`-family uniqueness and the convergence `Philam λ → Φeff`, `tasaki_lemma_10_1_degenerate_perturbation`'s own conclusion transported through `hEffGS`: `∃ Eeff, IsUniqueGroundStateOn (matrixKernel (liebPerturbationH0Compressed N nUp)) (secondOrderEffectiveHamiltonian (liebPerturbationH0Compressed N nUp) (liebPerturbationVCompressed N nUp A T) (liebPerturbationH0InvCompressed N nUp)) Eeff Φeff`, i.e. a genuine unique-ground-state statement for the second-order effective Hamiltonian on `ker Ĥ₀c`, not merely the existence of `Φeff` as a limit point. **Debt.** This PR consumes and closes PR-11a's `isUniqueGroundStateOn_liebPerturbationH0Compressed_kernel_iff_heisenberg`, `isUniqueGroundStateOn_sub_smul_one_iff`, and `isUniqueGroundStateOn_of_finrank_eigenspace_le_one`. PR-10b's sole reference-0 declaration `liebRepulsive_groundState_casimir_eq_predicted` is **not** closed by this PR: Capstone 1 calls `liebRepulsive_theorem23_instance` directly, and only shares the side-conditions lemma feeding that capstone's proof; it remains staged for PR-12/PR-13's assembly. `eigenspace_diagonal_eq_coordinateSpan`, `IsUniqueGroundStateOn.mono`, and `configSectorCompress_eq_submatrix` (all PR-11a) remain staged, unconsumed until PR-11c/PR-12 onward — PR-11c has since consumed the first two, closing those debts; `configSectorCompress_eq_submatrix` was **deleted in PR-12a** as a decorative (reference-0) declaration rather than staged a fourth time (see the PR-12a row below). This PR's own final capstone `tasaki_lemma_10_1_liebRepulsive_apply` stays at reference 0, staged for the arc's remaining assembly (PR-12/PR-13); axiom removal itself is PR-14's responsibility. **PR-13b has since consumed** `tasaki_lemma_10_1_liebRepulsive_apply` (in both the λ-family transport capstone `symmetricHomotopyHamiltonian_one_isUniqueGroundStateOn` and the Casimir-pinning capstone, `LiebRepulsiveCasimirPinning.lean`), closing that debt. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveUniquenessAssembly.lean` (+ `Fermion/JordanWigner/Hubbard/LiebRepulsiveTheorem23Instance.lean`) |
| `liebHalfFillingSpinZVal` / `numberSpinZSectorEuclidean_eq_coordinateSpan_liebHalfFillingPred` / `numberSpinZSectorEuclidean_le_spinZSectorEuclidean` / `repulsiveSpinZSector_ground_unique_on_numberSpinZSector` / `liebRepulsive_exists_unique_casimir_sector` / `coordinateSpan_inf_coordinateSpan` | **The three-way sector bridge, and the PR-3 debt discharge (Theorem 10.4 discharge arc, Issue #5320, PR #5337 PR-11c; PROVED, `#print axioms` = std3): connects the three distinct descriptions of the half-filled fixed-`Ŝ³` sector in play across the arc — `numberSpinZSectorEuclidean` (PR-3), `spinZSectorEuclidean` (PR-1), and `coordinateSpan (liebHalfFillingPred N nUp)` (PR-5) — and discharges a debt open since PR-3.** `liebHalfFillingSpinZVal N nUp = (2 nUp − (N+1))/2` pins the `nUp`-parameterized spin-`z` eigenvalue as its own named quantity, avoiding the cast-inversion trap of recovering `nUp` from a complex `m₀`. **The diagonal-charge bridge** `numberSpinZSectorEuclidean_eq_coordinateSpan_liebHalfFillingPred`: since both `N̂` and `Ŝ³` are diagonal in the configuration basis (`fermionTotalNumber_eq_diagonal`, `fermionTotalSpinZ_eq_diagonal`), the joint sector `numberSpinZSectorEuclidean N (N+1) m₀` equals `coordinateSpan (liebHalfFillingPred N nUp)` at the matching parameter — PR-11a's `eigenspace_diagonal_eq_coordinateSpan` applied to each factor, composed via the new generic lemma `coordinateSpan_inf_coordinateSpan` (`Math/MatrixAnalysis/SubmatrixGroundState.lean`: an intersection of coordinate spans is the coordinate span of the conjoined predicate). **The definitional inclusion** `numberSpinZSectorEuclidean_le_spinZSectorEuclidean`: the joint sector is contained in the spin-`z`-only sector, by `inf_le_right`. **Debt discharge (PR-3)**: `repulsiveSpinZSector_ground_unique_on_numberSpinZSector` restricts PR-1's `repulsiveSpinZSector_ground_unique` (stated on `spinZSectorEuclidean`) down to the joint sector `numberSpinZSectorEuclidean` via PR-11a's `IsUniqueGroundStateOn.mono`, using the number-operator eigenvalue conjunct `N̂ φ = (N+1) • φ` already exported by that ground state to place PR-1's witness `φ` inside the smaller sector — the `IsUniqueGroundStateOn`-on-`K` hypothesis PR-3's `exists_unique_casimir_sector_strict_min` could previously only take abstractly. **Capstone** `liebRepulsive_exists_unique_casimir_sector`: feeds this restriction into PR-3's `exists_unique_casimir_sector_strict_min` (given the SU(2) commute/Hermiticity adapters for `symmetricRepulsiveHubbardHamiltonian` as explicit hypotheses — not yet formalized for this Hamiltonian family, scheduled PR-12 work), yielding a unique, strictly-minimal occupied Casimir sector for the repulsive model's ground state on the joint sector. The capstone's own docstring pins down the four remaining obligations before `theorem_10_4_lieb_repulsive_half_filling` can be discharged: (1) the SU(2) commute/Hermiticity adapters (PR-12), (2) identifying the occupied Casimir eigenvalue with `liebRepulsiveSpinCasimir A` (PR-12/PR-13), (3) the site-dependent `U_x → U` reduction (PR-12b), (4) the finrank/degeneracy count assembly (PR-13/PR-14). **Debt discharged**: this PR consumes PR-11a's `eigenspace_diagonal_eq_coordinateSpan` and `IsUniqueGroundStateOn.mono`, closing both debts (see the updated PR-11a row above); PR-11a's `configSectorCompress_eq_submatrix` was later **deleted in PR-12a** as a decorative (reference-0) declaration rather than staged a fourth time (see the PR-12a row below). **Debt (new)**: two declarations introduced by this PR are at reference 0 — the diagonal-charge bridge `numberSpinZSectorEuclidean_eq_coordinateSpan_liebHalfFillingPred` (not itself chained into this file's own capstone, staged as a standalone fact for PR-12's sector-level transports) and the file's own final capstone `liebRepulsive_exists_unique_casimir_sector` (staged for PR-12's SU(2) adapters and the arc's remaining assembly). Axiom-free. **PR-12a has since consumed** `liebRepulsive_exists_unique_casimir_sector` by discharging all four SU(2)-adapter hypotheses and wrapping it as the unconditional corollary `liebRepulsive_exists_unique_casimir_sector_unconditional`, closing that debt (see the PR-12a row below); the diagonal-charge bridge remains unconsumed, staged for PR-12b/PR-13. **PR-13b has since consumed** `numberSpinZSectorEuclidean_eq_coordinateSpan_liebHalfFillingPred` (in the compressed-to-full transport of the λ-family ground state, `LiebRepulsiveCasimirPinning.lean`), closing that debt. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveSectorBridgeFinal.lean` + `Math/MatrixAnalysis/SubmatrixGroundState.lean` |
| `symmetricRepulsiveHubbardInteraction_eq_uniform_sub_siteNumber` / `symmetricRepulsiveHubbardHamiltonian_isHermitian` / `fermionTotalSpinPlus_commute_symmetricRepulsiveHubbardHamiltonian` / `fermionTotalSpinMinus_commute_symmetricRepulsiveHubbardHamiltonian` / `fermionTotalUpNumber_commute_symmetricRepulsiveHubbardHamiltonian` / `fermionTotalDownNumber_commute_symmetricRepulsiveHubbardHamiltonian` / `fermionTotalSpinZ_commute_symmetricRepulsiveHubbardHamiltonian` / `fermionTotalNumber_commute_symmetricRepulsiveHubbardHamiltonian` / `fermionTotalSpinSquared_commute_symmetricRepulsiveHubbardHamiltonian` / `liebHalfFillingSpinZVal_eq_of_two_mul` / `liebRepulsive_exists_unique_casimir_sector_unconditional` | **The `SU(2)` commute/Hermiticity adapters for the symmetric repulsive Hubbard Hamiltonian, and the unconditional capstone (Theorem 10.4 discharge arc, Issue #5320, PR #5338 PR-12a; PROVED, `#print axioms` = std3): supplies the four adapter hypotheses PR-11c's capstone took as explicit hypotheses, discharging them.** The site-dependent symmetric interaction `Ĥint' = Σ_x U_x(n̂_{x,↑}−½)(n̂_{x,↓}−½)` is not itself a multiple of `hubbardOnSiteInteractionSite`, so the attractive family's SU(2) proofs do not clone verbatim; `symmetricRepulsiveHubbardInteraction_eq_uniform_sub_siteNumber` expands it into three separately `SU(2)`-invariant summands (`hubbardOnSiteInteractionSite`, a per-site-number term, and a scalar multiple of `1`). For `Ŝ⁺`, the middle summand's invariance reuses the existing per-site-number commutator `totalSpinPlus_commute_fermionSiteNumber` (`TJSpinSymmetryRaising.lean`); no new commutator is needed there. For `N̂↑`/`N̂↓`, the per-site-number commutators are new, proved inline from `fermionMultiNumber_commute`; `Ŝ³` and `N̂` are then free corollaries of the `N̂↑`/`N̂↓` results. `symmetricRepulsiveHubbardHamiltonian_isHermitian` establishes Hermiticity (kinetic part via `hubbardKinetic_isHermitian`, interaction part via the manifestly real diagonal `symmetricRepulsiveHubbardInteraction_eq_diagonal`). The seven generator/Casimir commutators (`Ŝ⁺`, `Ŝ⁻`, `N̂↑`, `N̂↓`, `Ŝ³`, `N̂`, `(Ŝ_tot)²`) assemble from these, mirroring `LiebAttractiveSU2Invariance.lean` line for line. `liebHalfFillingSpinZVal_eq_of_two_mul` is the `Ne = 2·nUp` sector-arithmetic bridge PR-12b's per-`s` instantiation needs to match PR-11c's `liebHalfFillingSpinZVal` against the `Ne`-indexed statement of Theorem 10.4. **Capstone** `liebRepulsive_exists_unique_casimir_sector_unconditional`: feeds all four discharged adapters into PR-11c's `liebRepulsive_exists_unique_casimir_sector`, yielding the same unique-strictly-minimal-occupied-Casimir-sector conclusion **unconditionally** (no `SU(2)`/Hermiticity hypotheses left), closing PR-11c's reference-0 debt on that capstone. **Debt**: the sector-arithmetic bridge `liebHalfFillingSpinZVal_eq_of_two_mul` remains at reference 0 — PR-12b did **not** consume it as originally staged, so it is still staged for a later PR (matching PR-11c's `liebHalfFillingSpinZVal N nUp` against the `Ne`-indexed statement of Theorem 10.4). **PR-13b has since consumed** `liebHalfFillingSpinZVal_eq_of_two_mul` (matching that `Ne`-indexed statement inside the Casimir-pinning capstone, `LiebRepulsiveCasimirPinning.lean`), closing that debt. This PR's own final capstone `liebRepulsive_exists_unique_casimir_sector_unconditional` was staged at reference 0, but **PR-12b has since consumed it** (as `symmetricHomotopy_exists_unique_casimir_sector` in `LiebRepulsiveSymmetricHomotopy.lean`), closing that debt. **Also deletes** `configSectorCompress_eq_submatrix` (`HubbardImpossibilityLowUVariationalCore.lean`), a decorative reference-0 declaration staged unconsumed through PR-11a → PR-11b → PR-11c with no genuine consumer after three rounds — per `lean-coding-conventions`'s "capstone 以外の参照 0 は装飾宣言" discipline, deleted rather than staged a fourth time (PR-13 can re-derive the two-line corollary at its actual point of use if one emerges). Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveSU2Invariance.lean` |
| `homotopyOnSiteFn` / `symmetricHomotopyHamiltonian` / `symmetricHomotopyHamiltonian_zero` / `continuous_symmetricHomotopyHamiltonian` / `continuous_minEnergyOn_symmetricHomotopyHamiltonian` / `symmetricHomotopy_exists_unique_casimir_sector` / `symmetricHomotopy_casimirSelector_eq_const` | **The site-dependent-`U` homotopy discharge, discharging the homotopy leg of obligation (3) of PR-11c's capstone docstring (Theorem 10.4 discharge arc, Issue #5320, PR #5339 PR-12b; PROVED, `#print axioms` = std3): reruns PR-4's uniform-`U` homotopy argument directly at fixed site-dependent `U : Fin (N+1) → ℝ`, never reducing to the uniform model, so the `U_x → U` reduction step is discharged by direct repetition rather than by transport. This PR only establishes the homotopy leg (`c 0 = c 1`, the site-dependent model's occupied Casimir sector agrees with the sector at the homotopy's `s = 1` endpoint); identifying that endpoint's Hamiltonian with the uniform `λ`-endpoint model of PR-4/PR-11c (no Lean lemma of the form `symmetricHomotopyHamiltonian _ _ _ _ _ 1 = symmetricRepulsiveHubbardHamiltonian _ (liebEndpointHopping _ _ _) (fun _ => 1)` exists yet in this arc) is deferred to PR-13. **PR-13a has since supplied** this identification as `symmetricHomotopyHamiltonian_one_eq_uniform`.** `homotopyOnSiteFn U s x := homotopyOnSite (U x) s` lifts PR-4's scalar on-site homotopy pointwise; `symmetricHomotopyHamiltonian` reuses PR-4's hopping homotopy `homotopyHopping`/`liebEndpointHopping` verbatim and pairs it with `homotopyOnSiteFn` through `symmetricRepulsiveHubbardHamiltonian`, so PR-1's constant-`U` shift argument (documented as inapplicable to a site-dependent potential in PR-4's module docstring) is never invoked, and `repulsiveSpinZSector_ground_unique` (`LiebRepulsiveBalancedGround.lean`, already `U`-generic) applies at every `s` with no adapter. `symmetricHomotopyHamiltonian_zero` identifies `H_0` with the original symmetric repulsive model; `continuous_symmetricHomotopyHamiltonian`/`continuous_minEnergyOn_symmetricHomotopyHamiltonian` mirror PR-4's continuity pair for the pointwise-affine on-site term; `symmetricHomotopy_exists_unique_casimir_sector` is PR-12a's unconditional capstone `liebRepulsive_exists_unique_casimir_sector_unconditional` applied at `T_s`, `U_s(·)` for every `s ∈ [0,1]` (hopping side-conditions transport via PR-4's `homotopyHopping_symm`/`_bipartite`/`_connected`, on-site positivity via `homotopyOnSiteFn_pos`). **Capstone** `symmetricHomotopy_casimirSelector_eq_const`: assembles the per-`s` existence-uniqueness instance (via `choose`) with the continuity fact through PR-4's `casimirSelector_eq_const_of_locally_unique_strict_min`, yielding a selector `c : ℝ → ℂ` constant along the whole homotopy, in particular `c 0 = c 1` — the occupied Casimir sector of the site-dependent symmetric model (`s = 0`) agrees with the occupied Casimir sector at the homotopy's `s = 1` endpoint; that `s = 1` Hamiltonian's identification with the `λ`-endpoint model of PR-4/PR-11c is deferred to PR-13 (see above; **PR-13a has since supplied** it as `symmetricHomotopyHamiltonian_one_eq_uniform`). `symmetricHomotopyHamiltonian_zero` was at reference 0 (docstring mention only, not consumed by any proof); **PR-13b has since consumed** it as a genuine proof step identifying the coupling-independent `s = 0` endpoint at two distinct couplings (`LiebRepulsiveCasimirPinning.lean`), closing that debt. The capstone `symmetricHomotopy_casimirSelector_eq_const` is also at reference 0 as introduced by this PR, but **PR-13b has since consumed** it too (evaluating the selector at `s = 0` along the `λ → 0⁺` family), closing that debt as well; `symmetricHomotopy_exists_unique_casimir_sector` is **not** at reference 0 — it is consumed by the capstone's proof in this same file. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveSymmetricHomotopy.lean` |
| `sum_fermionSiteNumber_eq_fermionTotalNumber` / `configSectorCompress_fermionTotalNumber_eq_smul_one` / `symmetricHomotopyHamiltonian_one_eq_uniform` / `configSectorCompress_symmetricHomotopyHamiltonian_one_eq_perturbedHamiltonian_sub_smul` / `isUniqueGroundStateOn_coordinateSpan_iff_submatrix` (generalized) / `blockSupport_apply_eq_zero` | **Symmetric endpoint identification and block-invariance transport (Theorem 10.4 discharge arc, Issue #5320, PR #5340 PR-13a; PROVED, `#print axioms` = std3): identifies PR-12b's homotopy `s = 1` endpoint with PR-5/PR-6's `λ`-family perturbation setup, and generalizes the arc's block-transport lemma from block support to block invariance.** `sum_fermionSiteNumber_eq_fermionTotalNumber`: the site occupation numbers sum to the total particle number, `Σ_x n̂_x = N̂`. `configSectorCompress_fermionTotalNumber_eq_smul_one`: `N̂` compresses to the scalar `(N+1) • 1` on the half-filled fixed-`Ŝ³` sector, since every configuration of that sector carries exactly `N+1` electrons. `symmetricHomotopyHamiltonian_one_eq_uniform`: PR-12b's site-dependent-`U` homotopy's `s = 1` endpoint is exactly the uniform-`U = 1` symmetric repulsive Hubbard Hamiltonian on the endpoint hopping matrix `liebEndpointHopping A T lam` (`homotopyHopping … 1 = liebEndpointHopping A T lam`, `homotopyOnSiteFn U 1 ≡ 1`). **Capstone** `configSectorCompress_symmetricHomotopyHamiltonian_one_eq_perturbedHamiltonian_sub_smul`: combining that endpoint identity with PR-12a's `symmetricRepulsiveHubbardInteraction_eq_uniform_sub_siteNumber` (at `U ≡ 1`) and PR-6's `homotopyHamiltonian_one_compressed_eq_perturbedHamiltonian`, the compressed number operator collapses to `(N+1) • 1` on the sector, so the compressed endpoint equals PR-5's compressed perturbed Hamiltonian shifted by the real constant `((N+1)/4 : ℝ)` — the shape `isUniqueGroundStateOn_sub_smul_one_iff` (`Math/MatrixAnalysis/SubmatrixGroundState.lean`) consumes directly, so no sector-restricted shift lemma is needed. **`BlockTransport.lean` generalization**: `isUniqueGroundStateOn_coordinateSpan_iff_submatrix` (PR-10a) is re-derived under the weaker invariance hypothesis `hInv : ∀ i j, P j → ¬ P i → H i j = 0` in place of the block-support hypothesis `H = P̂·H·P̂`, so it now also covers a Hamiltonian merely block-*diagonal* across `P`/`¬P`; the new `blockSupport_apply_eq_zero` lets a block-supported model (e.g. `LiebRepulsiveSectorAssembly.lean`'s second-order effective Hamiltonian, via its kernel-projection sandwich) supply `hInv` in one line, so the sole existing call site needs no shape change (the block-diagonal-but-not-block-supported coverage this buys has no consumer yet, staged for a later PR — see the PR-10a entry above). **Debt discharged.** Deletes `Math/MatrixAnalysis/MinEnergyOnSubspace.lean`'s `minEnergyOn_add_const_smul_one` (staged unconsumed since PR-5, per its own doc comment's stipulation) as a decorative reference-0 declaration, since the arc's assembly normalises the energy origin at the `IsUniqueGroundStateOn` level instead (PR-11a's `isUniqueGroundStateOn_sub_smul_one_iff`). **Debt (new).** This PR's own capstone `configSectorCompress_symmetricHomotopyHamiltonian_one_eq_perturbedHamiltonian_sub_smul` was at reference 0, staged for PR-13b's consumption. **PR-13b has since consumed** it (in the compressed-to-full transport `isUniqueGroundStateOn_symmetricHomotopyHamiltonian_one_of_compressed`, `LiebRepulsiveCasimirPinning.lean`), closing that debt. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveEndpointIdentification.lean` + `Math/MatrixAnalysis/BlockTransport.lean` + `Fermion/JordanWigner/Hubbard/LiebRepulsiveSectorAssembly.lean` |
| `casimirSelector_strict_min_unique` / `symmetricHomotopyHamiltonian_one_isUniqueGroundStateOn` / `symmetricHomotopy_casimirSelector_zero_eq_liebRepulsiveSpinCasimir` | **Casimir pinning `c 0 = liebRepulsiveSpinCasimir A` (Theorem 10.4 discharge arc, Issue #5320, PR #5341 PR-13b; PROVED, `#print axioms` = std3): identifies the arc's occupied Casimir sector with `liebRepulsiveSpinCasimir A`, discharging obligation (2) of PR-11c's capstone docstring.** `casimirSelector_strict_min_unique`: two occupied Casimir eigenvalues of the *same* Hamiltonian, each satisfying the strict-minimality property of `exists_unique_casimir_sector_strict_min`, coincide (each would otherwise be strictly below the other). **Capstone 1** `symmetricHomotopyHamiltonian_one_isUniqueGroundStateOn`: the PR-13a-scoped λ-family transport capstone — for every sufficiently small `λ > 0`, PR-11b's compressed unique ground state (`tasaki_lemma_10_1_liebRepulsive_apply`) transports, via `configSectorCompress_apply`, PR-13a's compressed endpoint identity, and the generalized `isUniqueGroundStateOn_coordinateSpan_iff_submatrix` (`Math/MatrixAnalysis/BlockTransport.lean`), to a unique ground state of the homotopy's `s = 1` endpoint on the joint number/spin-`z` sector `K = numberSpinZSectorEuclidean N (N+1) m₀`. **Capstone 2** `symmetricHomotopy_casimirSelector_zero_eq_liebRepulsiveSpinCasimir`: extends PR-12b's `symmetricHomotopy_casimirSelector_eq_const` (`c 0 = c 1`) with `c 0 = liebRepulsiveSpinCasimir A`. For every `λ` the transported `s = 1` ground state occupies, by `exists_unique_casimir_sector_strict_min`, an occupied Casimir sector; selector uniqueness identifies it with the PR-12b selector value at `s = 1` for that coupling, hence — through `c 0 = c 1` and the `λ`-independence of the `s = 0` endpoint (`symmetricHomotopyHamiltonian_zero`) — with `c 0` for the *given* coupling. Restricting the resulting Casimir eigen-equation to the half-filled coordinate block and passing `λ → 0⁺` (Lemma 10.1's convergence `Philam λ → Φeff`) transports it to the effective ground state `Φeff` by uniqueness of limits; `Φeff` is then identified, via a reindexing onto the hard-core sector and mutual energy minimality, with the Marshall-positive Theorem 2.3 eigenvector of `liebRepulsive_groundState_casimir_eq_predicted`, which carries `liebRepulsiveSpinCasimir A`. **Debt (new).** Both of this PR's own capstones are at reference 0, staged for PR-14's finrank/degeneracy-count assembly: `symmetricHomotopyHamiltonian_one_isUniqueGroundStateOn` (Capstone 1) and `symmetricHomotopy_casimirSelector_zero_eq_liebRepulsiveSpinCasimir` (Capstone 2). Also extends `exists_unique_casimir_sector_strict_min`'s conclusion with the membership witness `φ ∈ K_c` (see the updated PR-3 row above) and factors `apply_eq_zero_of_mapsTo_coordinateSpan` / `toEuclideanLin_submatrix_equiv_apply` out of `Math/MatrixAnalysis/BlockTransport.lean` / `SubmatrixGroundState.lean` respectively (see the updated PR-10a/PR-11a rows above). **PR-14a has since consumed** Capstone 2 (`symmetricHomotopy_casimirSelector_zero_eq_liebRepulsiveSpinCasimir`, in `liebRepulsive_admissibleSector_groundState_casimir_eigenvector`, `LiebRepulsiveMultipletCompanion.lean`), closing that debt. Capstone 1 (`symmetricHomotopyHamiltonian_one_isUniqueGroundStateOn`) remains at reference 0, still staged for a later assembly step. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveCasimirPinning.lean` |
| `mulVec_eq_smul_iff_toEuclideanLin_toLp_eq_smul` / `finrank_iSup_le_sum` / `symmetricRepulsiveHubbardHamiltonian_mul_tJTotalSpinOne` / `symmetricRepulsiveHubbardHamiltonian_mul_tJTotalSpinTwo` / `liebRepulsive_su2_weight_transport` / `liebRepulsive_admissibleSector_groundState_casimir_eigenvector` / `liebRepulsive_multipletCompanion_capstone` | **SU(2) weight transport and the sector energy ladder (Theorem 10.4 discharge arc, Issue #5320, PR #5342 PR-14a; PROVED, `#print axioms` = std3): the first half of the arc's final assembly step (the complementary weight confinement + `finrank` count is PR-14b, and the axiom discharge itself is PR-15).** Two generic layer items open the file: `mulVec_eq_smul_iff_toEuclideanLin_toLp_eq_smul` (`Math/MatrixAnalysis/PiEuclideanEigenBridge.lean`) crosses, once and for all, the boundary between the plain function carrier (`Matrix.mulVec`, used by `hubbardGroundSubmoduleAtElectronNumber`) and the `EuclideanSpace ℂ n` carrier (`Matrix.toEuclideanLin`, used by the arc's sector machinery); `finrank_iSup_le_sum` (`Math/MatrixAnalysis/FinrankIndexedSup.lean`) generalizes mathlib's two-submodule `finrank (p ⊔ q) ≤ finrank p + finrank q` to a finite indexed supremum by induction, supplying the upper bound PR-14b's `Ŝ³`-decomposition step needs; **at reference 0 in this PR, staged for PR-14b's consumption.** `symmetricRepulsiveHubbardHamiltonian_mul_tJTotalSpinOne`/`Two` extend `LiebRepulsiveSU2Invariance.lean` (PR-12a) with the Cartesian-generator commutators (`Ĥ` commutes with `Ŝ⁽¹⁾_tot = ½(Ŝ⁺+Ŝ⁻)` and `Ŝ⁽²⁾_tot = −(i/2)(Ŝ⁺−Ŝ⁻)`), the repulsive analogues of `attractiveHubbardHamiltonian_mul_tJTotalSpinOne`/`Two` (`LiebAttractiveFullSectorSU2Algebra.lean`). **Step 1** `liebRepulsive_admissibleSector_groundState_casimir_eigenvector`: for an admissible `Ŝ³` value, the unique ground state on `numberSpinZSectorEuclidean` is an `Ŝ²`-eigenvector at `liebRepulsiveSpinCasimir A`, combining `repulsiveSpinZSector_ground_unique_on_numberSpinZSector` (`LiebRepulsiveSectorBridgeFinal.lean`) with `exists_unique_casimir_sector_strict_min` (`LiebRepulsiveCasimirSector.lean`) and PR-13b's `casimirSelector_strict_min_unique` + `symmetricHomotopy_casimirSelector_zero_eq_liebRepulsiveSpinCasimir` (`LiebRepulsiveCasimirPinning.lean`), instantiated at `λ = 1` (the homotopy coupling is irrelevant here). **Steps 2/3** `liebRepulsive_su2_weight_transport`: specializes `ham_su2_multiplet_companion` (`Math/AngularMomentum/Multiplet.lean`) to the physical Hamiltonian, producing at every weight `J − k` (`k ≤ 2J`) a nonzero companion sharing the seed's `Ĥ`/`N̂` eigenvalues; a private wrapper `liebRepulsive_transport_to_sector` packages the companion into a named `numberSpinZSectorEuclidean` sector. Two further private lemmas assemble the energy ladder and its converse: `liebRepulsive_sector_energy_le` (one-directional ground-energy inequality between two admissible sectors, via the weight-transport companion) and `liebRepulsive_groundEnergy_le_of_electronNumber` (global minimality: every `(N+1)`-electron eigenvalue of the physical Hamiltonian is at least the common admissible-sector ground energy, via `exists_joint_eigenvector_in_invariant_submodule` and the angular-momentum quantization `angMom_sub_mem_nat`). **Capstone** `liebRepulsive_multipletCompanion_capstone`: assembles all of the above into a single ground energy `E₀` such that every admissible `Ŝ³` sector has a unique ground state at `E₀` carrying `liebRepulsiveSpinCasimir A`, and `E₀` is minimal over the whole `(N+1)`-electron sector — conjunct (ii) of `theorem_10_4_lieb_repulsive_half_filling` (`LiebRepulsive.lean:134`), restricted to the symmetric disjunct; PR-14b's weight confinement and `finrank` count are **not** part of this capstone. **Debt discharged.** This PR consumes and closes PR-13b's Capstone 2 `symmetricHomotopy_casimirSelector_zero_eq_liebRepulsiveSpinCasimir` (see the updated PR-13b row above); PR-13b's Capstone 1 `symmetricHomotopyHamiltonian_one_isUniqueGroundStateOn` is **not** consumed by this PR (`liebRepulsive_admissibleSector_groundState_casimir_eigenvector` reaches its unique-ground-state premise via `repulsiveSpinZSector_ground_unique_on_numberSpinZSector` instead) and remains at reference 0, staged for a later assembly step. Axiom-free. | `Math/MatrixAnalysis/PiEuclideanEigenBridge.lean` / `Math/MatrixAnalysis/FinrankIndexedSup.lean` / `Fermion/JordanWigner/Hubbard/LiebRepulsiveSU2Invariance.lean` / `Fermion/JordanWigner/Hubbard/LiebRepulsiveMultipletCompanion.lean` |

---

[← Multi-mode fermion via Jordan–Wigner (P2 backbone)](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-03/) · [Catalogue](/lattice-system/formalization/legacy/) · End →
