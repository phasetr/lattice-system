---
layout: page
title: "Legacy catalogue: Multi-mode fermion via Jordan–Wigner (P2 backbone) (part 5 of 5)"
permalink: /formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-05/
---

# Legacy catalogue: Multi-mode fermion via Jordan–Wigner (P2 backbone) (part 5 of 5)

> **Interim authority.** This lossless catalogue chunk remains authoritative for formalization status and capstone identification until Issue #5228. The version 1 JSON catalogue is still a non-authoritative prototype.

[Interim catalogue](/lattice-system/formalization/legacy/) › [Fermions and Hubbard models](/lattice-system/formalization/legacy/#group-fermions-hubbard)

## Authoritative supplemental implementation record (§10.2.2 eq. (10.2.9) uniform/symmetric ground-submodule reduction) (continued)

This section is maintained by hand, lies outside the migrated catalogue block, and records new
supporting theorems (the eq. (10.2.9) reduction step) added after the migration baseline; it is
not subject to the frozen byte-for-byte parity of the migrated block. It continues the table
started in [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/#authoritative-supplemental-implementation-record-1022-eq-1029-uniformsymmetric-ground-submodule-reduction).

| Lean name | Statement | File |
|---|---|---|
| <a id="tasaki-chapter-10-part-03"></a> `mulVec_eq_smul_iff_toEuclideanLin_toLp_eq_smul` / `symmetricRepulsiveHubbardHamiltonian_mul_tJTotalSpinOne` / `symmetricRepulsiveHubbardHamiltonian_mul_tJTotalSpinTwo` / `liebRepulsive_su2_weight_transport` / `liebRepulsive_admissibleSector_groundState_casimir_eigenvector` / `liebRepulsive_multipletCompanion_capstone` | **SU(2) weight transport and the sector energy ladder (Theorem 10.4 discharge arc, Issue #5320, PR #5342 PR-14a; PROVED, `#print axioms` = std3): the first half of the arc's final assembly step (the complementary weight confinement + `finrank` count is PR-14b, and the axiom discharge itself is PR-15).** One generic layer item opens the file: `mulVec_eq_smul_iff_toEuclideanLin_toLp_eq_smul` (`Math/MatrixAnalysis/PiEuclideanEigenBridge.lean`) crosses, once and for all, the boundary between the plain function carrier (`Matrix.mulVec`, used by `hubbardGroundSubmoduleAtElectronNumber`) and the `EuclideanSpace ℂ n` carrier (`Matrix.toEuclideanLin`, used by the arc's sector machinery). `symmetricRepulsiveHubbardHamiltonian_mul_tJTotalSpinOne`/`Two` extend `LiebRepulsiveSU2Invariance.lean` (PR-12a) with the Cartesian-generator commutators (`Ĥ` commutes with `Ŝ⁽¹⁾_tot = ½(Ŝ⁺+Ŝ⁻)` and `Ŝ⁽²⁾_tot = −(i/2)(Ŝ⁺−Ŝ⁻)`), the repulsive analogues of `attractiveHubbardHamiltonian_mul_tJTotalSpinOne`/`Two` (`LiebAttractiveFullSectorSU2Algebra.lean`). **Step 1** `liebRepulsive_admissibleSector_groundState_casimir_eigenvector`: for an admissible `Ŝ³` value, the unique ground state on `numberSpinZSectorEuclidean` is an `Ŝ²`-eigenvector at `liebRepulsiveSpinCasimir A`, combining `repulsiveSpinZSector_ground_unique_on_numberSpinZSector` (`LiebRepulsiveSectorBridgeFinal.lean`) with `exists_unique_casimir_sector_strict_min` (`LiebRepulsiveCasimirSector.lean`) and PR-13b's `casimirSelector_strict_min_unique` + `symmetricHomotopy_casimirSelector_zero_eq_liebRepulsiveSpinCasimir` (`LiebRepulsiveCasimirPinning.lean`), instantiated at `λ = 1` (the homotopy coupling is irrelevant here). **Steps 2/3** `liebRepulsive_su2_weight_transport`: specializes `ham_su2_multiplet_companion` (`Math/AngularMomentum/Multiplet.lean`) to the physical Hamiltonian, producing at every weight `J − k` (`k ≤ 2J`) a nonzero companion sharing the seed's `Ĥ`/`N̂` eigenvalues; `liebRepulsive_transport_to_sector` packages the companion into a named `numberSpinZSectorEuclidean` sector. Two further private lemmas assemble the energy ladder and its converse: `liebRepulsive_sector_energy_le` (one-directional ground-energy inequality between two admissible sectors, via the weight-transport companion) and `liebRepulsive_groundEnergy_le_of_electronNumber` (global minimality: every `(N+1)`-electron eigenvalue of the physical Hamiltonian is at least the common admissible-sector ground energy, via `exists_joint_eigenvector_in_invariant_submodule` and the angular-momentum quantization `angMom_sub_mem_nat`). **Capstone** `liebRepulsive_multipletCompanion_capstone`: assembles all of the above into a single ground energy `E₀` such that every admissible `Ŝ³` sector has a unique ground state at `E₀` carrying `liebRepulsiveSpinCasimir A`, and `E₀` is minimal over the whole `(N+1)`-electron sector — conjunct (ii) of `theorem_10_4_lieb_repulsive_half_filling` (`LiebRepulsiveHalfFillingDischarge.lean`), restricted to the symmetric disjunct; PR-14b's weight confinement and `finrank` count are **not** part of this capstone. **Debt discharged.** This PR consumes and closes PR-13b's Capstone 2 `symmetricHomotopy_casimirSelector_zero_eq_liebRepulsiveSpinCasimir` (see the updated PR-13b row above); PR-13b's Capstone 1 `symmetricHomotopyHamiltonian_one_isUniqueGroundStateOn` is **not** consumed by this PR (`liebRepulsive_admissibleSector_groundState_casimir_eigenvector` reaches its unique-ground-state premise via `repulsiveSpinZSector_ground_unique_on_numberSpinZSector` instead) and remained at reference 0 through this PR; it was **deleted in PR-14b** (see the new PR-14b row below) rather than staged a third time. Axiom-free. | `Math/MatrixAnalysis/PiEuclideanEigenBridge.lean` / `Fermion/JordanWigner/Hubbard/LiebRepulsiveSU2Invariance.lean` / `Fermion/JordanWigner/Hubbard/LiebRepulsiveMultipletCompanion.lean` |
| `liebRepulsive_groundSubmodule_le_comap_of_commute` / `liebRepulsive_exists_jointEigenvector_of_ne_bot` / `liebRepulsive_mem_groundSubmodule_inf_spinZ_iff` / `liebRepulsive_groundSubmodule_eq_iSup_inf_eigenspace` / `liebRepulsive_groundSubmodule_spinZ_weight_admissible` / `liebRepulsive_groundSubmodule_inf_spinZ_le_span` / `liebRepulsive_finrank_groundSubmodule_le` / `liebRepulsive_finrank_groundSubmodule_ge` / `liebRepulsive_groundSubmodule_le_spinSquared_eigenspace` / `liebRepulsive_groundSubmodule_ne_bot` / `liebRepulsive_symmetric_halfFilling_conditional` | **Weight confinement, the `finrank` count, and the arc's symmetric-disjunct conditional capstone (Theorem 10.4 discharge arc, Issue #5320, PR #5343 PR-14b; PROVED, `#print axioms` = std3): the second half of the arc's final assembly step (axiom discharge itself remains PR-15's responsibility).** `liebRepulsive_groundSubmodule_le_comap_of_commute` factors the invariance-under-a-commuting-charge step out of PR-14a's `liebRepulsive_groundEnergy_le_of_electronNumber` for reuse on `Ŝ³` weight blocks. `liebRepulsive_exists_jointEigenvector_of_ne_bot` generalizes that PR's seed-extraction step from the whole `(N+1)`-electron ground submodule `G` to any `Ŝ²`/`Ŝ³`-invariant submodule `B ≤ G`, so **weight confinement** (`liebRepulsive_groundSubmodule_spinZ_weight_admissible`) can run it at `B = G ⊓ eigenspace Ŝ³ μ`: an occupied weight block sits at an admissible weight `liebHalfFillingSpinZVal N q`, `cB ≤ q ≤ cA`, by comparing the block's joint-eigenvector spin label against `S₀ = (cA − cB)/2` via `c₀ = S₀(S₀+1)`. `liebRepulsive_mem_groundSubmodule_inf_spinZ_iff` and `liebRepulsive_groundSubmodule_eq_iSup_inf_eigenspace` supply the Pi/Euclidean packaging and the `Ŝ³`-weight decomposition `G = ⨆ μ, G ⊓ eigenspace Ŝ³ μ` that confinement runs on. `liebRepulsive_groundSubmodule_inf_spinZ_le_span` shows each admissible block is one-dimensional (spanned by its sector's unique ground state); `liebRepulsive_finrank_groundSubmodule_le`/`_ge` combine confinement with this one-dimensionality (upper bound) and with the linear independence of distinct-`Ŝ³`-weight eigenvectors (lower bound, via `Module.End.eigenvectors_linearIndependent'`) to pin `finrank G = cA − cB + 1`. `liebRepulsive_groundSubmodule_le_spinSquared_eigenspace` and `liebRepulsive_groundSubmodule_ne_bot` supply conjuncts (iii) and (i). **Capstone** `liebRepulsive_symmetric_halfFilling_conditional`: assembles all of the above with PR-14a's `liebRepulsive_multipletCompanion_capstone` into `theorem_10_4_lieb_repulsive_half_filling`'s conclusion verbatim for `H = symmetricRepulsiveHubbardHamiltonian N T U` under `1 ≤ |A|`, `1 ≤ |B|` — a genuine conditional theorem, not itself a replacement of the axiom. **Debt discharged.** Also deletes PR-13b's Capstone 1 `symmetricHomotopyHamiltonian_one_isUniqueGroundStateOn` (see the updated PR-13b/PR-14a rows above): it remained at reference 0 through PR-14a, and this PR's route to weight confinement does not need it, so it is removed as a decorative reference-0 declaration rather than staged a third time. **Debt (new).** This PR's own capstone `liebRepulsive_symmetric_halfFilling_conditional` was at reference 0, staged for PR-15's axiom discharge, **since consumed by PR-15c** (`LiebRepulsiveHalfFillingDischarge.lean`). Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveMultipletCompanion.lean` / `Fermion/JordanWigner/Hubbard/LiebRepulsiveWeightConfinement.lean` |
| `mem_eigenspace_diagonal_mulVecLin_iff` / `finrank_eigenspace_diagonal_mulVecLin` / `liebRepulsive_hopping_eq_zero_of_degenerate` / `liebRepulsive_degenerate_N_eq_zero` / `liebRepulsive_degenerate_sublatticeImbalance_eq_one` / `liebRepulsive_groundSubmodule_N0_eq_numberEigenspace` / `liebRepulsive_singlyOccupied_card_eq_two` / `liebRepulsive_symmetric_halfFilling_degenerate` / `liebRepulsive_symmetric_halfFilling` / `liebRepulsive_uniform_of_symmetric` | **Degenerate-sublattice reduction, the `N = 0` block, and the all-`A` symmetric/uniform assembly (Theorem 10.4 discharge arc, Issue #5320, PR #5345 PR-15b; PROVED, `#print axioms` = std3).** Two generic layer items open a new file: `mem_eigenspace_diagonal_mulVecLin_iff` (a vector lies in the `L`-eigenspace of `(Matrix.diagonal d).mulVecLin` iff it vanishes at every coordinate `i` with `d i ≠ L`) and `finrank_eigenspace_diagonal_mulVecLin` (that eigenspace's `finrank` is the `Nat.card` of the coordinates with `d i = L`), both in `Math/MatrixAnalysis/PiDiagonalEigenspace.lean`. **Degeneracy reduction:** `liebRepulsive_hopping_eq_zero_of_degenerate` shows `|A| = 0 ∨ |B| = 0` forces `T = 0` (vacuous/total membership in `HoppingRespectsBipartition`); `liebRepulsive_degenerate_N_eq_zero` then forces `N = 0` via the vanishing hopping's edgeless support graph and `SimpleGraph.preconnected_bot_iff_subsingleton`; `liebRepulsive_degenerate_sublatticeImbalance_eq_one` records `sublatticeImbalance A = 1` at `N = 0` for every bipartition. **The `N = 0` block:** with `T = 0` the symmetric Hamiltonian reduces to a diagonal matrix, so `liebRepulsive_groundSubmodule_N0_eq_numberEigenspace` identifies the `E₀ = −U₀/4` ground submodule with the singly-occupied (`= 1`) eigenspace of the total number operator; `liebRepulsive_singlyOccupied_card_eq_two` counts its two spanning configurations (`decide`); `liebRepulsive_symmetric_halfFilling_degenerate` assembles the four Theorem 10.4 conjuncts at `N = 0` (Casimir `3/4`, `finrank = 2`, both matching `liebRepulsiveSpinCasimir`/`liebRepulsiveGroundMultiplicity` at the one-point bipartition), the Casimir witness on the spin-down basis vector routed through PR-14a's `liebRepulsive_su2_weight_transport` at a trivial (`T = 0`, `U = 0`) companion Hamiltonian. **All-`A` assembly:** `liebRepulsive_symmetric_halfFilling` combines PR-14b's `liebRepulsive_symmetric_halfFilling_conditional` (`1 ≤ |A|`, `1 ≤ |B|`) with the degenerate block above into the four Theorem 10.4 conjuncts for `symmetricRepulsiveHubbardHamiltonian`, for **every** bipartition `A`; `liebRepulsive_uniform_of_symmetric` transports that all-`A` symmetric-form statement at a constant `U` to the uniform-interaction Hamiltonian `repulsiveHubbardHamiltonian`, via `symmetricRepulsiveHubbardHamiltonian_groundSubmodule_eq_uniform` (`LiebRepulsiveBalancedGround.lean:328`, an energy shift). **Debt (new, both reference 0 within this PR, staged for PR-15c's capstone):** `liebRepulsive_symmetric_halfFilling` and `liebRepulsive_uniform_of_symmetric` are not yet consumed; PR-15c's responsibility is to assemble the uniform-Hamiltonian conclusion into `theorem_10_4_lieb_repulsive_half_filling`'s exact statement and move it out of `LiebRepulsive.lean`. **PR #5346 PR-15c has since consumed** both, discharging the Theorem 10.4 axiom (see the [Theorem 10.4 row](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/#tasaki-chapter-10-part-02)) and closing this debt. **Incidental:** `LiebRepulsiveShibaKinetic.lean`'s `diagonal_mulVec_basisVec` (a diagonal matrix scales a basis vector) is made non-`private` in this PR to avoid duplicating it in the new file. Axiom-free. | `Math/MatrixAnalysis/PiDiagonalEigenspace.lean` / `Fermion/JordanWigner/Hubbard/LiebRepulsiveHalfFillingDischarge.lean` |

## Authoritative supplemental implementation record (Theorem 10.6 discharge arc, PR-3: `SU(2)` invariance of `Ô²`)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/#authoritative-supplemental-implementation-record-theorem-106-discharge-arc-pr-2-transverse-casimir-identity)
(Issue #5347, PR #5350); it is not subject to the frozen byte-for-byte parity of the migrated
block. New capacity is recorded here rather than in part 4, whose migrated-block byte budget is
near its `HARD_BYTES` limit (`scripts/check_docs_hierarchy.py`).

| Lean name | Statement | File |
|---|---|---|
| `fermionStaggeredCasimirOp_commute_fermionTotalSpinZ` / `fermionStaggeredCasimirOp_commute_fermionTotalSpinPlus` | **`SU(2)` invariance of the squared staggered order parameter** (Theorem 10.6 discharge arc, Issue #5347, PR #5350 PR-3; **PROVED**, axiom-free): `(Ô_L)² = Σ_{x,y} ε_xε_y Ŝ_x·Ŝ_y` (`fermionStaggeredCasimirOp`) commutes with the total-spin generators used downstream, `[(Ô_L)², Ŝ³_tot] = [(Ô_L)², Ŝ⁺_tot] = 0`. Each per-pair dot product `Ŝ_x·Ŝ_y` is itself an `SU(2)` scalar, commuting with `Ŝ³_tot` (`totalSpinZ_commute_fermionSpinDot`, `TJSpinSymmetry.lean`) and `Ŝ⁺_tot` (`totalSpinPlus_commute_fermionSpinDot`, `TJSpinSymmetryRaising.lean`); scaling by the staggered sign product `ε_xε_y` and summing over the double sum preserves commutation. Mirrors the spin-`S` template of §4.1 Theorem 4.4 (eq. (4.1.12), `Quantum/SpinS/StaggeredCasimirSU2Invariance.lean`), transplanted to the fermionic carrier. **Debt discharged.** Both conjuncts are instantiated at `O = (Ô_L)²` by PR-8's final assembly into the ferrimagnetic bound (10.2.17) — `fermionStaggeredCasimirOp_commute_fermionTotalSpinPlus` feeds the tower-ratio transport, `fermionStaggeredCasimirOp_commute_fermionTotalSpinZ` feeds the cross-term vanishing — completing `theorem_10_6_lieb_ferrimagnetism` (Theorem 10.6 row, [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/), **PROVED**). The `Ŝ⁻_tot` conjunct recorded here through PR-4 was **deleted in PR-5**: no step of the arc consumes it (PR-4's ladder-ratio lemma takes only the `Ŝ⁺_tot` commutation, PR-5's cross terms only the `Ŝ³_tot` one); that deletion also left PR-1's `fermionStaggeredCasimirOp_isHermitian` at reference 0, and it was **deleted in PR-8** (see the updated PR-1 row in [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/#authoritative-supplemental-implementation-record-theorem-106-discharge-arc-pr-1-staggered-spin-component-algebra)). Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebFerrimagnetismSU2Invariance.lean` |

---

## Authoritative supplemental implementation record (Theorem 10.6 discharge arc, PR-4: generic ladder-expectation-ratio invariance)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-3 section above (Issue #5347, PR #5351); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `ladder_expectation_cross` / `ladder_expectationRatioRe_invariant` / `fermionSpinMinus_expectationRatioRe_invariant` | **Generic ladder-expectation-ratio invariance, and the fermionic instance for Theorem 10.6** (Theorem 10.6 discharge arc, Issue #5347, PR #5351 PR-4; **PROVED**, axiom-free): reusable model-agnostic matrix-algebra infrastructure, **not specific to this arc**. For `O Sp Sm : Matrix ι ι ℂ` with `Smᴴ = Sp`, `Commute O Sp`, and `v` an eigenvector of `Sp * Sm` at scalar `c`, `ladder_expectation_cross` (`Math/MatrixAnalysis/LadderExpectationRatio.lean`) gives the cross identity `⟨Sm v, O (Sm v)⟩ = c • ⟨v, O v⟩` (`⟨a, b⟩ := star a ⬝ᵥ b`), via `star_mulVec_dotProduct` + the adjoint hypothesis + the commuting rearrangement `Sp * O * Sm = O * (Sp * Sm)`. `ladder_expectationRatioRe_invariant` adds `Sm *ᵥ v ≠ 0`, takes the `O = 1` instance for the squared-norm scaling, deduces `c.im = 0` from the two real squared norms, and divides to get the invariant real Rayleigh quotient `⟨Sm v, O (Sm v)⟩.re / ‖Sm v‖² = ⟨v, O v⟩.re / ‖v‖²`. `fermionSpinMinus_expectationRatioRe_invariant` (`Fermion/JordanWigner/Hubbard/LiebFerrimagnetismLadderRatio.lean`) instantiates both at `Sp := Ŝ⁺_tot`, `Sm := Ŝ⁻_tot`, supplying `fermionTotalSpinMinus_conjTranspose` for the adjoint hypothesis and the Casimir rearrangement `fermionTotalSpinPlus_mul_fermionTotalSpinMinus` (`Ŝ⁺_tot Ŝ⁻_tot = (Ŝ_tot)² − Ŝ³_tot(Ŝ³_tot − 1)`) for the scalar `c = γ − m² + m` on a joint `Ŝ³_tot`/Casimir eigenvector. **Retrofit.** The two pre-existing spin-`S` theorems in `Quantum/SpinS/SU2ExpectationLadderInvariant.lean` (`su2_expectation_ladder_cross`, `su2_expectationRatioRe_ladder_invariant`) are retargeted onto this generic pair in the same PR, deleting roughly 90 lines of duplicated proof logic from that file while leaving both theorems' types unchanged; the only binder-level change is a rename, the now-unused lowering-commutation hypothesis of `su2_expectationRatioRe_ladder_invariant` becoming `_hOminus` (required by `warningAsError = true`), which the positional call site in `Quantum/SpinS/SU2ExpectationLadderIterated.lean` does not see. No declaration is added to the spin-`S` file: the generic lemma's scalar-action input is the pre-existing `totalSpinSOpPlus_mul_totalSpinSOpMinus_mulVec_eq`. **Debt discharged.** The fermionic instance `fermionSpinMinus_expectationRatioRe_invariant` supplies the arc's ground-multiplet lowering-tower step and is consumed by PR-8's final assembly `liebFerrimagnetism_tower_ratioRe_eq` into `theorem_10_6_lieb_ferrimagnetism` (Theorem 10.6 row, [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/), **PROVED**). Axiom-free. | `Math/MatrixAnalysis/LadderExpectationRatio.lean` / `Fermion/JordanWigner/Hubbard/LiebFerrimagnetismLadderRatio.lean` |

---

## Authoritative supplemental implementation record (Theorem 10.6 discharge arc, PR-5: ground-multiplet tower basis)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-3 section above (Issue #5347, PR #5353); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `fermionTotalSpin_abs_weight_le` | **The `Ŝ³_tot` weight band on the fermionic carrier** (Theorem 10.6 discharge arc, Issue #5347, PR #5353 PR-5; **PROVED**, axiom-free): a nonzero joint eigenvector of `(Ŝ_tot)²` at `J(J+1)` (`J ≥ 0`) and of `Ŝ³_tot` at `m` has `|m| ≤ J`. Tasaki Lemma A.15 (`angMom_abs_le_J`) fed with the fermionic Cartesian generators through `fermionTotalSpinSquared_eq_cartesianSqSum`; it names a preamble that was previously inlined in `attractiveHubbardFullSectorGround_inf_spinZ_eigenspace_eq_bot` and `liebRepulsive_exists_jointEigenvector_of_ne_bot`, both now retrofitted onto it. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebAttractiveFullSectorUnique.lean` |
| `liebRepulsiveSpinCasimir_eq_ofReal` / `liebRepulsive_ground_spinZ_abs_le` / `liebRepulsive_ground_exists_topWeight` / `liebRepulsive_ground_spinMinusPow_mem` / `liebRepulsive_ground_tower_ne_zero` / `liebRepulsive_ground_tower_linearIndependent` / `liebRepulsive_ground_eq_span_tower` / `liebRepulsive_tower_crossTerm_eq_zero` | **The repulsive-Hubbard ground multiplet is a single `SU(2)` lowering tower** (Theorem 10.6 discharge arc, Issue #5347, PR #5353 PR-5; **PROVED**, axiom-free): taking Theorem 10.4's conclusions as hypotheses (Casimir value `liebRepulsiveSpinCasimir A`, dimension `liebRepulsiveGroundMultiplicity A = L + 1` with `L = sublatticeImbalance A`, and `G ≠ ⊥`), the `(N+1)`-electron ground submodule `G` of `symmetricRepulsiveHubbardHamiltonian N T U` satisfies: every `Ŝ³_tot`-eigenvector of `G` has weight in `[−L/2, L/2]`; `G` contains a highest-weight vector `w` (`Ŝ⁺_tot w = 0`, `Ŝ³_tot w = (L/2) w`), obtained by raising a `Ŝ³_tot` seed until the band forces termination and pinning the weight through the Casimir equation `μ(μ+1) = (L/2)(L/2+1)` whose second root `−(L/2+1)` the band excludes; the lowered iterates `(Ŝ⁻_tot)^k w` stay in `G` (`k ≤ L` nonzero, all `L + 1` independent) and span it, `G = span ℂ (range fun k : Fin (L+1) => (Ŝ⁻_tot)^k w)`; and cross terms `⟨(Ŝ⁻_tot)^j w, O (Ŝ⁻_tot)^k w⟩` vanish for `j ≠ k` and any `Ŝ³_tot`-commuting `O`. **Debt discharged.** Three of the seven are consumed inside the file (`liebRepulsive_ground_spinZ_abs_le` by the top-weight construction, `liebRepulsive_ground_spinMinusPow_mem` and `liebRepulsive_ground_tower_linearIndependent` by the span identity). **`liebRepulsive_ground_exists_topWeight` and `liebRepulsive_ground_tower_ne_zero` were consumed by PR-6** (PR-6 section below); `liebRepulsive_ground_eq_span_tower` and `liebRepulsive_tower_crossTerm_eq_zero` are consumed by PR-8's final assembly `liebFerrimagnetism_bound_of_mem_ground` into `theorem_10_6_lieb_ferrimagnetism` (Theorem 10.6 row, [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/), **PROVED**). **`liebRepulsiveSpinCasimir_eq_ofReal` was made public in PR-8** (previously `private`), for PR-8's assembly to rewrite the Casimir value as a real-cast expectation. **Also deletes** PR-3's `fermionStaggeredCasimirOp_commute_fermionTotalSpinMinus` (see the updated PR-3 row above), which left PR-1's `fermionStaggeredCasimirOp_isHermitian` at reference 0; **that one was deleted in PR-8** (see the updated PR-1 row in [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/#authoritative-supplemental-implementation-record-theorem-106-discharge-arc-pr-1-staggered-spin-component-algebra)). Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebFerrimagnetismGroundTower.lean` |

---

## Authoritative supplemental implementation record (Theorem 10.6 discharge arc, PR-6: centered sector to Theorem 10.5 bridge)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-3 section above (Issue #5347, PR #5354); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `liebRepulsive_groundEnergy_eq_ofReal` / `liebRepulsive_sectorGroundEnergy_eq_groundEnergy` / `liebRepulsive_centered_eq_smul_sectorGround` / `liebRepulsive_centered_transverse_sign` / `liebRepulsive_exists_centered_transverse_sign` | **The centered tower member carries Theorem 10.5's correlation signs** (Theorem 10.6 discharge arc, Issue #5347, PR #5354 PR-6; **PROVED**, axiom-free): PR-5's ground multiplet is the lowering tower `(Ŝ⁻_tot)^k w` (`k = 0, …, L`, `L = sublatticeImbalance A`) of a highest-weight ground vector `w`; this file evaluates it at the **centered** exponent `k₀ = L/2` (`ℕ` division) and identifies that member with Theorem 10.5's sector ground state. `liebRepulsive_groundEnergy_eq_ofReal`: the `(N+1)`-electron ground energy `E₀` of a nonzero ground submodule is real (`Hermitian` eigenvalue), needed to feed `E₀.re` into the `ℝ`-valued sector predicate `IsUniqueGroundStateOn`. `liebRepulsive_sectorGroundEnergy_eq_groundEnergy`: the ground energy `E` of the centered spin-`z` sector `Ŝ³ = (Ne₀ − (N+1))/2` (electron number `Ne₀ = N + 1 + L % 2`, even since `L` and `N + 1` share parity) equals `E₀.re`, by a two-sided pinch — the sector ground state carries the half-filling number eigenvalue so `E₀.re ≤ E`, while the centered tower member is a nonzero `E₀.re`-eigenvector inside the sector so `E ≤ E₀.re`. `liebRepulsive_centered_eq_smul_sectorGround`: the energy match plus `IsUniqueGroundStateOn`'s uniqueness clause gives `(Ŝ⁻_tot)^{k₀} w = c • φ` for a nonzero `c`, where `φ` is Theorem 10.5's unique sector ground state. `liebRepulsive_centered_transverse_sign`: transports Theorem 10.5's transverse-correlation sign pattern (`theorem_10_5_shen_qiu_tian_transverse_sign`) onto the centered tower member — real, strictly positive on a shared sublattice, strictly negative across sublattices — the rescaling by `c` contributing only the harmless positive factor `|c|²`. `liebRepulsive_exists_centered_transverse_sign`: the existential capstone, pairing a single highest-weight ground vector `w` (`liebRepulsive_ground_exists_topWeight`) with both its weight equation and its centered-member sign pattern, which is what the ferrimagnetic bound (10.2.17) needs to consume. **Private supporting layer** (not listed above): `liebRepulsive_sublatticeImbalance_mod_two` (`L % 2 = (N + 1) % 2`, from `bipartitionComplement_card_add`); `vectorExpectation_smul_vector` (`⟨cv|O|cv⟩ = (c̄c)⟨v|O|v⟩`, the state-rescaling identity used for the `|c|²` factor); `liebRepulsive_tower_mem_spinZSector` (the tower member `(Ŝ⁻_tot)^k w` lies in the spin-`z` sector named by its weight `L/2 − k`, crossing the Pi carrier of `hubbardGroundSubmoduleAtElectronNumber` to the `EuclideanSpace` carrier of `spinZSectorEuclidean` via `mulVec_eq_smul_iff_toEuclideanLin_toLp_eq_smul`). The centered-weight arithmetic `liebRepulsive_centeredWeight_eq` (the centered tower weight `L/2 − k₀` equals the sector's spin-`z` value `(Ne₀ − (N+1))/2`) is public, being the weight match the centered instantiation supplies. **Debt discharged.** Consumes PR-5's `liebRepulsive_ground_exists_topWeight` and `liebRepulsive_ground_tower_ne_zero` (see the updated PR-5 row above), both previously at reference 0. The other four public declarations are consumed within this module in a straight-line chain up to `liebRepulsive_exists_centered_transverse_sign` (`liebRepulsive_sectorGroundEnergy_eq_groundEnergy` feeds `liebRepulsive_centered_eq_smul_sectorGround`, which together with `liebRepulsive_groundEnergy_eq_ofReal` feeds `liebRepulsive_centered_transverse_sign`, which in turn feeds `liebRepulsive_exists_centered_transverse_sign`). **`liebRepulsive_exists_centered_transverse_sign` has since been consumed by PR-7** (see the new PR-7 row below), whose capstone `liebRepulsive_exists_centered_ratioRe_ge_sq` supplies its sign-pattern hypothesis directly from this existential. **Signature generalized (Theorem 10.8 discharge arc, PR-3, see the new PR-3 row below).** `liebRepulsive_sectorGroundEnergy_eq_groundEnergy` now takes an explicit tower exponent `k ≤ L` and an implicit sector weight `m` matched by a hypothesis `hkm : L/2 − k = m`, in place of the hard-coded centered exponent, so callers outside this arc can present the sector at their own weight; `liebRepulsive_centered_mem_spinZSector` is renamed `liebRepulsive_tower_mem_spinZSector` and takes the same generalization. The centered use above (feeding `liebRepulsive_centered_eq_smul_sectorGround`) instantiates the generalized lemma at `k = k₀ = L/2` via the now-public `liebRepulsive_centeredWeight_eq`, unchanged in effect. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebFerrimagnetismCenteredSector.lean` |

---

## Authoritative supplemental implementation record (Theorem 10.6 discharge arc, PR-7: the centered-sector `Ô²` lower bound)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-3 section above (Issue #5347, PR #5355); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `gaugeSign_mul_sameSublattice` / `gaugeSign_mul_not_sameSublattice` | **De-privatized sublattice-gauge sign lemmas** (Theorem 10.6 discharge arc, Issue #5347, PR #5355 PR-7; **PROVED**, axiom-free): the two per-pair sign facts `ε_x ε_y = +1` (same sublattice) / `= −1` (across sublattices) that Theorem 10.5's proof already established are made non-`private` in `LiebRepulsiveCorrelation.lean` so this PR's sign step can call them directly instead of re-deriving them. No statement change. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveCorrelation.lean` |
| `vectorExpectation_sum` | **Additivity of the raw expectation over a `Finset` sum of observables** (Theorem 10.6 discharge arc, Issue #5347, PR #5355 PR-7; **PROVED**, axiom-free): `⟨v\| Σ_k O_k \|v⟩ = Σ_k ⟨v\|O_k\|v⟩`, from additivity of `Matrix.mulVec` and `dotProduct` in the observable. Added to `LiebRepulsiveCorrelation.lean` alongside the two de-privatized sign lemmas; used by this PR's sign step to distribute `vectorExpectation` across the `x, y` double sum. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveCorrelation.lean` |
| `liebRepulsive_centered_staggeredTransverse_ge_sum` / `liebRepulsive_centered_sum_transverse_eq` / `liebRepulsive_centered_ratioRe_ge_sq` / `liebRepulsive_exists_centered_ratioRe_ge_sq` | **Tasaki's ferrimagnetic bound (10.2.17) on the centered tower member** (Theorem 10.6 discharge arc, Issue #5347, PR #5355 PR-7; **PROVED**, axiom-free): establishes `S₀² ≤ ⟨(Ô_L)²⟩.re / ‖u‖²` (`S₀ = L/2`, `L = sublatticeImbalance A`) on the single centered tower member `u = (Ŝ⁻_tot)^{k₀} w` (`k₀ = L/2` in `ℕ` division) that PR-6 identified with Theorem 10.5's sector ground state, working entirely inside `u` (no sum over the tower). **The sign step** `liebRepulsive_centered_staggeredTransverse_ge_sum`: staggering the un-staggered transverse double sum by the gauge product `ε_x ε_y` can only raise its expectation, since `ε_x ε_y = +1` exactly where Theorem 10.5's sign pattern is positive (same sublattice) and `= −1` exactly where it is negative (different sublattices) — `⟨Σ_{x,y} Ŝ⊥_{xy}⟩.re ≤ ⟨(Ô_L)²_⊥⟩.re`. **The Casimir step** `liebRepulsive_centered_sum_transverse_eq`: rewriting the un-staggered double sum as `(Ŝ_tot)² − (Ŝ³_tot)²` (PR-2's `sum_fermionSpinTransverse_eq_totalSpinSquared_sub_spinZ_sq`), `u` is a joint eigenvector of the two operators — at PR-6's Casimir value `γ₀ = S₀(S₀+1)` (lowering stays in the ground submodule, `liebRepulsive_ground_spinMinusPow_mem`) and at the centered weight `m₀ = S₀ − k₀` (`fermionTotalSpinZ_mulVec_spinMinusPow_general`) — so the expectation is `(γ₀ − m₀²) ‖u‖²`. **The capstone** `liebRepulsive_centered_ratioRe_ge_sq`: restores the positive-semidefinite longitudinal square via PR-1's `fermionStaggeredTransverse_expectation_le_staggeredCasimir_expectation` and closes the arithmetic with `S₀² ≤ γ₀ − m₀²` (true because `m₀ = (L % 2)/2 ≤ 1/2`), giving `S₀² ≤ ⟨(Ô_L)²⟩.re / ‖u‖²`; `‖u‖² > 0` because the tower survives `k₀ ≤ L` steps (PR-5's `liebRepulsive_ground_tower_ne_zero`). **The existential form** `liebRepulsive_exists_centered_ratioRe_ge_sq`: supplies one highest-weight ground vector `w` (via PR-6's `liebRepulsive_exists_centered_transverse_sign`) carrying both the weight equation `Ŝ³_tot w = S₀ w` and the bound at its centered tower member, so Theorem 10.4's conclusions and Theorem 10.5's model hypotheses enter only here — the sign step and the capstone take the correlation-sign pattern as a hypothesis and are independent of how it was obtained. **Private supporting layer** (not listed above): `liebRepulsive_staggeredPair_re_le` (the per-pair sign step underlying `liebRepulsive_centered_staggeredTransverse_ge_sum`); `liebRepulsive_centeredCasimirGap_eq_ofReal` (realifies the Casimir gap `γ₀ − m₀²` as a cast of a real, crossing from the complex Casimir-step identity to the real capstone inequalities); `liebRepulsive_sq_le_centeredCasimirGap` (the arithmetic fact `S₀² ≤ γ₀ − m₀²`, via `L = 2(L/2) + L%2` and `(L%2)² ≤ L%2`). **Debt discharged.** Consumes PR-1's `fermionStaggeredTransverse_expectation_le_staggeredCasimir_expectation` (see the updated PR-1 row in [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/)), PR-2's `sum_fermionSpinTransverse_eq_totalSpinSquared_sub_spinZ_sq` (see the updated PR-2 row in [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/)), and PR-6's `liebRepulsive_exists_centered_transverse_sign` (see the updated PR-6 row above), all three previously staged at reference 0. **Also discharged (by PR-8).** `liebRepulsive_exists_centered_ratioRe_ge_sq` is consumed by PR-8's `liebFerrimagnetism_symmetric` into `theorem_10_6_lieb_ferrimagnetism` (Theorem 10.6 row, [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/), **PROVED**); the other three public declarations are consumed within this module in a straight-line chain (`liebRepulsive_centered_sum_transverse_eq` and `liebRepulsive_centered_staggeredTransverse_ge_sum` feed the capstone `liebRepulsive_centered_ratioRe_ge_sq`, which feeds the existential form). This PR bounds a single centered tower member; PR-8 universalizes the bound to an arbitrary normalized ground state via a ground-multiplet tower-transport argument. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebFerrimagnetismCenteredBound.lean` |

## Authoritative supplemental implementation record (Theorem 10.6 discharge arc, PR-8: universal assembly and axiom discharge)

This section is maintained by hand, lies outside the migrated catalogue block, and records the
final PR of the Theorem 10.6 discharge arc (Issue #5347, PR #5356), which discharges the axiom
itself (Theorem 10.6 row, [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/)); it is not
subject to the frozen byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `liebFerrimagnetism_tower_ratioRe_eq` / `liebFerrimagnetism_tower_ratioRe_ge_sq` | **Tower transport of the ferrimagnetic ratio** (Theorem 10.6 discharge arc, Issue #5347, PR #5356 PR-8; **PROVED**, axiom-free): `(Ô_L)²` commutes with `Ŝ⁺_tot` (PR-3's `fermionStaggeredCasimirOp_commute_fermionTotalSpinPlus`), so PR-4's ladder-ratio invariance makes the real Rayleigh quotient `⟨(Ô_L)²⟩.re / ‖·‖²` constant along the whole lowering tower `w_k = (Ŝ⁻_tot)^k w` (`liebFerrimagnetism_tower_ratioRe_eq`), and feeding it PR-7's centered-member bound spreads `S₀² ≤ ⟨(Ô_L)²⟩.re / ‖w_k‖²` to every `k ≤ L` (`liebFerrimagnetism_tower_ratioRe_ge_sq`). | `Fermion/JordanWigner/Hubbard/LiebFerrimagnetismDischarge.lean` |
| `liebFerrimagnetism_bound_of_mem_ground` | **The bound on every ground vector, not only the tower** (Theorem 10.6 discharge arc, Issue #5347, PR #5356 PR-8; **PROVED**, axiom-free): since PR-5's `liebRepulsive_ground_eq_span_tower` writes the ground submodule as the tower's span and `(Ô_L)²` commutes with `Ŝ³_tot` (PR-3's `fermionStaggeredCasimirOp_commute_fermionTotalSpinZ`), PR-5's `liebRepulsive_tower_crossTerm_eq_zero` kills the cross terms of both the numerator and the squared norm of an expansion `v = Σ_k c_k w_k`, diagonalizing both quadratic forms in the tower basis; averaging the per-member bound over the weights `\|c_k\|²` gives `S₀² ≤ ⟨v\|(Ô_L)²\|v⟩.re` for every normalized `v` of the ground submodule. | `Fermion/JordanWigner/Hubbard/LiebFerrimagnetismDischarge.lean` |
| `liebFerrimagnetism_symmetric` / `theorem_10_6_lieb_ferrimagnetism` | **Assembly and axiom discharge — Tasaki Theorem 10.6** (Theorem 10.6 discharge arc, Issue #5347, PR #5356 PR-8; **PROVED**, `#print axioms` = std3): `liebFerrimagnetism_symmetric` assembles the tower-transport and tower-expansion steps with Theorem 10.4 (which supplies the Casimir value and multiplet dimension at its own ground energy, identified with the caller's via the two-sided energy pinch `liebRepulsive_groundEnergy_eq_of_min`) and the separate single-site (`N = 0`) branch `liebFerrimagnetism_N_zero` (where `(Ô_L)²` is the plain Casimir, whose ground expectation `S₀(S₀+1)` exceeds `S₀²` outright since no tower exists). Splitting `IsLiebRepulsiveModel`'s Hamiltonian disjunction — the uniform form differs from the symmetric one only by the constant energy shift of `symmetricRepulsiveHubbardHamiltonian_groundSubmodule_eq_uniform` — gives the capstone `theorem_10_6_lieb_ferrimagnetism`, discharging the axiom: every normalized ground state of a repulsive-Hubbard model satisfying Theorem 10.4's hypotheses has `⟨v\|(Ô_L)²\|v⟩ ≥ ((\|A\|−\|B\|)/2)²`. Proved via Theorem 10.4 and Theorem 10.5 (inequality (10.2.7)), exactly as Theorem 4.4, and **not** by reflection positivity. **Deleted (this PR).** The reference-0 `fermionStaggeredCasimirOp_isHermitian` (PR-1 row, [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/)) was removed as a decorative declaration, since the capstone reads `⟨v\|(Ô_L)²\|v⟩` through `vectorExpectation`'s own real part rather than through a separate self-adjointness lemma. | `Fermion/JordanWigner/Hubbard/LiebFerrimagnetismDischarge.lean` |

## Authoritative supplemental implementation record (Theorem 10.8 discharge arc, PR-1: generic shift/uniqueness lemmas and the Shiba Hamiltonian bridge)

This section is maintained by hand, lies outside the migrated catalogue block, and records new
supporting declarations added after the migration baseline (Issue #5357, PR #5358); it is not
subject to the frozen byte-for-byte parity of the migrated block. New capacity is recorded here
rather than in [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/),
whose migrated-block byte budget is near its `HARD_BYTES` limit (`scripts/check_docs_hierarchy.py`).

| Lean name | Statement | File |
|---|---|---|
| `IsUniqueGroundStateOn.sub_smul_one` / `IsUniqueGroundStateOn.energy_eq` / `IsUniqueGroundStateOn.exists_smul_eq` | **Generic `IsUniqueGroundStateOn` shift and uniqueness-matching layer** (Theorem 10.8 discharge arc, Issue #5357, PR #5358 PR-1; **PROVED**, axiom-free): three model-independent additions to `Math/MatrixAnalysis/DegeneratePerturbation.lean`, alongside the pre-existing `IsUniqueGroundStateOn.smul_of_norm_one` (Lemma 10.1 layer, [part 3](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-03/)). `sub_smul_one`: shifting `H` by `(c:ℝ)•1` shifts a unique ground state's energy from `E` to `E − c` with the same eigenvector, by an eigen-equation `iff` rewrite. `energy_eq`: two unique ground states on the same `(K, H)` share the same ground energy, each one's minimality bounding the other's eigenvalue from both sides. `exists_smul_eq`: consequently they are collinear, `φ₂ = c • φ₁` with `‖c‖ = 1` — the two unique ground states of a Hamiltonian on a fixed subspace agree up to a phase. Generalizes the ad hoc energy-match/collinearity block Theorem 10.5's proof used to derive inline (see the retrofit note below). **Consolidation.** PR-11a's `isUniqueGroundStateOn_sub_smul_one_iff` ([part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/), `Math/MatrixAnalysis/SubmatrixGroundState.lean`) stated the same shift as an `iff` with its own proof; it keeps its statement and all three call sites, but its proof body is replaced by a short derivation from `sub_smul_one` (the reverse direction being the same one-sided shift run at `-a`), so a single proof of the shift now exists. Axiom-free. | `Math/MatrixAnalysis/DegeneratePerturbation.lean` + `Math/MatrixAnalysis/SubmatrixGroundState.lean` |
| `symmetricAttractiveHubbardHamiltonian_eq_attractive_sub_smul` / `shibaSignedUnitary_conj_symmetricRepulsive_eq_symmetricAttractive` / `hubbardKinetic_add_diagonal_half` | **The Shiba bridge onto the symmetric attractive Hamiltonian** (Tasaki §10.2.3, eq. (10.2.21), combined with §10.2.2 eq. (10.2.10)/(10.2.11); Theorem 10.8 discharge arc, Issue #5357, PR #5358 PR-1; **PROVED**, axiom-free): Theorem 10.8 is stated for the *symmetric* attractive Hamiltonian `Ĥ^{attr,sym}(T,U) = Ĥhop(T) − Σ_x U_x(n̂_{x↑}−½)(n̂_{x↓}−½)`, while the existing Shiba conjugation `shibaSignedUnitary_conj_symmetricRepulsive_eq_attractive` ([part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/), eq. (10.2.10)) delivers the *plain* attractive Hamiltonian with on-site energies shifted by `U/2`, minus the scalar `¼ Σ_x U_x`. `symmetricAttractiveHubbardHamiltonian_eq_attractive_sub_smul` reads eq. (10.2.11) in the attractive direction: `Ĥ^{attr,sym}(T,U) = Ĥ^{attr}(T + diag(U/2), U) − (¼ Σ_x U_x)•1` — the same constant, so the two shifts cancel. **Capstone** `shibaSignedUnitary_conj_symmetricRepulsive_eq_symmetricAttractive`: `Ûᴴ Ĥ^{rep,sym}(T,U) Û = Ĥ^{attr,sym}(T,U)` with **no** residual scalar. This is the conjugation identity only; reading it as unitary equivalence (spectra coincide, `Û` transports eigenvectors at unchanged energy) additionally takes `Û Ûᴴ = 1` (`shibaSignedUnitary_self_mul_conjTranspose`, `LiebRepulsiveShibaInteraction.lean`), a step left to the consuming PR. **Shared helper.** The chemical-shift split `Ĥhop(T + diag(U/2)) = Ĥhop(T) + Σ_x (U_x/2)(n̂_{x↑}+n̂_{x↓})` — needed identically by the plain conjugation (eq. (10.2.10)) and by this bridge — is factored out as the named lemma `hubbardKinetic_add_diagonal_half` in `LiebRepulsiveShibaConjugation.lean` and reused from both, in place of the previously inlined `hubbardKinetic_add`/`hubbardKinetic_diagonal` block. **Not yet consumed** by `theorem_10_8_lieb_shen_qiu_superconductivity` (still an axiom, [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/)); staged for the later PRs of this arc (Ŝ³φ=0 extraction, PR-2 onward). **PR #5359 PR-2 has since consumed the capstone** `shibaSignedUnitary_conj_symmetricRepulsive_eq_symmetricAttractive` as the conjugation hypothesis of `shibaTransport_uniqueGroundStateOn_spinZSector_symmetricAttractive` (see the PR-2 row below), and with it `symmetricAttractiveHubbardHamiltonian_eq_attractive_sub_smul`, on which its proof rests; the eigenvector-transport step deferred above is performed there by `IsUniqueGroundStateOn.conj_unitary`, which supplies the `Û Ûᴴ = 1` it needs. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebShenQiuShibaBridge.lean` + `Fermion/JordanWigner/Hubbard/LiebRepulsiveShibaConjugation.lean` |
| `euclideanExpectation_smul` / `euclideanExpectation_add` / `euclideanExpectation_shiba_conj` / `euclideanExpectation_conjTranspose_mul_self` | **De-privatized `euclideanExpectation` helper layer, moved to sit next to its own definition** (Theorem 10.8 discharge arc, Issue #5357, PR #5358 PR-1; **PROVED**, axiom-free; no statement change). These four helpers (homogeneity, additivity, Shiba-conjugation transport, and the `⟨v\|Aᴴ A\|v⟩` = squared-norm identity) were `private` in `LiebRepulsiveCorrelation.lean:111-147` ([part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/), Theorem 10.5 row); this PR moves them, unchanged, to `LiebAttractive.lean` next to `euclideanExpectation` itself and makes them public, so the later Theorem 10.8 layers can reuse them across the module boundary instead of re-deriving them. **Retrofit.** Theorem 10.5's proof (`theorem_10_5_shen_qiu_tian_transverse_sign`, [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/)) is retargeted in the same PR: its inline energy-match/collinearity block (deriving `E = Elem` by a two-sided minimality bound and then `φ = c•ψ`, `‖c‖=1`, by hand) now calls this PR's generic `IsUniqueGroundStateOn.exists_smul_eq` directly — no statement or citation change to the Theorem 10.5 row itself, only its proof term. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebAttractive.lean` |

## Authoritative supplemental implementation record (Theorem 10.8 discharge arc, PR-2: the `Ŝ³φ = 0` extraction and the reusable Shiba transport)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-1 section above (Issue #5357, PR #5359); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `IsUniqueGroundStateOn.conj_unitary` / `fermionTotalSpinZ_mulVec_eq_zero_of_fermionTotalSpinSquared_mulVec_eq_zero` / `shibaTransport_uniqueGroundStateOn_spinZSector_of_conj` / `shibaTransport_uniqueGroundStateOn_spinZSector` / `shibaTransport_uniqueGroundStateOn_spinZSector_symmetricAttractive` | **The `Ŝ³φ = 0` extraction and the reusable Shiba transport** (Tasaki §10.2.2, eqs. (10.2.10)/(10.2.11), pp. 350–352, and §10.2.3, eq. (10.2.21), p. 359; Theorem 10.8 discharge arc, Issue #5357, PR #5359 PR-2; **PROVED**, `#print axioms` = std3). **Generic layer.** `IsUniqueGroundStateOn.conj_unitary` (new file `Math/MatrixAnalysis/UnitaryGroundTransport.lean`) states unitary sector transport once and for all: for `U` with `U Uᴴ = 1` intertwining two Hamiltonians, `Uᴴ H' U = H`, and mapping a subspace `K` into `K'` while `Uᴴ` maps `K'` back into `K`, the unique normalized ground state of `H` on `K` is carried by `U` to the unique normalized ground state of `H'` on `K'` at the **same** energy. Both membership directions are hypotheses and both are used — the forward one places the transported vector in `K'`, the backward one pulls a competing `K'`-eigenvector into `K` where the minimality and uniqueness clauses live; the companion unitarity identity `Uᴴ U = 1` needed for norm preservation is *not* a hypothesis, being derivable on a square matrix (`mul_eq_one_comm`); a constant energy offset is deliberately not absorbed here but removed beforehand with `IsUniqueGroundStateOn.sub_smul_one` (PR-1). **Spin-algebra layer.** `fermionTotalSpinZ_mulVec_eq_zero_of_fermionTotalSpinSquared_mulVec_eq_zero` (`LiebAttractiveFullSectorUnique.lean`, next to `fermionTotalSpinSquared_eq_cartesianSqSum`): a null vector of the fermionic Casimir `Ŝ²` is a null vector of `Ŝ³`, since the Cartesian square-sum form of `Ŝ²` makes the vanishing Casimir expectation force each Hermitian component to annihilate the vector (Tasaki Lemma A.11 route, `mulVec_eq_zero_of_sq_sum_inner_zero`). Previously an inline block inside Theorem 10.4's capstone proof; extracted, not copied. **Hubbard transport.** `shibaTransport_uniqueGroundStateOn_spinZSector_of_conj` (`LiebRepulsiveBalancedGround.lean`) is the shared Shiba transport, parametrized by the conjugation identity of the model at hand: from `Ûᴴ Ĥ^{rep} Û = Ĥ` and a number-sector unique ground state `φ` of `Ĥ` at energy `E` with the singlet property `Ŝ² φ = 0`, it produces `ψ = Û φ` as the unique ground state of `Ĥ^{rep}` on the spin-`z` sector `Ŝ³ = (Ne−(N+1))/2` at the **same** energy, together with the half-filling number eigenvalue `N̂ ψ = (N+1)·ψ` (which is `N+1`, **not** `Ne`; dropping `Ŝ³ φ = 0` would make it false). Everything model-specific — the conjugation constant — is thereby confined to the hypothesis, and the two uses of the transport are its two instances. **§10.2.2 face.** `shibaTransport_uniqueGroundStateOn_spinZSector` instantiates it at eq. (10.2.10), whose leftover `¼ Σ_x U_x` is moved onto the source Hamiltonian by `IsUniqueGroundStateOn.sub_smul_one`, giving the plain-attractive source `Ĥ^{attr}(T + diag(U/2), U)` and output energy `E − ¼ Σ_x U_x`; stated in the plain-attractive form so that the §10.2.2 module keeps no dependence on the Theorem 10.8 statement layer. **Theorem 10.8 face.** `shibaTransport_uniqueGroundStateOn_spinZSector_symmetricAttractive` (new file `LiebShenQiuShibaTransport.lean`) instantiates the same transport at PR-1's residual-free bridge `shibaSignedUnitary_conj_symmetricRepulsive_eq_symmetricAttractive`, so the symmetric-attractive source `Ĥ^{attr,sym}(T,U)` transports at **unchanged** energy `E`; no constant is cancelled twice and the bridge is consumed directly. **Refactor (no statement change).** Theorem 10.4's capstone `repulsiveSpinZSector_ground_unique` ([part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/)) keeps its statement byte-identical (same binders, same conjunct order — its three positional call sites in `LiebRepulsiveCorrelation.lean`, `LiebFerrimagnetismCenteredSector.lean` and `LiebRepulsiveSectorBridgeFinal.lean` compile untouched, and a `Tests` pin restates the full existential type to freeze it) and becomes the composition of Theorems 10.2/10.3 with the new transport lemma, its proof shrinking from ~225 lines to ~20. **Debt (new).** `shibaTransport_uniqueGroundStateOn_spinZSector_symmetricAttractive` has no proof-level consumer yet — it is the sole deliverable the later PRs of this arc consume, and is covered here by a `Tests` signature pin. Axiom-free. | `Math/MatrixAnalysis/UnitaryGroundTransport.lean` + `Fermion/JordanWigner/Hubbard/LiebAttractiveFullSectorUnique.lean` + `Fermion/JordanWigner/Hubbard/LiebRepulsiveBalancedGround.lean` + `Fermion/JordanWigner/Hubbard/LiebShenQiuShibaTransport.lean` |

## Authoritative supplemental implementation record (Theorem 10.8 discharge arc, PR-3: sector generalization and the Casimir value on the transported ground state)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-1 section above (Issue #5357, PR #5360); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `liebRepulsive_centeredWeight_eq` | **The centered-weight arithmetic, made public** (Theorem 10.8 discharge arc, Issue #5357, PR #5360 PR-3; **PROVED**, axiom-free; no statement change). This lemma of the Theorem 10.6 arc's PR-6 ([row above](#authoritative-supplemental-implementation-record-theorem-106-discharge-arc-pr-6-centered-sector-to-theorem-105-bridge)) is made non-`private` in the same file so it can supply the `hkm` weight-match hypothesis of the now-generalized `liebRepulsive_sectorGroundEnergy_eq_groundEnergy` (below) from outside the module, exactly as it already did for the centered instantiation inside it. | `Fermion/JordanWigner/Hubbard/LiebFerrimagnetismCenteredSector.lean` |
| `liebRepulsive_sectorGroundEnergy_eq_groundEnergy` (generalized) | **The sector-ground-energy pinch, generalized from the centered exponent to an arbitrary tower exponent** (Theorem 10.6 arc's PR-6 lemma, retrofitted by the Theorem 10.8 discharge arc, Issue #5357, PR #5360 PR-3; **PROVED**, axiom-free; statement generalized, no proof-strategy change). Previously hard-coded at the centered exponent `k₀ = L/2`, the lemma now takes an explicit tower exponent `k` (`hk : k ≤ sublatticeImbalance A`) and an implicit sector weight `m` matched by a hypothesis `hkm : L/2 − k = m`, so a caller can present the sector at whatever weight its own electron-number bookkeeping produces, supplying `hkm` via its own arithmetic (e.g. `liebShenQiu_towerExponent_weight_eq` below) instead of being forced through the centered exponent. The private membership helper `liebRepulsive_centered_mem_spinZSector` is renamed `liebRepulsive_tower_mem_spinZSector` and takes the same `k`/`hkm` generalization. The Theorem 10.6 arc's own centered use (`liebRepulsive_centered_eq_smul_sectorGround`, PR-6 row above) instantiates the generalized lemma at `k = k₀ = L/2` via `liebRepulsive_centeredWeight_eq`, so its conclusion is unchanged. | `Fermion/JordanWigner/Hubbard/LiebFerrimagnetismCenteredSector.lean` |
| `liebShenQiu_towerExponent_weight_eq` / `liebShenQiu_sectorGround_mem_halfFillingGround` / `liebShenQiu_casimir_eq` | **The transported sector ground state carries Theorem 10.4's Casimir value** (Tasaki §10.2.2 (Theorem 10.4, p. 350) and §10.2.3 (Theorem 10.8, p. 359, eqs. (10.2.21)/(10.2.22)); Theorem 10.8 discharge arc, Issue #5357, PR #5360 PR-3; **PROVED**, axiom-free). New file `LiebShenQiuSectorCasimir.lean`. The Shiba transport of §10.2.3 delivers a unique ground state `ψ` of the symmetric repulsive model on the spin-`z` sector `Ŝ³ = (Ne − (N+1))/2` at half filling (`Ne` even, `2\|B\| ≤ Ne ≤ 2\|A\|`); Theorem 10.8's superconducting bound needs `ψ`'s total spin, which is what Theorem 10.4 fixes for the half-filling ground multiplet (`Ŝ² = S₀(S₀+1)`, `S₀ = L/2`, `L := sublatticeImbalance A`). `liebShenQiu_towerExponent_weight_eq`: the tower exponent `k = \|A\| − Ne/2` reaches exactly that sector, `L/2 − k = (Ne − (N+1))/2`, a linear-arithmetic identity from `\|A\| + \|B\| = N + 1` and `Even Ne`. `liebShenQiu_sectorGround_mem_halfFillingGround`: feeding that weight match to the (now-generalized) two-sided pinch `liebRepulsive_sectorGroundEnergy_eq_groundEnergy` identifies the sector ground energy with the half-filling ground energy `E₀`, placing `ψ` inside the `(N+1)`-electron `E₀`-ground submodule. `liebShenQiu_casimir_eq`: assembling that membership with Theorem 10.4's Casimir clause (`liebRepulsive_symmetric_halfFilling`) and a highest-weight ground vector (`liebRepulsive_ground_exists_topWeight`) gives `Ŝ²_tot ψ = S₀(S₀+1) ψ`. Theorem 10.5 is **not** used here: only Theorem 10.4's ground-multiplet data and the highest-weight tower enter directly at this layer (Theorem 10.4's own proof pulls in `repulsiveSpinZSector_ground_unique`, Theorem 10.2, and Theorem 10.3), so the transverse-correlation layer of §10.2.2 stays out of this route. **Debt (new).** `liebShenQiu_towerExponent_weight_eq` and `liebShenQiu_casimir_eq` are at reference 0 outside the Tests signature pin; `liebShenQiu_sectorGround_mem_halfFillingGround` is consumed by `liebShenQiu_casimir_eq`. None is yet consumed by `theorem_10_8_lieb_shen_qiu_superconductivity` (still an axiom, [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/)): they supply the arc's spin-value identification, staged for a later PR that assembles this Casimir value with the Shiba transport (PR-1/PR-2 rows above) into the superconducting bound itself. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebShenQiuSectorCasimir.lean` |

**Note on [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/)'s frozen Theorem 10.8 row.** That row's "Reflection positivity + Theorem 10.2 uniqueness → faithful documented axiom" phrasing is superseded by this PR (Issue #5357): Theorem 10.8 is an axiom pending discharge (Issue #5357), matching `LiebShenQiu.lean` and `tex/proof-guide.tex`. The part-4 wording is unchanged here because it sits inside that file's migrated, byte-frozen catalogue block (`scripts/check_docs_hierarchy.py`).

---

## Authoritative supplemental implementation record (Theorem 10.8 discharge arc, PR-4: pair operator and ladder algebra)

This section is maintained by hand, lies outside the migrated catalogue block, and continues the
table started in the PR-1 section above (Issue #5357, PR #5361); it is not subject to the frozen
byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `totalPairCreationOperator_eq_conjTranspose` / `totalPairCorrelationOperator_eq_sum` / `fermionTotalSpinPlusMinus_eq_sum` / `shiba_spinPlusMinus_expectation_eq_signed_sum` / `signed_sum_le_sum_of_pos` / `liebShenQiuPairLowerBound_le_casimir_gap` / `liebShenQiu_towerExponent_le_sublatticeImbalance` | **The pair operator, its Shiba-transported ladder expectation, and the real-arithmetic step of eq. (10.2.22)** (Tasaki §10.2.3, Theorem 10.8, eqs. (10.2.13)/(10.2.22)/(10.2.25); Theorem 10.8 discharge arc, Issue #5357, PR #5361 PR-4; **PROVED**, axiom-free). New file `LiebShenQiuPairAlgebra.lean`. `totalPairCreationOperator_eq_conjTranspose` (`b̂† = b̂ᴴ`) and `totalPairCorrelationOperator_eq_sum`/`fermionTotalSpinPlusMinus_eq_sum` expand `b̂†b̂` and `Ŝ⁺_tot Ŝ⁻_tot` into the matching double sums `Σ_{x,y} hubbardPairCorrelationOp N x y` and `Σ_{x,y} Ŝ⁺_x Ŝ⁻_y`. `shiba_spinPlusMinus_expectation_eq_signed_sum` transports the ladder expectation through the Shiba unitary into the sublattice-signed pair-correlation sum `Σ_{x,y} ε_xε_y ⟨φ|pair x y|φ⟩` of eq. (10.2.13). `signed_sum_le_sum_of_pos` is the term-wise sign bound `ε_xε_y·p ≤ p` (`p > 0`) turning the signed sum into the plain one; unlike Tasaki's route through Theorem 10.5's correlation sign, this development derives the sign directly from the Shiba identity plus Theorem 10.3's strict positivity, so Theorem 10.5 is not invoked. `liebShenQiuPairLowerBound_le_casimir_gap` is the real-arithmetic inequality `(|A|−Ne/2)(Ne/2−|B|) ≤ S₀(S₀+1) − m(m−1)` (`S₀ = ||A|−|B||/2`, `m = (Ne−(N+1))/2`) under `2|B| ≤ Ne ≤ 2|A|`. `liebShenQiu_towerExponent_le_sublatticeImbalance` discharges PR-3's tower-exponent hypothesis `hk` of `liebShenQiu_sectorGround_mem_halfFillingGround` at `k = |A| − Ne/2`, under the same two-sided bound. **Shared extraction.** `fermionSitePairCreation_conjTranspose` and `euclideanExpectation_sum`, added to `LiebAttractive.lean` next to the pre-existing `euclideanExpectation` helpers, replace Theorem 10.5's inline `hPadj` block (`LiebRepulsiveCorrelation.lean`) and supply this file's own expectation-distribution step; both are consumed immediately (the former by the retargeted Theorem 10.5 proof, the latter within this file itself), so neither is new debt. **Debt (new).** All seven listed declarations are at reference 0 outside the `Tests` signature pin (`Tests/LiebShenQiuPairAlgebra.lean`); staged for PR-5's assembly of the superconducting bound itself (`theorem_10_8_lieb_shen_qiu_superconductivity`, still an axiom, [part 4](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/)). Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebShenQiuPairAlgebra.lean` + `Fermion/JordanWigner/Hubbard/LiebAttractive.lean` |

---

[← Multi-mode fermion via Jordan–Wigner (P2 backbone)](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/) · [Catalogue](/lattice-system/formalization/legacy/) · End →
