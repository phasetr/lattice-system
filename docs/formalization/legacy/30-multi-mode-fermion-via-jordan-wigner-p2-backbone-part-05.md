---
layout: page
title: "Legacy catalogue: Multi-mode fermion via Jordan–Wigner (P2 backbone) (part 5 of 9)"
permalink: /formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-05/
---

<a id="legacy-catalogue-multi-mode-fermion-via-jordanwigner-p2-backbone-part-5-of-5"></a>
# Legacy catalogue: Multi-mode fermion via Jordan–Wigner (P2 backbone) (part 5 of 9)

> **Interim authority.** This lossless catalogue chunk remains authoritative for formalization status and capstone identification until Issue #5228. The version 1 JSON catalogue is still a non-authoritative prototype.

[Interim catalogue](/lattice-system/formalization/legacy/) › [Fermions and Hubbard models](/lattice-system/formalization/legacy/#group-fermions-hubbard)

## Authoritative supplemental implementation record (§10.2.2 eq. (10.2.9) uniform/symmetric ground-submodule reduction)

This section is maintained by hand, lies outside the migrated catalogue block, and records
a new supporting theorem (the eq. (10.2.9) reduction step) added after the migration baseline; it
is not subject to the frozen byte-for-byte parity of the migrated block.

| Lean name | Statement | File |
|---|---|---|
| `symmetricRepulsiveHubbardHamiltonian_groundSubmodule_eq_uniform` | **Eq. (10.2.9): symmetric-interaction ground submodule reduces to the uniform-interaction one** (Tasaki, 1st ed. (2020), §10.2.2, p. 351, eq. (10.2.9); **PROVED**, `#print axioms` = std3, Issue #5320, PR #5321 PR-1): per site, `U(n̂↑−½)(n̂↓−½) = U n̂↑n̂↓ − (U/2)(n̂↑+n̂↓) + U/4`, so summing over all `N+1` sites gives `Ĥint^{sym}(U) = Ĥint^{unif}(U) − (U/2)N̂ + (U/4)|Λ|`, and hence, restricted to the `N̂ = Ne` electron-number sector, `Ĥ^{rep,sym}(U) = Ĥ^{rep,unif}(U) + c·1` with the scalar shift `c = −(U/2)Ne + (U/4)(N+1)`. Consequently, for every target energy `E`, the `E`-ground submodule of the symmetric-interaction Hamiltonian on the `Ne`-electron sector equals the `(E−c)`-ground submodule of the uniform-interaction Hamiltonian on the same sector — the two variants have the same ground submodule up to the constant energy shift `c`, supplying the form conversion (10.2.6) → (10.2.5) **at a constant `U`**. Scope: `hubbardGroundSubmoduleAtElectronNumber` is the `E`-eigenspace intersected with the `Ne`-electron sector and carries **no** minimality of `E`, so what this transports between the two variants is the eigenspace equality alone; transporting ground-state minimality (that the shifted `E − c` is the least sector eigenvalue) is a separate step. Axiom-free; finite-dimensional linear algebra (per-site algebraic identity + `Finset.sum` bookkeeping), no operator-algebra dependency. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveBalancedGround.lean` |
| `minEnergyOn` / `minEnergyOn_isGroundEigenvalueOn` / `abs_minEnergyOn_sub_le_sum_entryNorms` / `Continuous.minEnergyOn_comp` | **Minimum energy on a subspace** (generic infrastructure for the Theorem 10.4 discharge arc, Issue #5320, PR #5322 PR-2; **PROVED**, `#print axioms` = std3): `minEnergyOn W H = inf {re⟪v,Hv⟫ \| v∈W, ‖v‖=1}` restricts the whole-space `hermitianMinEigenvalue` variational functional (`Quantum/SpinS/SubmatrixMinEigenvalue.lean`; Lipschitz and continuity properties in `HermitianMinEigenvalueLipschitz.lean` / `HermitianMinEigenvalueContinuous.lean`) to unit vectors of a fixed nonzero subspace `W`. `minEnergyOn_isGroundEigenvalueOn` identifies it with the ground eigenvalue produced by `exists_isGroundEigenvalueOn` (`DegeneratePerturbationGroundEnergy.lean`), the `≥` half being that file's variational sharpness bound `IsGroundEigenvalueOn.mul_norm_sq_le`; the entry-norm Lipschitz bound and its parameter-continuity corollary mirror `abs_hermitianMinEigenvalue_sub_le_sum_entryNorms` / `Continuous.hermitianMinEigenvalue_comp`. Not itself a direct formalization of a numbered Tasaki statement — a reusable building block feeding the continuity arguments of Theorem 10.4's proof. Axiom-free. | `Math/MatrixAnalysis/MinEnergyOnSubspace.lean` |
| `numberSpinZSectorEuclidean` / `numberSpinZCasimirSectorEuclidean` / `numberSpinZSectorEuclidean_mem_of_commute` / `numberSpinZCasimirSectorEuclidean_mem_of_commute` / `exists_unique_casimir_sector_strict_min` | **Casimir sector machinery** (generic infrastructure for the Theorem 10.4 discharge arc, Issue #5320, PR #5324 PR-3; **PROVED**, `#print axioms` = std3): `numberSpinZSectorEuclidean N L m₀` is the joint sector `K = {N̂ = L} ⊓ {Ŝ³ = m₀}` (refining `spinZSectorEuclidean` by also fixing the total number `N̂`), and `numberSpinZCasimirSectorEuclidean N L m₀ c` further refines it by the total-spin Casimir eigenvalue, `K_c = K ⊓ ker(Ŝ² − c)`. **Proposition 1** (`numberSpinZSectorEuclidean_mem_of_commute`, `numberSpinZCasimirSectorEuclidean_mem_of_commute`): any Hamiltonian commuting with `N̂`, `Ŝ³` (and, for `K_c`, also `Ŝ²`) preserves `K` and `K_c`. **Proposition 2** (`exists_unique_casimir_sector_strict_min`): if `H` is Hermitian, commutes with `N̂`, `Ŝ³`, `Ŝ²`, and has a *unique* normalized ground state on `K`, then there is an occupied Casimir sector `K_c` (`K_c ≠ ⊥`) attaining the ground energy, unique among occupied sectors — every other occupied sector `K_{c'}` (`c' ≠ c`, `K_{c'} ≠ ⊥`) is strictly higher. Unoccupied sectors (`K_{c'} = ⊥`) are excluded on both sides, since `minEnergyOn ⊥ H` is a junk value. Not itself a direct formalization of a numbered Tasaki statement — sector-invariance/unique-minimality infrastructure that the later homotopy/perturbation PRs of the arc (PR-4 onward) use to locate the one Casimir eigenvalue the transported ground state occupies. **PR-13b extends** the conclusion of `exists_unique_casimir_sector_strict_min` with the membership witness `φ ∈ K_c` (needed by the selector-uniqueness argument across several couplings, `LiebRepulsiveCasimirPinning.lean`); of the three consumers, `liebRepulsive_exists_unique_casimir_sector` (`LiebRepulsiveSectorBridgeFinal.lean`, call site in the same file) discards the new component via `-`, while `LiebRepulsiveCasimirPinning.lean:349` and `LiebRepulsiveMultipletCompanion.lean:198` both use the membership witness. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveCasimirSector.lean` |
| `mulVec_mem_eigenspace_of_commute` / `toEuclideanLin_mem_eigenspace_of_commute` | **A commuting matrix preserves every eigenspace** (generic matrix analysis, Issue #5320, PR #5324 PR-3; **PROVED**, `#print axioms` = std3): from `B x = e x` and `A B = B A` one gets `B (A x) = A (B x) = e (A x)`, so `A` maps each `B`-eigenspace into itself. Stated for both carriers of the finite-dimensional matrix action — the function carrier `n → ℂ` acting by `Matrix.mulVecLin`, and the inner-product carrier `EuclideanSpace ℂ n` acting by `Matrix.toEuclideanLin`, the latter a transport of the former along `WithLp.ofLp` (which intertwines the two actions definitionally). Single home for the eigenspace-invariance step used by the full-`Ne`-sector weight decomposition (`Fermion/JordanWigner/Hubbard/LiebAttractiveFullSectorUnique.lean`) and by the Casimir sector invariance above. Axiom-free. | `Math/MatrixAnalysis/CommutingEigenspaceInvariance.lean` |
| `homotopyHamiltonian` / `homotopyHopping` / `liebEndpointHopping` / `homotopyOnSite` / `homotopyHamiltonian_zero` / `continuous_homotopyHamiltonian` / `continuous_minEnergyOn_homotopyHamiltonian` / `casimirSelector_eq_const_of_locally_unique_strict_min` | **Homotopy continuity and local-constancy for the deformation argument** (generic infrastructure for the Theorem 10.4 discharge arc, Issue #5320, PR #5325 PR-4; **PROVED**, `#print axioms` = std3): sets up Tasaki's coupling deformation `H_s` (p. 353) — the convex-combination hopping homotopy `T_s = (1-s)T + sT_1` (`homotopyHopping`) toward an explicit uniform `±λ` endpoint (`liebEndpointHopping`) and the matching on-site homotopy `U_s = (1-s)U + s` (`homotopyOnSite`), assembled into `H_s` (`homotopyHamiltonian`) via the uniform repulsive Hubbard Hamiltonian — together with the following algebraic/analytic lemmas: the base point `H_0 = Ĥ^{rep,unif}(T, U)` (`homotopyHamiltonian_zero`), sign preservation on the original support and the value `sλ` on added `A`-`B` edges, vanishing of same-sublattice entries, symmetry/bipartition/connectivity preservation along the homotopy (connectivity needs the strict endpoint hypothesis `0 < λ`, not merely `λ ≠ 0`, since a negative `λ` can cancel the original entry at an interior `s`), positivity of `U_s`, and continuity of `s ↦ H_s` (transported to continuity of the sector-restricted minimum energy via PR-2's `Continuous.minEnergyOn_comp`). The topological core, `casimirSelector_eq_const_of_locally_unique_strict_min`, upgrades PR-3's local (pointwise-in-`s`) unique-occupied-Casimir-sector conclusion to a single sector occupied for *every* `s ∈ [0, 1]` — the original model at `s = 0` included — via `IsLocallyConstant.apply_eq_of_preconnectedSpace` on the preconnected set `[0, 1]`; it assumes continuity of the sector minimum energy only at *occupied* sectors (matching what `continuous_minEnergyOn_homotopyHamiltonian` supplies) and assumes no continuity of the ground energy `E`, which is recovered from the fixed-sector continuity through the strict-minimality hypothesis. **Scope.** `homotopyOnSite` is a scalar, so `H_s` deforms the **uniform** Hamiltonian (eq. (10.2.5), one `U > 0`), not the site-dependent symmetric form (eq. (10.2.6)). What PR-1's `symmetricRepulsiveHubbardHamiltonian_groundSubmodule_eq_uniform` (eq. (10.2.9)) supplies is the form conversion (10.2.6) → (10.2.5) **at a constant `U`**: its coupling argument is a scalar `U : ℝ` and it compares `symmetricRepulsiveHubbardHamiltonian N T (fun _ => U)` with `repulsiveHubbardHamiltonian N T U`, so a non-constant `U_x` never occurs in it, and what it transports is the eigenspace equality alone (no minimality in either direction). **Open obligation (Issue #5320).** The site-dependent reduction `U_x → U` — Tasaki's own first deformation step, "For each `x ∈ Λ`, we change `U_x` to a common constant `U > 0`" (p. 353) — is therefore discharged nowhere yet: expanding the symmetric form with a genuine `U : Λ → ℝ` gives `Σ_x U_x n̂_{x,↑} n̂_{x,↓} − (1/2) Σ_x U_x (n̂_{x,↑} + n̂_{x,↓}) + (1/4) Σ_x U_x`, whose middle term is a site-dependent one-body potential rather than a multiple of the total number `N̂`, so PR-1's constant-energy-shift argument is inapplicable in principle, and this file cannot supply the step either (`homotopyOnSite : ℝ → ℝ → ℝ` is a scalar by definition). The second disjunct of `IsLiebRepulsiveHamiltonian` (the site-dependent symmetric form) thus remains unreduced, and the arc's final assembly of Theorem 10.4 (PR-12/PR-13) must either discharge it or restrict the theorem's hypothesis accordingly. This file starts from a uniform model, and what is deformed here is `U → 1`. **Update (PR-14b).** The arc's actual capstone route bypasses this reduction entirely: `liebRepulsive_symmetric_halfFilling_conditional` (`LiebRepulsiveWeightConfinement.lean`) proves the Theorem 10.4 conclusion directly for a general site-dependent `U : Fin (N+1) → ℝ` symmetric Hamiltonian (only `∀ x, 0 < U x` is assumed), so the `U_x → U` reduction sketched above is never needed by the arc; it remains an observation about the book's own stated deformation argument, not a blocking gap for the arc's discharge (**completed by PR-15c**). **Provenance.** Tasaki's endpoint keeps the original bond set — "We do not modify `t_{x,y}` which is zero to begin with" (p. 353) — whereas `liebEndpointHopping` carries `sign(T x y) · λ` on the original support and `λ` on every *previously vanishing* `A`-`B` pair, completing the support to the full bipartite graph on `A ⊔ B`. That is a deliberate strengthening of the arc's design (the same "independent-route replacement" pattern as the Lemma 10.1 discharge, #5313), not a formalization of the book's stated deformation; it is harmless for what the arc consumes (the endpoint stays symmetric, bipartite and connected), its one visible consequence being that the `λ → 0` effective Heisenberg model (eq. (10.2.12)) sits on the complete bipartite graph rather than on the original bond set `{ {x, y} | t_{x,y} ≠ 0 }` — both bipartite for the same `A ⊔ B` and connected, hence the same Lieb–Mattis total spin `S₀ = ||A| − |B||/2`. Not itself a direct formalization of a numbered Tasaki statement — the homotopy-independent topological machinery that the concrete endpoint identification (PR-5 onward) reuses verbatim. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveHomotopyContinuity.lean` |
| `kernelProjectionMatrix_eq_of_fixes_kernel` / `kernelProjectionMatrix_diagonal` / `configSectorCompress_apply` / `liebHalfFillingPred` / `liebPerturbationH0` / `liebPerturbationV` / `homotopyHamiltonian_one_eq_perturbedHamiltonian` / `liebPerturbationH0_posSemidef` / `liebPerturbationH0_isReducedInverse` / `kernelProjectionMatrix_liebPerturbationH0_eq_hardcoreProjection` / `liebPerturbationH0Compressed` / `liebPerturbationVCompressed` / `liebPerturbationH0Compressed_posSemidef` / `liebPerturbationVCompressed_isHermitian` / `kernelProjectionMatrix_liebPerturbationH0Compressed_eq_diagonal` / `kernelProjection_mul_liebPerturbationVCompressed_mul_kernelProjection` | **Perturbation setup at the homotopy endpoint (Theorem 10.4 arc, Issue #5320, PR #5326 PR-5; PROVED, `#print axioms` = std3): not itself a direct formalization of a numbered Tasaki statement — generic degenerate-perturbation-theory infrastructure that sets up the `λ → 0` deformation of §10.2.2 (p. 353) as an instance of Tasaki Lemma 10.1's `Ĥ(λ) = Ĥ₀ + λV̂` contract (`tasaki_lemma_10_1_degenerate_perturbation`, discharged independently in issue #5313).** Generic layer (`Math/MatrixAnalysis/DegeneratePerturbation.lean`, `Math/MatrixAnalysis/MinEnergyOnSubspace.lean`): `kernelProjectionMatrix_eq_of_fixes_kernel` — a Hermitian matrix that maps into `ker H` and fixes every kernel vector *is* the orthogonal projection onto `ker H`; `kernelProjectionMatrix_diagonal` — the kernel projection of a diagonal matrix is the diagonal indicator of its zero entries; `minEnergyOn_add_const_smul_one` — shifting a Hamiltonian by `c • 1` shifts `minEnergyOn` by the same constant `c` (normalises the energy origin before comparing with the second-order effective Hamiltonian). Model-specific layer (`Fermion/JordanWigner/Hubbard/HubbardImpossibilityLowUVariationalCore.lean`, `LiebRepulsivePerturbationSetup.lean`, new file): `configSectorCompress_apply` — the `(s, s')` entry of the sector-compressed matrix `Tᴴ A T` is the Fock-space matrix element `A_{s,s'}`; `liebPerturbationH0`/`liebPerturbationV` — the on-site interaction `Ĥ₀ = Σ_x n̂_{x↑}n̂_{x↓}` (`U = 1`) and the unit-coupling endpoint hopping `V̂`, related to `H_s` at `s = 1` by `homotopyHamiltonian_one_eq_perturbedHamiltonian`; `liebPerturbationH0_posSemidef`, and the diagonal/kernel identification `kernelProjectionMatrix_liebPerturbationH0_eq_hardcoreProjection` (`ker Ĥ₀` = the hard-core subspace) together with the explicit reduced inverse `liebPerturbationH0_isReducedInverse`. `liebHalfFillingPred` is the half-filled, fixed-`Ŝ³` configuration-basis sector `K`; `liebPerturbationH0Compressed`/`liebPerturbationVCompressed` are the compressions of `Ĥ₀`, `V̂` to `K` (PSD and Hermiticity survive compression). **The capstone `kernelProjection_mul_liebPerturbationVCompressed_mul_kernelProjection` proves `P̂₀ V̂ P̂₀ = 0` only inside the compressed sector `K`, under the hypothesis that the hopping matrix respects the bipartition — this statement is FALSE on the whole Fock space** (a hard-core configuration with an empty site can absorb a hopping electron and stay hard-core, so `P̂₀ V̂ P̂₀ ≠ 0` there), so the half-filling restriction is essential, not cosmetic. What the proof uses of the hopping matrix is only the vanishing of the endpoint diagonal (`liebEndpointHopping_diag_eq_zero`), which removes the on-site number-operator part of `V̂`; bipartiteness is a sufficient condition for that, not a necessary one — a non-bipartite hopping matrix with vanishing diagonal gives the same first-order vanishing on the half-filled sector. **Left for later PRs of the arc (status after PR-6, Issue #5320, PR #5327)**, all on the compressed sector `K`: (a) the compressed `IsReducedInverse` — **discharged** by PR-6's `liebPerturbationH0Compressed_isReducedInverse` (`LiebRepulsiveSuperexchangeReducedInverse.lean`), the bridge from this `Ĥeff = −P̂₀V̂Ĥ₀⁻¹V̂P̂₀` setup to the PR-3 `K`-definition; (b) the compressed `perturbedHamiltonian` bridge `Ĥ_{s=1}(λ)|_K = Ĥ₀|_K + λ V̂|_K` — **discharged** by PR-6's `homotopyHamiltonian_one_compressed_eq_perturbedHamiltonian`, together with the sector-preservation of `V̂` it presupposed, proved separately by PR-6's `liebPerturbationV_preserves_liebHalfFillingPred`; and (c) nonemptiness of the compressed sector, `Nonempty (configSector N (liebHalfFillingPred N nUp))` — **discharged** by PR-6's `configSector_liebHalfFillingPred_nonempty`, an instance hypothesis of `tasaki_lemma_10_1_degenerate_perturbation`. **Debt.** What the compressed layer consumes from the whole-Fock-space layer is only the diagonality of `Ĥ₀` (`liebPerturbationH0_mulVec_basisVec`, read off in the sector basis through `configSectorCompress_apply`), `liebPerturbationH0_posSemidef`, and the entries of `V̂` between singly-occupied configurations; PR-6 additionally consumes the whole-space bridge `homotopyHamiltonian_one_eq_perturbedHamiltonian` and the *definition* `liebPerturbationH0Inv`, whose compression **is** `liebPerturbationH0InvCompressed`. What it does not consume is the *statement* `liebPerturbationH0_isReducedInverse`, because PR-6 proves the compressed contract off the diagonal form (`liebPerturbationH0InvCompressed_eq_diagonal`) instead of transporting the whole-Fock-space one; **that theorem alone is the reference-0 remnant of the pair**, and its deletion debt is still active and unresolved (the definition is referenced and cannot be deleted). Still at reference 0 after PR-6, and so still debt rather than settled API: on the whole Fock space the hard-core projection identity `kernelProjectionMatrix_liebPerturbationH0_eq_hardcoreProjection` (itself the sole consumer of the kernel criterion `mem_matrixKernel_liebPerturbationH0_iff`) and `liebPerturbationH0_isReducedInverse`; on the compressed sector `liebPerturbationH0Compressed_posSemidef` (sole consumer of `liebPerturbationH0_posSemidef`), `liebPerturbationVCompressed_isHermitian`, and the capstone `kernelProjection_mul_liebPerturbationVCompressed_mul_kernelProjection` — PR-6's `secondOrderEffectiveHamiltonian_liebPerturbation_eq` reaches `−P̂₀ V̂\|_K V̂\|_K P̂₀` from the diagonal reduced inverse alone and uses none of these three; and in the generic layer `minEnergyOn_add_const_smul_one` (**deleted in PR-13a** as a decorative reference-0 declaration, per its own doc comment's stipulation, rather than staged a further round; see the PR-13a row in [part 6](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-06/)). The rest were staged, unconsumed until the arc assembly (PR-11 to PR-13). Whatever that assembly does not consume is to be deleted, not kept. | `Fermion/JordanWigner/Hubbard/LiebRepulsivePerturbationSetup.lean` (+ `Math/MatrixAnalysis/DegeneratePerturbation.lean`, `Math/MatrixAnalysis/MinEnergyOnSubspace.lean`, `Fermion/JordanWigner/Hubbard/HubbardImpossibilityLowUVariationalCore.lean`) |
| `liebPerturbationV_preserves_liebHalfFillingPred` / `liebPerturbationH0InvCompressed` / `liebPerturbationH0InvCompressed_eq_diagonal` / `liebPerturbationH0Compressed_isReducedInverse` / `liebPerturbationV_intermediate_weight_eq_one` / `liebPerturbationH0InvCompressed_mul_V_mul_kernelProjection` / `secondOrderEffectiveHamiltonian_liebPerturbation_eq` / `configSector_liebHalfFillingPred_nonempty` / `homotopyHamiltonian_one_compressed_eq_perturbedHamiltonian` | **Superexchange reduced-inverse layer (Theorem 10.4 arc, Issue #5320, PR #5327 PR-6; PROVED, `#print axioms` = std3): not itself a direct formalization of a numbered Tasaki statement — generic infrastructure discharging PR-5 debt items (a)-(c) and preparing the superexchange operator identity that PR-7/PR-8 compute.** Discharges PR-5 debt (a): `liebPerturbationH0Compressed_isReducedInverse` is the compressed `IsReducedInverse` contract for `Ĥ₀|_K`, built directly off the diagonal form (`liebPerturbationH0InvCompressed`, `liebPerturbationH0InvCompressed_eq_diagonal`) rather than by compressing the whole-Fock-space `liebPerturbationH0Inv`. Discharges PR-5 debt (b): `homotopyHamiltonian_one_compressed_eq_perturbedHamiltonian` is the compressed bridge `Ĥ_{s=1}(λ)|_K = Ĥ₀|_K + λV̂|_K`, by linearity of `configSectorCompress` alone; the sector-preservation of `V̂` it presupposed is proved separately, `liebPerturbationV_preserves_liebHalfFillingPred` (every hopping term of `V̂` moves one electron of a fixed spin between two sites, so total electron number and total spin-up number are unchanged). Discharges PR-5 debt (c): `configSector_liebHalfFillingPred_nonempty` (an explicit half-filled witness configuration), the instance hypothesis `tasaki_lemma_10_1_degenerate_perturbation` needs. **Crux**: `liebPerturbationV_intermediate_weight_eq_one` — Tasaki's "the site `x` is doubly occupied in `ĉ†_{x,σ}ĉ_{y,σ}\|Φ⟩`" (eq. (10.1.7), p. 344), read at this arc's `U = 1` normalisation as "the intermediate interaction weight is exactly `1`" (not merely `1/U`); essential use of the one-electron-per-site half-filling hypothesis, since a hard-core configuration with an empty site can absorb the hopping electron and stay hard-core (weight `0`). Combined with the diagonal reduced inverse, this gives `liebPerturbationH0InvCompressed_mul_V_mul_kernelProjection` (`Ĥ₀⁻¹\|_K` acts as the identity on `V̂\|_K · P̂₀`) and the **PR-6 capstone** `secondOrderEffectiveHamiltonian_liebPerturbation_eq`: the second-order effective Hamiltonian collapses to `−(P̂₀ · V̂\|_K · V̂\|_K · P̂₀)`, the object PR-8 computes explicitly as the superexchange sum. **Generic layer reused**: `liebPerturbationV_preserves_liebHalfFillingPred` supplies the entrywise sector-preservation hypothesis needed by the new generic compression homomorphism `configSectorCompress_mul_of_preserves` (`HubbardImpossibilityLowUVariationalCore.lean`, PR-6); the pre-existing number-sector instance `configSectorNumberCompress_mul_of_right_preserves` (`LiebAttractiveFullSectorSU2Algebra.lean`, Theorem 10.2 full-sector-lift arc) was refactored in this PR to derive from that same generic lemma rather than reprove it. **Debt.** Of PR-5's whole-Fock-space reduced-inverse pair this file consumes the *definition* `liebPerturbationH0Inv` — `liebPerturbationH0InvCompressed` is literally `configSectorCompress` applied to it — but not the *statement* `liebPerturbationH0_isReducedInverse`, which the compressed contract re-derives from the diagonal form rather than transporting; that theorem alone stays at reference 0, with its deletion debt unresolved (see PR-5's debt note above). `liebPerturbationV_preserves_liebHalfFillingPred` — **discharged**: PR-8b (`LiebRepulsiveSuperexchange.lean`) consumes it via `configSectorCompress_mul_of_preserves` to identify the compressed product `V̂\|_K · V̂\|_K` with the compression of `V̂ · V̂`. Still staged and unconsumed: `liebPerturbationH0Compressed_isReducedInverse`, `configSector_liebHalfFillingPred_nonempty` (for the Lemma 10.1 application at the arc assembly, PR-11 to PR-13); PR-13a has since consumed `homotopyHamiltonian_one_compressed_eq_perturbedHamiltonian` (`LiebRepulsiveEndpointIdentification.lean`), closing that debt. Each is carried as debt, not settled API; whatever that assembly does not consume is to be deleted, not kept. The layer's capstone `secondOrderEffectiveHamiltonian_liebPerturbation_eq` is the exception (a capstone is the endpoint of its layer): PR-8 computes it explicitly as the superexchange sum. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveSuperexchangeReducedInverse.lean` |
| `isReducedInverse_diagonal` | **The reduced inverse of a real diagonal matrix** (generic infrastructure for the Theorem 10.4 discharge arc, Issue #5320, PR #5327 PR-6; **PROVED**, axiom-free): for a diagonal matrix `diag(d)` with real (self-adjoint) diagonal entries, the diagonal matrix that inverts each nonzero `d i` and is `0` at each zero entry is a reduced inverse (`IsReducedInverse`) of `diag(d)`. Consumed by the arc's compressed reduced-inverse contract `liebPerturbationH0Compressed_isReducedInverse` (`Fermion/JordanWigner/Hubbard/LiebRepulsiveSuperexchangeReducedInverse.lean`), and it also replaces the prior bespoke whole-Fock-space proof of `liebPerturbationH0_isReducedInverse` (`Fermion/JordanWigner/Hubbard/LiebRepulsivePerturbationSetup.lean`) by an instance of this lemma. Not itself a direct formalization of a numbered Tasaki statement. | `Math/MatrixAnalysis/DegeneratePerturbation.lean` |
| `fermionHopReturn` / `fermionHopReturn_same_spin_eq` / `fermionHopReturn_opposite_spin_eq` / `sum_fermionMultiNumber_spinfulIndex_mul_eq` / `fermionSiteSpinPlus_mul_Minus_add_fermionSiteSpinMinus_mul_Plus_eq` / `fermionHopReturn_eq` | **The superexchange operator identity (Theorem 10.4 arc, Issue #5320, PR #5328 PR-7; PROVED, `#print axioms` = std3): a direct formalization of Tasaki's eq. (10.1.9) together with the two auxiliary identities stated just below it (`Σ_σ n̂_{x,σ}n̂_{y,σ} = 2(Ŝ^{(3)}_xŜ^{(3)}_y + ¼n̂_xn̂_y)` and `Ŝ⁺_yŜ⁻_x + Ŝ⁻_yŜ⁺_x = 2(Ŝ^{(1)}_xŜ^{(1)}_y + Ŝ^{(2)}_xŜ^{(2)}_y)`), §10.1, p. 344, and — unlike PR-1 through PR-6 — model-independent, with no dependence on the arc's half-filling sector, hard-core projection, or bipartite endpoint graph.** **Not** eq. (10.1.10) (`Ĥspin = Σ_{x,y}(|t_{x,y}|²/U_x)·2(Ŝ_x·Ŝ_y − ¼)P̂₀`, p. 345): that is a distinct statement needing the hopping weights, the kernel projection `P̂₀` and `n̂_xP̂₀ = P̂₀`, and it is PR-8's target. The capstone `fermionHopReturn_eq` proves, for any two distinct sites `x ≠ y` on the spinful Jordan–Wigner backbone, `Σ_{σ,τ} ĉ†_{y,τ}ĉ_{x,τ}ĉ†_{x,σ}ĉ_{y,σ} = n̂_y − 2Ŝ_x·Ŝ_y − ½n̂_xn̂_y` (`fermionHopReturn`, the round-trip hopping sum). Proved via CAR (canonical anticommutation relations) rather than Jordan–Wigner sign combinatorics: `fermionHopReturn_same_spin_eq` collapses the `σ = τ` diagonal to `n̂_{y,σ} − n̂_{x,σ}n̂_{y,σ}`; `fermionHopReturn_opposite_spin_eq` turns the `σ ≠ τ` off-diagonal into `−Ŝ^{±}_yŜ^{∓}_x`; `sum_fermionMultiNumber_spinfulIndex_mul_eq` and `fermionSiteSpinPlus_mul_Minus_add_fermionSiteSpinMinus_mul_Plus_eq` assemble the two halves into the stated spin-dot/number form. `x ≠ y` is essential (at `x = y` the identity is false). The ladder decomposition reuses the different-site commutation `fermionSiteSpinMinus_mul_Plus_comm` (`Ŝ⁻_xŜ⁺_y = Ŝ⁺_yŜ⁻_x` for `x ≠ y`), which this PR moved to the shared `Fermion/JordanWigner/Hubbard/FermionSiteSpin.lean` so that the §11.5 t-J bond-sum and this identity import one proof instead of two copies. Prepares the sandwiching with the kernel projection `P̂₀` that PR-8 (`LiebRepulsiveSuperexchange.lean`) performs on the half-filled hard-core sector. **Debt.** `fermionHopReturn_eq` — **discharged**: PR-8b (`LiebRepulsiveSuperexchange.lean`) consumes it directly (`fermionHopReturn_apply_eq_of_singly_occupied`'s half-filling diagonal collapse rewrites through it), sandwiching the identity between `P̂₀` on the half-filled hard-core sector to reach eq. (10.1.10). The four feeder lemmas (`fermionHopReturn_same_spin_eq`, `fermionHopReturn_opposite_spin_eq`, `sum_fermionMultiNumber_spinfulIndex_mul_eq`, `fermionSiteSpinPlus_mul_Minus_add_fermionSiteSpinMinus_mul_Plus_eq`) are each consumed inside this file, by `fermionHopReturn_eq` alone, and have no consumer outside it — a single-consumer chain for the arc assembly to judge, not decoration. Axiom-free. | `Fermion/JordanWigner/Hubbard/SuperexchangeOperatorIdentity.lean` |
| `sum_val_update_hop_site` / `hop_pair_apply_eq_zero_of_ne` / `liebPerturbationV_sq_apply_eq_of_singly_occupied` | **The hop-pair collapse for `V̂·V̂` (Theorem 10.4 arc, Issue #5320, PR #5329 PR-8a; PROVED, `#print axioms` = std3): a direct formalization of Tasaki's collapse argument at eq. (10.1.8), p. 344 — "one electron must hop from `x` to `y` to resolve the double occupancy so that the excited state returns to the subspace `H₀`" — not generic infrastructure.** On the singly-occupied (half-filled hard-core) sector, expanding `V̂·V̂ = Σ_{σ,i,j,τ,k,l} t_{ij}t_{kl}(ĉ†_{i,σ}ĉ_{j,σ})(ĉ†_{k,τ}ĉ_{l,τ})` and evaluating a matrix entry between two singly-occupied configurations collapses the double sum to the diagonal `i = l ∧ j = k` (`hop_pair_apply_eq_zero_of_ne`, via the per-site occupancy transport lemma `sum_val_update_hop_site`, replacing nested `Function.update` case splits): the right factor (`k ≠ l`) forces an intermediate configuration with site `k` doubly occupied and site `l` empty, and the left factor (`i ≠ j`) can then only reach a singly-occupied result by hopping out of the doubly-occupied site and into the emptied one. The **capstone** `liebPerturbationV_sq_apply_eq_of_singly_occupied` proves `(V̂·V̂) e c = Σ_{x,y} (t_{yx}·t_{xy})·fermionHopReturn N x y e c` on the whole Fock space: the surviving `(i,j,k,l) = (l,k,k,l)` term is literally the `(τ',σ') = (σ,τ)` summand of PR-7's `fermionHopReturn N k l`, no Jordan–Wigner sign computation needed (both sides carry the same operator, `jwSign` cancels by construction). The coefficient is left as the **asymmetric** product `t_{yx}·t_{xy}` — reducing it to the endpoint-graph indicator `t_{xy}²` needs the additional hopping-symmetry hypothesis `hT`, which is **not** required here and is deferred to PR-8b. Same-site (`i = j` or `k = l`) terms vanish upstream via the coefficient `liebEndpointHopping_diag_eq_zero` (not a filter on the sum). **Debt.** The capstone `liebPerturbationV_sq_apply_eq_of_singly_occupied` — **discharged**: PR-8b (`LiebRepulsiveSuperexchange.lean`) consumes it directly in the `hentry` step of its own capstone, reducing its right-hand side further via the coefficient reduction and the half-filling diagonal collapse and lifting the result to the compressed sector `K`. Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveSuperexchangeCollapse.lean` |
| `liebEndpointGraph` / `liebEndpointGraph_adj` / `liebEndpointHopping_sq_eq_indicator` / `liebEndpointHopping_mul_symm_eq_indicator` / `fermionHopReturn_apply_eq_of_singly_occupied` / `liebPerturbationVCompressed_sq_eq_configSectorCompress` / `kernelProjection_mul_liebPerturbationVCompressed_sq_mul_kernelProjection` / `secondOrderEffectiveHamiltonian_liebPerturbation_eq_tJExchange` | **The superexchange capstone — Tasaki eq. (10.1.10), p. 345 (Theorem 10.4 arc, Issue #5320, PR #5330 PR-8b; PROVED, `#print axioms` = std3): the second and final file of the superexchange-identity sub-arc (PR-6 to PR-8), completing the identity PR-7's model-independent `fermionHopReturn_eq` and PR-8a's whole-Fock-space collapse `liebPerturbationV_sq_apply_eq_of_singly_occupied` set up.** `liebEndpointGraph A` is the complete bipartite graph on the bipartition `(A, Aᶜ)` (the arc's intentional endpoint-graph substitution for the book's literal bond set `{ {x,y} \| t_{x,y}≠0 }`, documented since PR-4), defined not as a fresh `SimpleGraph` but as the `Finset` specialisation of the sublattice-indicator graph `bipartiteGraphFromA` (`Quantum/MarshallLiebMattis/BipartiteGraph.lean`) at the indicator `fun x => decide (x ∈ A)`, with `liebEndpointGraph_adj` recovering the `Finset` adjacency `x ∈ A ↔ y ∉ A` this file's proofs use. The coefficient reduction `liebEndpointHopping_mul_symm_eq_indicator` turns PR-8a's surviving **asymmetric** product `t_{yx}·t_{xy}` into the endpoint-graph indicator `t_{xy}²`, under the additional symmetry hypothesis `hT : ∀ x y, T x y = T y x` — genuinely necessary (not decoration): without it an asymmetric `T` with `T x y > 0 > T y x` gives `t_{yx}t_{xy} = −1` against indicator `+1`. `fermionHopReturn_apply_eq_of_singly_occupied` collapses PR-7's `fermionHopReturn_eq` number-operator remainder `n̂_y − ½n̂_xn̂_y` to `½n̂_xn̂_y` on the half-filled hard-core sector (both `n̂_x`, `n̂_y` act with eigenvalue `1`), turning it into `2((1/4)n̂_xn̂_y − Ŝ_x·Ŝ_y)`. `liebPerturbationVCompressed_sq_eq_configSectorCompress` lifts `V̂\|_K·V̂\|_K` to the compression of the whole-Fock-space `V̂·V̂` via `configSectorCompress_mul_of_preserves` applied to PR-6's `liebPerturbationV_preserves_liebHalfFillingPred`. **Capstone** `kernelProjection_mul_liebPerturbationVCompressed_sq_mul_kernelProjection`: `P̂₀·V̂\|_K·V̂\|_K·P̂₀ = 2•(P̂₀·compress(tJExchange(liebEndpointGraph A))·P̂₀)`. **Corollary for PR-9/PR-10** `secondOrderEffectiveHamiltonian_liebPerturbation_eq_tJExchange`, composing with PR-6's `secondOrderEffectiveHamiltonian_liebPerturbation_eq`: `Ĥeff = −2•(P̂₀·compress(tJExchange(liebEndpointGraph A))·P̂₀)`, i.e. Tasaki's `Ĥspin = Σ_{x,y}(\|t_{x,y}\|²/U_x)·2(Ŝ_x·Ŝ_y−¼)P̂₀` at this arc's `U=1` normalisation and on the complete-bipartite endpoint graph. This closes the superexchange sub-arc (PR-6 reduced-inverse layer, PR-7 model-independent identity, PR-8a whole-Fock-space collapse, PR-8b endpoint-graph capstone); nothing here yet depends on the sub-arc's own debt (all cleared above), and the capstone is consumed inside this file by the corollary (as the four feeder lemmas, `liebEndpointGraph` and its adjacency lemma `liebEndpointGraph_adj` are consumed by the capstone chain), leaving `secondOrderEffectiveHamiltonian_liebPerturbation_eq_tJExchange` as the file's only declaration at reference 0; PR-9a (Issue #5320, PR #5331) consumed it and resolved this debt (see the PR-9a row below). | `Fermion/JordanWigner/Hubbard/LiebRepulsiveSuperexchange.lean` |
| `liebHardCoreHalfFillingPred` / `liebHardCoreDownOccupation` / `liebHardCoreDownOccupation_magSumS_eq` / `liebHardCoreToMagConfigS` / `liebHardCoreOfMagConfigSFock` / `liebHardCoreOfMagConfigSFock_apply_up` / `liebHardCoreOfMagConfigSFock_apply_down` / `liebHardCoreOfMagConfigSFock_mem_pred` / `liebHardCoreOfMagConfigS` / `liebHardCoreToMagConfigS_apply_eq_zero_iff_up_occupied` / `singlyOccupied_eq_iff_downOccupation` / `liebHardCoreOfMagConfigS_liebHardCoreToMagConfigS` / `liebHardCoreToMagConfigS_liebHardCoreOfMagConfigS` / `liebHardCoreHalfFillingSectorEquivS` / `liebHardCoreSiteState` / `liebHardCoreSiteState_ne_zero` / `tJConfigOf_liebHardCoreSiteState_eq` / `fermionSiteNumber_mul_apply_eq_ite_of_singlyOccupied` / `fermionSpinDot_apply_eq_spinSDot_of_singlyOccupied` / `liebHardCoreToAmbientSector` / `kernelProjection_mul_liebPerturbationVCompressed_sq_mul_kernelProjection_reindex_eq` / `secondOrderEffectiveHamiltonian_liebPerturbation_reindex_eq_heisenbergOnMagSector` | **The Fermion–Spin bridge, first half — sector `Equiv` + Hamiltonian reindexing (Theorem 10.4 arc, Issue #5320, PR #5331 PR-9a; PROVED, `#print axioms` = std3): the first of the two-PR 9a/9b split of "PR-9: Fermion-Spin bridge", bridging the half-filled hard-core Fock sector to a spin-`1/2` magnetization sector so that PR-8b's superexchange capstone can feed Theorem 2.3 in PR-10.** `liebHardCoreHalfFillingPred` strengthens `liebHalfFillingPred` (double occupancy excluded, `LiebRepulsivePerturbationSetup.lean`) with single occupancy at every site, since `configSector N (liebHalfFillingPred N nUp)` alone is not in bijection with `magConfigS`. **The sector `Equiv`** `liebHardCoreHalfFillingSectorEquivS` (`M = N + 1 − nUp`, the **down**-count, matching `magSumS`/`spinSOp3`'s `0`=up/`1`=down convention): forward `liebHardCoreToMagConfigS` reads off the down-orbital occupation, backward `liebHardCoreOfMagConfigS` sets `c(spinfulIndex x 0) = 1 − σ x`, needing `nUp ≤ N + 1` for the truncated-subtraction round-trip; `liebHardCoreToMagConfigS_apply_eq_zero_iff_up_occupied` pins the up/down reading against a silent flip; `singlyOccupied_eq_iff_downOccupation` (the injectivity core of the forward map: a singly occupied configuration is already determined by its down-orbital occupation, the up orbital being the complement) is public rather than `private` because PR-9b's diagonal step consumes it across the module boundary. The **t-J reuse route** (`liebHardCoreSiteState`, `tJConfigOf_liebHardCoreSiteState_eq`) lets the crux **entrywise identity** `fermionSpinDot_apply_eq_spinSDot_of_singlyOccupied` reuse the already-proved sign-free t-J bond action (`tJExchangeBond_mulVec_tJConfigOf_full`, `TJHalfFillingBondAction.lean`) instead of re-deriving Jordan–Wigner sign combinatorics. **Capstones**: the **raw form** `kernelProjection_mul_liebPerturbationVCompressed_sq_mul_kernelProjection_reindex_eq` lands on the constant `\|A\|(N+1−\|A\|)` **minus** the antiferromagnetic Heisenberg matrix `heisenbergHamiltonianSMatrixOnMagSector ((2:ℂ) • bipartiteCoupling (fun x => decide (x ∈ A))) 1 (N+1−nUp)` (**constant − Heisenberg**), while its PR-8b `Ĥeff`-corollary reindex `secondOrderEffectiveHamiltonian_liebPerturbation_reindex_eq_heisenbergOnMagSector` lands on the *same* Heisenberg matrix **minus** the *same* constant (**Heisenberg − constant**, the opposite order) — exactly the shape PR-10 needs for Theorem 2.3. **The constant shift is `\|A\|(N+1−\|A\|)` (no `/2`)**, for both capstones: the `tJExchange`-level constant `κ = \|A\|(N+1−\|A\|)/2` is itself multiplied by the same factor `2` that the `Ĥeff` identity `P̂₀V̂V̂P̂₀ = 2•(P̂₀·tJExchange·P̂₀)` uses to scale the bond coupling to `(2:ℂ) • bipartiteCoupling`, giving the surviving shift `2κ = (A.card : ℂ) * (N+1−A.card : ℂ)` (no division). **Debt discharged**: this PR consumes PR-8b's sole reference-0 declaration `secondOrderEffectiveHamiltonian_liebPerturbation_eq_tJExchange`, closing that debt. **Debt**: PR-11a has since consumed the `Ĥeff` corollary `secondOrderEffectiveHamiltonian_liebPerturbation_reindex_eq_heisenbergOnMagSector`, closing that debt; the raw capstone `kernelProjection_mul_liebPerturbationVCompressed_sq_mul_kernelProjection_reindex_eq` is consumed inside the file by that corollary. The convention-guard lemma `liebHardCoreToMagConfigS_apply_eq_zero_iff_up_occupied` remains at reference 0 deliberately, as a single visible pinning statement, not decoration. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveFermionSpinBridge.lean` |
| `fermionTotalSpinSquared_eq_sum_fermionSpinDot` / `fermionSpinDot_self_eq` / `fermionTotalSpinSquared_apply_eq_totalSpinSSquared_of_singlyOccupied` / `fermionTotalSpinSquared_reindex_eq_totalSpinSSquaredOnMagSector` | **The Fermion–Spin bridge, second half — the ladder-vs-Cartesian Casimir bridge (Theorem 10.4 arc, Issue #5320, PR #5332 PR-9b; PROVED, `#print axioms` = std3): the second of the two-PR 9a/9b split of "PR-9: Fermion-Spin bridge", completing the Fermion–Spin bridge.** PR-9a's module docstring flagged as separate work the comparison of `fermionTotalSpinSquared` with `totalSpinSSquared`. **What the two Casimirs are, and what is *not* claimed of them**: `fermionTotalSpinSquared` (ladder form `Ŝ⁻_totŜ⁺_tot + Ŝ³_tot(Ŝ³_tot+1)`) is **not** a PR-9a declaration — it is the §11.1.1 saturated-ferromagnetism operator of `SaturatedFerromagnetism.lean`, acting on the whole Hubbard Fock space `Fin (2N+2) → Fin 2` — while `totalSpinSSquared` (the spin-`1/2` Cartesian Casimir `(Ŝ¹)²+(Ŝ²)²+(Ŝ³)²`, `Quantum/SpinS/TotalSquaredCore.lean`) acts on spin configurations `Fin (N+1) → Fin 2`. They are therefore **not operators on a common space**, and no identity between them *as operators* is even well-typed; the content of this PR is the **entrywise** correspondence on the hard-core half-filled sector, each fermionic matrix entry between two hard-core configurations equalling the Cartesian entry between their images under PR-9a's sector `Equiv` (`liebHardCoreHalfFillingSectorEquivS`). The restriction to that sector is essential rather than a convenience: off it the fermionic Casimir also sees empty and doubly occupied sites, whose same-site dot vanishes instead of contributing the spin-`1/2` value `3/4`. **Route**: both Casimirs are re-expressed as the *same* double sum of two-site dots `Σ_{x,y} Ŝ_x·Ŝ_y` — `fermionTotalSpinSquared_eq_sum_fermionSpinDot` for the fermionic ladder form (via the SU(2) ladder commutator `Ŝ⁺_totŜ⁻_tot − Ŝ⁻_totŜ⁺_tot = 2Ŝ³_tot`), matching the pre-existing spin-side expansion `totalSpinSSquared_eq_sum_spinSDot`. The off-diagonal terms of the double sum are PR-9a's crux `fermionSpinDot_apply_eq_spinSDot_of_singlyOccupied` verbatim; the new same-site diagonal term is computed in closed form by `fermionSpinDot_self_eq`, `Ŝ_x·Ŝ_x = (3/4)(n̂_{x↑}+n̂_{x↓}−2n̂_{x↑}n̂_{x↓})` (from the CAR `ĉĉ† = 1−n̂` and idempotence `n̂²=n̂`), which collapses to the scalar `3/4` on a hard-core configuration, matching the spin-`1/2` value `S(S+1)=3/4` (`spinSDot_self`). **Crux** `fermionTotalSpinSquared_apply_eq_totalSpinSSquared_of_singlyOccupied`: the entrywise identity on hard-core half-filled bra/ket Fock configurations, stated directly on Fock-space matrix entries (no `submatrix` plumbing), the same shape as PR-9a's crux. **Capstone** `fermionTotalSpinSquared_reindex_eq_totalSpinSSquaredOnMagSector`: packages the crux into the `submatrix`-along-the-sector-`Equiv` form matching PR-9a's capstone shape, restricting `fermionTotalSpinSquared N` to the hard-core sub-sector and reindexing onto `totalSpinSSquared (Fin (N+1)) 1` on the magnetization sector — the shape needed to transport the Casimir eigenvalue from Theorem 2.3 back to the fermionic ground states in the later PR-11/PR-12 assembly steps. **This closes the Fermion–Spin bridge arc (PR-9a sector `Equiv` + Hamiltonian reindexing, PR-9b Casimir bridge)**; also non-private'd `singlyOccupied_eq_iff_downOccupation` (`LiebRepulsiveFermionSpinBridge.lean`, PR-9a) to reuse it across the module boundary instead of duplicating it, and put the per-site spin-z sum `fermionTotalSpinZ_eq_sum_fermionSiteSpinZ` next to its `Plus`/`Minus` siblings in `FermionSiteSpin.lean` rather than here, that being the file that owns the per-site/total correspondence. **Debt discharged**: the capstone `fermionTotalSpinSquared_reindex_eq_totalSpinSSquaredOnMagSector` was this file's **only** reference-0 declaration at the time of this PR — the crux is consumed by the capstone, `fermionSpinDot_self_eq` by the crux's diagonal step (through the private `fermionSpinDot_self_mulVec_basisVec_of_singlyOccupied` / `fermionSpinDot_self_apply_of_singlyOccupied` chain), and the expansion `fermionTotalSpinSquared_eq_sum_fermionSpinDot` by the crux, so no feeder lemma was left unconsumed; PR-10b (`LiebRepulsiveTheorem23Instance.lean`) has since consumed the capstone itself, closing that debt. PR-9a's own debt is unchanged by this PR: its `Ĥeff` corollary remained staged until PR-11a consumed it (see the PR-11a entry in [part 6](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-06/)). Axiom-free. | `Fermion/JordanWigner/Hubbard/LiebRepulsiveFermionSpinCasimirBridge.lean` |

---

[← Multi-mode fermion via Jordan–Wigner (P2 backbone)](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-04/) · [Catalogue](/lattice-system/formalization/legacy/) · [Multi-mode fermion via Jordan–Wigner (P2 backbone) →](/lattice-system/formalization/legacy/30-multi-mode-fermion-via-jordan-wigner-p2-backbone-part-06/)
