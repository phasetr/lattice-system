---
layout: page
title: "Legacy catalogue: Spin-`S` saturated ferromagnetic state (Tasaki §2.4 generalised) (part 2 of 2)"
permalink: /formalization/legacy/28-spin-saturated-ferromagnetic-state-tasaki-2-4-generalised-part-02/
---

# Legacy catalogue: Spin-`S` saturated ferromagnetic state (Tasaki §2.4 generalised) (part 2 of 2)

> **Interim authority.** This lossless catalogue chunk remains authoritative for formalization status and capstone identification until Issue #5228. The version 1 JSON catalogue is still a non-authoritative prototype.

[Interim catalogue](/lattice-system/formalization/legacy/) › [Spin foundations and Tasaki Chapter 2](/lattice-system/formalization/legacy/#group-spin-foundations)

<!-- legacy-source:start:2129:2148 -->
| Lean name | Statement |
|---|---|
| `twoSpinPlusMinus_ladder_signed_entry_re_pos_of_off_two_site_raise_lower` / `exists_twoSpinPlusMinus_ladder_signed_entry_re_pos_of_bipartite_ne_balanced_sector` / `twoSpinCorrelationS_bipartite_signed_re_pos_of_marshall_balanced_sector_cross_eigenphase` | **Problem 2.5.d balanced-sector ladder witness**: supplies the strict sector-local `S_x^+ S_y^-` matrix-entry witness required by PR #4076 at the balanced sector `M0 = |A| * N`. The witness starts from the complement Néel configuration, updates the two cross-sublattice sites touched by the ladder operator, and uses `magSumS_configUpdateTwo_eq` plus `magSumS_neelConfigOfS_complement` to keep both configurations in `M0`. The resulting wrapper removes the explicit strict-entry hypothesis from the sector-supported eigenspace-phase theorem at the balanced sector. Tasaki, Springer 2020, Problem 2.5.d, p. 40 and solution pp. 498-499, equations (S.22)-(S.23), with Theorems 2.3-2.4 context, pp. 42-44 (PR #4077, files `Quantum/SpinS/Problem25dBalancedSectorWitnessCore.lean` for the signed ladder entries and balanced-sector witness + `Quantum/SpinS/Problem25dBalancedSectorWitness.lean` for the balanced-sector dot-product wrapper, split for build speed) |
| `phase_unit_of_unitary_mulVec_eq_smul_of_ne_zero` / `exists_phase_unit_of_finrank_eigenspace_le_one_of_unitary_commute_of_ne_zero` / `twoSpinCorrelationS_bipartite_signed_re_pos_of_tasaki23_balanced_pf_cross` | **Problem 2.5.d balanced Perron-Frobenius endpoint**: extracts the balanced sector PF vector from the Theorem 2.3 common-energy data, rewrites the existing real-sign embedding into the sector-supported Marshall form using `marshallSignS_eq_ofReal_re`, and derives the axis-swap / z-rotation unit-modulus phases without normalizing the vector. The endpoint returns the common Hermitian minimum, the strictly positive balanced coefficients, the non-zero embedded eigenvector, the full eigenspace `finrank ≤ 1` input from the SU(2) endpoint, and the signed cross-sublattice two-spin correlation positivity for that concrete PF vector. Tasaki, Springer 2020, Problem 2.5.d, p. 40 and solution pp. 498-499, equations (S.22)-(S.23), with Theorems 2.3-2.4 context, pp. 42-44 (PR #4078, files `Quantum/SpinS/Problem25dBalancedPFEndpointCore.lean` for the non-normalized phase bridge + `Quantum/SpinS/Problem25dBalancedPFEndpoint.lean` for the balanced PF endpoint correlation positivity, split for build speed) |
| `twoSpinCorrelationS_sign_cases_of_tasaki23_balanced_pf_cross` | **Problem 2.5.d balanced Perron-Frobenius sign cases**: applies the final bipartite-gauge sign conversion to the concrete balanced PF endpoint from PR #4078. The wrapper preserves the Hermitian minimum, strictly positive balanced coefficients, non-zero embedded eigenvector, and full eigenspace `finrank ≤ 1` package, while replacing the signed-positivity conclusion by the four Boolean same-sublattice positive / cross-sublattice negative sign cases for the original two-spin correlation. Tasaki, Springer 2020, Problem 2.5.d, p. 40 and solution pp. 498-499, equations (S.22)-(S.23), with Theorems 2.3-2.4 context, pp. 42-44 (PR #4079, file `Quantum/SpinS/Problem25dBalancedPFSignCases.lean`) |
| `twoSpinCorrelationS_re_neg_of_tasaki23_balanced_pf_cross` | **Problem 2.5.d balanced Perron-Frobenius cross sign**: extracts the concrete non-vacuous cross-sublattice conclusion from PR #4079. Under `A x ≠ A y`, the Boolean sign cases collapse to the original two-spin correlation inequality `(twoSpinCorrelationS x y Φ).re < 0` for the same balanced PF vector, while preserving the Hermitian minimum, positive coefficients, non-zero eigenvector, and `finrank ≤ 1` package. Tasaki, Springer 2020, Problem 2.5.d, p. 40 and solution pp. 498-499, equations (S.22)-(S.23), with Theorems 2.3-2.4 context, pp. 42-44 (PR #4080, file `Quantum/SpinS/Problem25dBalancedPFCrossSign.lean`) |
| `spinSOp{Plus,Minus,1,2,3}_one_eq_spinHalfOp{Plus,Minus,1,2,3}` | **spin-`S` ↔ spin-`1/2` bridge at `N = 1`**: `spinSOp{Plus, Minus, 1, 2, 3} 1 = spinHalfOp{Plus, Minus, 1, 2, 3}` (each is the corresponding half-Pauli matrix) (PRs #922 + #923, file `Quantum/SpinS/SpinHalfSpecialization.lean`) |
| `onSiteS_spinSOp3_mulVec_allAlignedStateS` / `allAlignedStateS_expectation_onSiteS_spinSOp3` / `_sq` / `onSiteS_spinSOp3_sq_mulVec_allAlignedStateS` | **single-site `Ŝ^(3)_x` and `(Ŝ^(3)_x)²` on `|c..c⟩`**: `Ŝ^(3)_x · |c..c⟩ = (N/2 − c.val) · |c..c⟩` and expectation of `(Ŝ^(3)_x)²` is `(N/2 − c.val)²` (PR #925, file `Quantum/SpinS/SingleSiteZExpectation.lean`) |
| `allAlignedStateS_expectation_onSiteS_spinSOp1_sq_add_spinSOp2_sq` | **xy-plane Casimir expectation**: `⟨((Ŝ^(1)_x)² + (Ŝ^(2)_x)²) · |c..c⟩⟩ = N(N+2)/4 − (N/2 − c.val)²`. From #920 minus #925; for `c=0` gives `S/2` (PR #926, file `Quantum/SpinS/SingleSiteXYExpectation.lean`) |
| `basisVecS_expectation_onSiteS_spinSOp1` / `_spinSOp2` / `allAlignedStateS_expectation_onSiteS_spinSOp1` / `_spinSOp2` | **transverse mean is zero**: `⟨basisVecS σ, Ŝ^(α)_x · basisVecS σ⟩ = 0` for `α = 1, 2` (transverse operators are purely off-diagonal). Specialised to `|c..c⟩` (PR #927, file `Quantum/SpinS/SingleSiteTransverseMeanZero.lean`) |
| `spinSDot_mulVec_allAlignedStateS_zero_of_ne` | **per-pair eigenvalue**: for `x ≠ y`, `Ŝ_x · Ŝ_y · |σ_⊤⟩ = (N²/4) · |σ_⊤⟩`. Proof via `spinSDot_eq_plus_minus`: ladder annihilations + `(3)(3) → (N/2)²` (PR #939, file `Quantum/SpinS/SpinSDotAllAlignedZero.lean`) |
| `spinSDot_mulVec_allAlignedStateS_last_of_ne` | symmetric raising-side per-pair eigenvalue on `|σ_⊥⟩` (PR #940, file `Quantum/SpinS/SpinSDotAllAlignedLast.lean`) |
| `allAlignedStateS_zero_expectation_heisenbergHamiltonianS` / `_last_expectation_heisenbergHamiltonianS` | **Heisenberg expectation on saturated states**: `⟨|σ_⊤⟩, H · |σ_⊤⟩⟩ = saturatedFerromagnetEigenvalueS J N`; `⟨|σ_⊥⟩, H · |σ_⊥⟩⟩ = H(σ_⊥, σ_⊥)` (PR #943, file `Quantum/SpinS/SaturatedHeisenbergExpectation.lean`) |
| `heisenbergHamiltonianS_diag_allAlignedConfigS_last_eq_zero` | **`H(σ_⊥, σ_⊥) = saturatedFerromagnetEigenvalueS J N`**: both extremal H-diagonals equal (via #875/#876 same explicit formula + uniqueness on non-zero eigenvectors) (PR #946, file `Quantum/SpinS/SaturatedHeisenbergSymmetric.lean`) |
| `saturatedFerromagnetEigenvalueS_explicit` / `saturatedFerromagnetEigenvalueS_im_zero` / `saturatedFerromagnetEigenvalueS_exists_real` | **explicit form and realness**: `saturatedFerromagnetEigenvalueS J N = ∑_x ∑_y J(x,y) · (if x = y then N(N+2)/4 else (N/2)²)`, and real couplings make this saturated energy a real scalar. (PR #951 and Tasaki §2.5 Theorem 2.3 follow-up, file `Quantum/SpinS/SaturatedEigenvalueExplicit.lean`) |
| `allAlignedConfigS_injective` / `allAlignedStateS_ne_of_ne` | distinct constants give distinct configurations and distinct states for `[Nonempty V]` (PR #956, file `Quantum/SpinS/AllAlignedStateDistinct.lean`) |
| `allAlignedStateS_inner_of_ne` | all-aligned states at distinct constants are orthogonal (PR #960, file `Quantum/SpinS/AllAlignedStateOrthogonal.lean`) |
| `allAlignedStateS_mem_magSubspaceS` | `|c..c⟩ ∈ magSubspaceS V N (|V|·N/2 − |V|·c.val)` for any `c` (PR #962, file `Quantum/SpinS/AllAlignedStateMagSubspace.lean`) |

References: H. Tasaki, *Physics and Mathematics of Quantum Many-Body
Systems*, Springer 2020, §2.4 (pp. 30–37, spin-1/2 case).

<!-- legacy-source:end:2129:2148 -->

## Authoritative supplemental implementation record (Problem 2.4.b saturated coherent amplitude and weight vectors)

This section is maintained by hand, lies outside the migrated catalogue block above, and records
a new capstone added after the migration baseline; it is not subject to the frozen byte-for-byte
parity of the block above.

| Lean name | Statement | File |
|---|---|---|
| `saturatedCoherentAmp` / `saturatedCoherentAmp_ne_zero` / `saturatedCoherentState` / `saturatedGlobalRot2` / `saturatedGlobalRot3` / `onSiteS_mulVec_prod` / `saturatedCoherentState_zero_apply` | **Problem 2.4.c eq. (S.18) product form (foundation for Problem 2.4.b)** (**PROVED**, `#print axioms saturatedCoherentState_zero_apply` = std3; Tasaki, *Physics and Mathematics of Quantum Many-Body Systems*: the coherent state is eq. (2.4.6), p. 33, and the global rotation `Û_θ^{(α)} = exp(-iθ Ŝ_tot^{(α)})` is eq. (2.2.11), p. 22; the site-product form proved here is eq. (S.18) of the solution to Problem 2.4.c, p. 497, stated there for `S = 1/2`; Problem 2.4.b is stated on p. 34 with solution on pp. 496–497, eq. (S.17)): for the generic-spin (`N = 2S`) saturated coherent state `Ξ_{θ,φ} := Û_φ^{(3)} Û_θ^{(2)} Φ↑` (`saturatedCoherentState`, built from the total-spin rotation matrix exponentials `saturatedGlobalRot2`/`saturatedGlobalRot3`), at `φ = 0` its coefficient at basis configuration `σ` is the product of one-site amplitudes, `Ξ_{θ,0}(σ) = ∏_x saturatedCoherentAmp N θ (σ x)`, with the one-site amplitude `saturatedCoherentAmp N θ j = √(binom N j) · cos(θ/2)^{N−j} · sin(θ/2)^j`, and `saturatedCoherentAmp_ne_zero` proves this amplitude is nonzero for `0 < θ < π`. Proved from uniqueness of solutions of the linear ODE `ẋ = -i Ŝ_tot^{(2)} x` (via `onSiteS_mulVec_prod`, the Leibniz action of a one-site operator on a product state): both the matrix-exponential coherent state and the amplitude product solve this ODE and agree at `θ = 0`, so the half-angle `θ/2` in the amplitude is forced by the ODE rather than inserted by hand. This is the product form, eq. (S.18) of the solution to Problem 2.4.c, that the solution to Problem 2.4.b starts from (it expands `Û_θ^{(2)} Φ↑ = Σ_M c_M Φ_M` and needs every `c_M` to be nonzero); the Fourier-inversion capstone eq. (S.17), the solution to Problem 2.4.b itself, expressing `Φ_M` in terms of `Ξ_{θ,φ}`, is proved below | `Quantum/SpinS/SaturatedCoherentAmplitude.lean`; `Math/Combinatorics/SqrtChooseLadder.lean` |
| `saturatedLadderNorm` / `saturatedWeightVector` / `saturatedCoherentCoeff` / `ladderIterateUp_apply_eq_zero_of_magEigenvalueS_ne` / `saturatedCoherentState_zero_eq_sum` / `saturatedCoherentCoeff_ne_zero` | **Problem 2.4.b weight vectors, expansion, and coefficient nonvanishing** (**PROVED**, `#print axioms saturatedCoherentState_zero_eq_sum` and `#print axioms saturatedCoherentCoeff_ne_zero` = std3; Tasaki, *Physics and Mathematics of Quantum Many-Body Systems*: the normalised magnetisation-sector state `Φ_M` is eq. (2.4.9), p. 33; the expansion of a saturated-ferromagnet ground state `Φ = Σ_M c_M Φ_M` is Theorem 2.1 / eq. (2.4.10), p. 34, instantiated here at `Φ = Ξ_{θ,0}`; Problem 2.4.b is stated on p. 34 with solution on pp. 496–497, eq. (S.17)): `saturatedWeightVector` normalises the `k`-th ladder iterate `(Ŝ_tot^-)^k Φ↑` by its `ℓ²` norm `saturatedLadderNorm` to give `Φ_M` at `M = |Λ|S − k`; `saturatedCoherentCoeff` is `c_M(θ) = ⟪Φ_M, Ξ_{θ,0}⟫`; `saturatedCoherentState_zero_eq_sum` proves `Ξ_{θ,0} = Σ_M c_M(θ) Φ_M` from the identification of the joint `(Ĥ, (Ŝ_tot)²)`-eigenspace with the span of the ladder iterates and their pairwise orthogonality; and `saturatedCoherentCoeff_ne_zero` proves every `c_M(θ) ≠ 0` for `0 < θ < π` from the disjointness of the magnetisation-sector supports together with the site-product form of `Ξ_{θ,0}`: at a configuration where a given sector's ladder iterate is nonzero, every other sector contributes zero to the expansion, forcing the coefficient to be a nonzero multiple of the nonzero product value, with no closed-form coefficient needed. These are exactly the ingredients Problem 2.4.b's solution needs to invert the expansion by Fourier inversion; the inversion itself, eq. (S.17), expressing `Φ_M` in terms of `Ξ_{θ,φ}`, is proved below | `Quantum/SpinS/SaturatedCoherentWeight.lean` (PR #5382) |
| `saturatedCoherentState_apply_phase` / `saturatedCoherentState_eq_sum` / `tasaki_problem_2_4_b_phase_projection` / `Math.integral_exp_int_weight_smul_sum` | **Problem 2.4.b capstone: the Fourier-inversion projection formula, eq. (S.17)** (**PROVED**, `#print axioms tasaki_problem_2_4_b_phase_projection` = std3; Tasaki, *Physics and Mathematics of Quantum Many-Body Systems*: Problem 2.4.b, statement p. 34, solution pp. 496–497, eq. (S.17); `Φ_M` is eq. (2.4.9), p. 33; the expansion is Theorem 2.1 / eq. (2.4.10), p. 34; the coherent state `Ξ_{θ,φ}` is eq. (2.4.6), p. 33; the global rotations are eq. (2.2.11), p. 22): `saturatedCoherentState_apply_phase` proves the azimuthal phase factorisation `Ξ_{θ,φ}(σ) = e^{-iφM(σ)} Ξ_{θ,0}(σ)` (the general-`S` form of the per-site phases of eq. (S.18), p. 497); `saturatedCoherentState_eq_sum` lifts this to the general-`φ` sector expansion `Ξ_{θ,φ} = Σ_M e^{-iφM} c_M(θ) Φ_M`, the identity that gives the middle member of eq. (S.17); `Math.integral_exp_int_weight_smul_sum` is the source-independent finite-Fourier lemma (no spin or lattice content: for a finite family indexed by injective integer weights, integrating the weighted sum against the conjugate character of one weight over a full period keeps only that term, scaled by `2π`) that the capstone applies with the integer sector-index weights; and `tasaki_problem_2_4_b_phase_projection` is the capstone itself, proving for `0 < θ < π` and every sector `k` (with `M = |Λ|S − k`) that `c_M(θ) ≠ 0`, that `∫₀^{2π} dφ e^{iMφ} Ξ_{θ,φ} = 2π c_M(θ) Φ_M` (eq. (S.17)), and hence that `Φ_M = (2π c_M(θ))⁻¹ ∫₀^{2π} dφ e^{iMφ} Ξ_{θ,φ}` — the answer to the problem as posed. This completes Problem 2.4.b | `Quantum/SpinS/SaturatedCoherentProjection.lean`; `Math/Analysis/FiniteExponentialWeightIntegral.lean` (PR #5383) |

---

## Authoritative supplemental implementation record (Problem 2.4.c coherent-state expansion capstone)

This section is maintained by hand, lies outside the migrated catalogue block above, and records
a new capstone added after the migration baseline; it is not subject to the frozen byte-for-byte
parity of the block above.

| Lean name | Statement | File |
|---|---|---|
| `Math.sum_prod_choose_fiber` / `onSiteS_mulVec_apply` / `dotProduct_star_basisVecS` / `norm_toLp_basisVecS_eq_one` / `totalSpinSOpMinus_pow_allAlignedStateS_zero_apply` / `saturatedLadderNorm_eq` / `saturatedWeightVector_apply` / `saturatedCoherentCoeff_eq` / `tasaki_problem_2_4_c_coherent_expansion` | **Problem 2.4.c: closed-form coherent-state expansion, eq. (S.19)** (**PROVED**, `#print axioms tasaki_problem_2_4_c_coherent_expansion` = std3; Tasaki, *Physics and Mathematics of Quantum Many-Body Systems*: Problem 2.4.c, statement p. 34, solution p. 497, eqs. (S.18)–(S.19); `Φ_M` is eq. (2.4.9), p. 33; eq. (2.4.11), p. 34; the coherent state `Ξ_{θ,φ}` is eq. (2.4.6), p. 33): `Math.sum_prod_choose_fiber` is the source-independent `|Λ|`-fold Vandermonde convolution `Σ_{σ, Σ σ_x = k} ∏_x binom N σ_x = binom (|Λ|N) k`, proved by comparing coefficients of `(1+X)^{|Λ|N} = ∏_x (1+X)^N`; `onSiteS_mulVec_apply` is the general one-site-operator matrix-vector action `(onSiteS i A *ᵥ v) σ = Σ_c A(σ i) c · v(update σ i c)`, from which `onSiteS_mulVec_prod` (used by the earlier Problem 2.4.c product-form PR) is re-derived; `dotProduct_star_basisVecS` and `norm_toLp_basisVecS_eq_one` record the two `ℓ²`-level facts about a standard basis vector (pairing a vector against it reads off that component; its `ℓ²` norm is `1`) that the regression fixtures use; `totalSpinSOpMinus_pow_allAlignedStateS_zero_apply` gives the `k`-fold-lowered highest-weight state in closed component form, `k! · ∏_x √(binom N σ_x)` on the fiber `magSumS σ = k` and `0` off it; `saturatedLadderNorm_eq` evaluates its `ℓ²` norm to `k! · √(binom (|Λ|N) k)` via the Vandermonde identity; `saturatedWeightVector_apply` is eq. (2.4.11) in general-`S` form, the normalised sector state `Φ_M` on the fiber; `saturatedCoherentCoeff_eq` is the closed-form sector coefficient of eq. (S.19), `c_M(θ) = √(binom (2S_max)(S_max−M)) cos(θ/2)^{S_max+M} sin(θ/2)^{S_max−M}`; and `tasaki_problem_2_4_c_coherent_expansion` is the capstone, `Ξ_{θ,φ} = Σ_M e^{-iMφ} c_M(θ) Φ_M`, proved for arbitrary real `θ, φ` and general spin `S = N/2` (Tasaki states the problem for `S = 1/2`). Phase convention: the adopted azimuthal factor is `e^{-iMφ}`, matching (S.18) on the same page (per-site phases `e^{∓iφ/2}` give `e^{-i(n↑-n↓)φ/2} = e^{-iMφ}`), (S.17) immediately above (`e^{i(M-M')φ}`, no `/2`), and the already-proved `saturatedCoherentState_apply_phase` derived from eqs. (2.4.6)/(2.2.11); the `/2` in the exponent as printed in (S.19) is inconsistent with all three and is not reproduced | `Math/Combinatorics/ChooseConfigFiber.lean`; `Quantum/SpinS/MultiSite.lean`; `Quantum/SpinS/BasisVecSOrthonormal.lean`; `Quantum/SpinS/SaturatedLadderComponent.lean`; `Quantum/SpinS/SaturatedCoherentExpansion.lean` (PR #5384) |

---

## Authoritative supplemental implementation record (Problem 2.4.d graph-Laplacian quadratic form)

This section is maintained by hand, lies outside the migrated catalogue block above, and records
a new capstone added after the migration baseline; it is not subject to the frozen byte-for-byte
parity of the block above.

| Lean name | Statement | File |
|---|---|---|
| `sum_adj_eq_sum_dart` / `two_sum_edgeFinset_lift_eq_sum_adj` | **generic ordered-adjacency-sum ↔ unordered-edge-sum bridge** (**PROVED**; graph-theoretic, no book content of its own): `sum_adj_eq_sum_dart` rewrites `Σ_x Σ_y [Adj x y] f x y` as a sum over `G.Dart`; `two_sum_edgeFinset_lift_eq_sum_adj` uses the dart fibration (`Dart.edge_fiber = {d, d.symm}`, the same machinery mathlib uses for the handshaking lemma) to show that for a symmetric `f`, `Σ_{e ∈ edgeFinset} f(e) + Σ_{e ∈ edgeFinset} f(e) = Σ_x Σ_y [Adj x y] f x y`; the factor `2` is introduced once as `a + a`, never as `2 • a` or `/2` | `Lattice/Graph.lean` |
| `latticeLaplacian` / `latticeLaplacian_apply` / `tasaki_problem_2_4_d_graph_quadratic_form` | **Problem 2.4.d: the graph-Laplacian quadratic form, eq. (2.4.14)** (**PROVED**, `#print axioms tasaki_problem_2_4_d_graph_quadratic_form` = std3; Tasaki, *Physics and Mathematics of Quantum Many-Body Systems*: Problem 2.4.d, statement p. 35, solution p. 497, eq. (S.20); the lattice Laplacian is eq. (2.4.13), p. 35): `latticeLaplacian` is `Δ := -(G.lapMatrix ℂ)`, i.e. `Δ = A - D`, the sign opposite to mathlib's own `L = D - A`; `latticeLaplacian_apply` is the single lemma pinning that sign entrywise (`Δ_{x,y} = -deg(x)` if `x = y`, `1` if `{x,y}` is an edge, `0` otherwise), so everything downstream reads only this lemma and never unfolds `lapMatrix` again; and `tasaki_problem_2_4_d_graph_quadratic_form` is the capstone, `Σ_{x,y∈Λ} conj(g_x) Δ_{x,y} g_y = -Σ_{ {x,y}∈B } |g_x-g_y|²` for a finite graph `(Λ, B)` and arbitrary `g : Λ → ℂ`, with the right-hand side a genuine unordered `G.edgeFinset` sum (via `Sym2.lift`), matching the book's printed statement rather than a halved ordered double sum (the shape used by mathlib's own `SimpleGraph.lapMatrix_toLinearMap₂'`, which is also non-conjugated and carries the opposite sign, so is not reusable here). Instances are exactly `[Fintype Λ] [DecidableEq Λ] [DecidableRel G.Adj]`; the identity holds with no hypothesis on `g` and no connectivity or non-emptiness assumption | `Lattice/GraphLaplacianQuadraticForm.lean` (PR #5385) |

---

[← Spin-`S` saturated ferromagnetic state (Tasaki §2.4 generalised)](/lattice-system/formalization/legacy/28-spin-saturated-ferromagnetic-state-tasaki-2-4-generalised-part-01/) · [Catalogue](/lattice-system/formalization/legacy/) · [Single-mode fermion (P2 skeleton) →](/lattice-system/formalization/legacy/29-single-mode-fermion-p2-skeleton/)
